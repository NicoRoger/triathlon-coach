---
phase: 02-fitness-test-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_physiology.py
autonomous: true
requirements:
  - FITNESS-02
  - FITNESS-03
  - VERIFY-02

must_haves:
  truths:
    - "Running `python scripts/verify_physiology.py` prints all three discipline sections (BIKE, RUN, SWIM) or an explicit empty-table message"
    - "Each present value shows [OK] or [FUORI RANGE] with the bound limits"
    - "Each section shows CLAUDE.md comparison (match or discrepanza with the expected field)"
    - "If physiology_zones is empty, prints the exact instruction to trigger the processor"
    - "Script is read-only: no DB writes, no sys.exit(1)"
  artifacts:
    - path: "scripts/verify_physiology.py"
      provides: "Read-only informational script — shows physiology zones from DB vs CLAUDE.md"
      min_lines: 80
  key_links:
    - from: "scripts/verify_physiology.py"
      to: "physiology_zones table"
      via: "get_supabase().table('physiology_zones').select()"
      pattern: "physiology_zones.*select"
    - from: "scripts/verify_physiology.py"
      to: "CLAUDE.md"
      via: "Path read for ftp_attuale_w / threshold_pace_per_km / css_attuale_per_100m"
      pattern: "CLAUDE.md|ftp_attuale_w|threshold_pace_per_km|css_attuale_per_100m"
---

<objective>
Create `scripts/verify_physiology.py` — an informational, read-only script that queries the
`physiology_zones` table and prints the current state of FTP (bike), threshold pace (run), and CSS
(swim) for Nicolò. Each value is shown with a plausibility bounds check [OK/FUORI RANGE] and
compared against the corresponding field in CLAUDE.md. If the table is empty, prints an explicit
instruction to trigger the fitness test processor.

Purpose: First deliverable of Phase 2 — gives an immediate read on whether the physiology zones
are populated, plausible, and consistent with CLAUDE.md after the June 2026 tests. D-01 through D-05
per CONTEXT.md.

Output: `scripts/verify_physiology.py` — runnable with `PYTHONPATH=. python scripts/verify_physiology.py`
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-fitness-test-correctness/02-CONTEXT.md
@scripts/verify_analytics.py
@coach/coaching/fitness_test_processor.py
@coach/utils/supabase_client.py
</context>

## Phase Goal

**As a** coach-athlete system operator, **I want to** see the current state of physiology zones (FTP/threshold/CSS) in the DB with bounds checks and CLAUDE.md comparison, **so that** I can immediately know if the June 2026 fitness tests were processed correctly and whether the data is plausible.

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify_physiology.py</name>
  <files>scripts/verify_physiology.py</files>
  <read_first>
    - scripts/verify_analytics.py (structural pattern: load_dotenv before coach.* imports, section functions, logging, main(), no sys.exit)
    - coach/coaching/fitness_test_processor.py (PLAUSIBLE_BOUNDS dict, _fmt_pace, _fmt_swim_pace helpers)
    - coach/utils/supabase_client.py (get_supabase() call pattern)
    - .planning/phases/02-fitness-test-correctness/02-CONTEXT.md (D-01 through D-05, output format in specifics section)
  </read_first>
  <action>
    Create `scripts/verify_physiology.py` following the exact structural pattern of `scripts/verify_analytics.py`:
    `load_dotenv()` at module level before any `from coach.` import; `logger = logging.getLogger(__name__)`; private `_verify_*` section functions; `def main()` with `logging.basicConfig`; `if __name__ == "__main__": main()`.

    The script queries `physiology_zones` table and outputs three sections with the header `=== Physiology Zones ===`.

    Per D-09 from CONTEXT.md, import `PLAUSIBLE_BOUNDS` from `coach.coaching.fitness_test_processor`.
    The bounds for display are:
    - BIKE FTP: key `ftp_bike_20min` or `ftp_bike_ramp` → range (80, 450) W
    - RUN threshold: key `threshold_run_30min` → range (150, 360) s/km
    - SWIM CSS: key `css_swim_400_200` → range (70, 150) s/100m

    DB columns to query: `discipline`, `valid_from`, `method`, `ftp_w`, `threshold_pace_s_per_km`, `css_pace_s_per_100m`, `notes`.
    Order by `valid_from DESC`, one row per discipline (most recent).

    Section output format (per D-02, D-03 from CONTEXT.md and `<specifics>` in context file):

    ```
    === Physiology Zones ===

    BIKE:
      FTP: 240W  [OK — range 80-450W]  (metodo: 20min_test, data: 2026-06-03)
      CLAUDE.md ftp_attuale_w: 240W  match

    RUN:
      Threshold: 4:30/km (270 s/km)  [OK — range 150-360 s/km]  (metodo: 30min_test, data: 2026-06-05)
      CLAUDE.md threshold_pace_per_km: 4:30  match

    SWIM:
      CSS: 1:45/100m (105 s/100m)  [OK — range 70-150 s/100m]  (metodo: css_400_200, data: 2026-06-01)
      CLAUDE.md css_attuale_per_100m: 1:45  match
    ```

    For [OK/FUORI RANGE]: compare the numeric value against the bounds. Show `[OK — range X-YW]` or `[FUORI RANGE — range X-YW]`.
    For CLAUDE.md comparison (D-03): read `CLAUDE.md` with `Path(__file__).resolve().parent.parent / "CLAUDE.md"` and extract the three fields using `re.search`. If DB value matches CLAUDE.md value → `match`; if not → `DISCREPANZA: DB={db_val} CLAUDE.md={md_val}`.

    If a discipline has no row in `physiology_zones`: print `  (nessun dato)`.
    If the entire table is empty (D-04): print:
    `physiology_zones: vuoto — test eseguiti ma non processati. Usare: PYTHONPATH=. python scripts/trigger_fitness_processor.py`

    CLAUDE.md field extraction patterns (use re.search, not re.sub):
    - `ftp_attuale_w`: line matching `ftp_attuale_w:\s*(.+)`
    - `threshold_pace_per_km`: line matching `threshold_pace_per_km:\s*(.+)`
    - `css_attuale_per_100m`: line matching `css_attuale_per_100m:\s*(.+)`

    Use `_fmt_pace(s_per_km)` and `_fmt_swim_pace(s_per_100m)` from `fitness_test_processor` for display formatting. Do not inline these helpers — import them from `coach.coaching.fitness_test_processor`.

    Wrap each section in try/except (pattern from verify_analytics.py). No DB writes. No `sys.exit`. No `exit(1)`.
  </action>
  <verify>
    <automated>PYTHONPATH=C:/dev/triathlon-coach python -c "import ast; ast.parse(open('scripts/verify_physiology.py').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_physiology.py` exists and is syntactically valid Python
    - File contains `load_dotenv()` before the first `from coach.` import (same line ordering as verify_analytics.py)
    - File contains `=== Physiology Zones ===` section header
    - File contains `PLAUSIBLE_BOUNDS` import from `coach.coaching.fitness_test_processor`
    - File contains `_fmt_pace` and `_fmt_swim_pace` imported from `coach.coaching.fitness_test_processor`
    - File contains `CLAUDE.md` path construction using `Path(__file__).resolve().parent.parent`
    - File contains `ftp_attuale_w`, `threshold_pace_per_km`, `css_attuale_per_100m` for CLAUDE.md field extraction
    - File contains `[OK` and `[FUORI RANGE` strings for bounds display
    - File contains `DISCREPANZA` for mismatch display
    - File contains `physiology_zones: vuoto` for empty-table message
    - File does NOT contain `sys.exit` or `exit(1)`
    - File does NOT contain `SUPABASE_URL` or `SUPABASE_SERVICE_KEY` in any string literal
    - File contains `def main` and `if __name__ == "__main__": main()`
    - `PYTHONPATH=. python -c "import ast; ast.parse(open('scripts/verify_physiology.py').read())"` exits 0
  </acceptance_criteria>
  <done>
    scripts/verify_physiology.py exists, is syntactically valid, follows the verify_analytics.py structural pattern,
    imports PLAUSIBLE_BOUNDS and formatters from fitness_test_processor, reads CLAUDE.md for comparison,
    shows [OK/FUORI RANGE] per value, shows match/DISCREPANZA for CLAUDE.md fields, handles empty table with
    explicit processor trigger instruction, no DB writes, no exit codes.
  </done>
</task>

</tasks>

## Artifacts this phase produces

| Symbol | Type | File | Description |
|--------|------|------|-------------|
| `main()` | function | `scripts/verify_physiology.py` | Entry point, callable via `python scripts/verify_physiology.py` |
| `_verify_physiology_zones(sb)` | function | `scripts/verify_physiology.py` | Queries `physiology_zones` table, prints per-discipline sections |
| `_read_claude_md_fields()` | function | `scripts/verify_physiology.py` | Reads CLAUDE.md and extracts ftp_attuale_w / threshold_pace_per_km / css_attuale_per_100m |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script → Supabase | Read-only DB queries with service key from .env |
| script → CLAUDE.md | Local file read; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Information Disclosure | stdout | mitigate | Do not print SUPABASE_URL or SUPABASE_SERVICE_KEY in any log or output line |
| T-02-01-02 | Tampering | physiology_zones table | accept | Script is read-only by design (D-01); no upsert/insert/delete calls |
| T-02-01-SC | Tampering | pip installs | accept | No new packages; uses existing supabase, python-dotenv, coach.* already in requirements.txt |
</threat_model>

<verification>
Run: `PYTHONPATH=. python scripts/verify_physiology.py`

Expected: prints `=== Physiology Zones ===` followed by BIKE/RUN/SWIM sections with values and bounds, or the empty-table message. No exception stack traces. No DB writes.
</verification>

<success_criteria>
- scripts/verify_physiology.py exists and passes syntax check
- Output shows [OK] or [FUORI RANGE] per discipline per D-02
- Output shows CLAUDE.md comparison (match or DISCREPANZA) per D-03
- Empty table case prints processor trigger instruction per D-04
- No sys.exit, no DB writes per D-01
</success_criteria>

<output>
Create `.planning/phases/02-fitness-test-correctness/02-01-SUMMARY.md` when done
</output>
