---
phase: 04-live-behavior-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - coach/utils/budget.py
  - tests/test_budget.py
autonomous: true
requirements: [VERIFY-06]
must_haves:
  truths:
    - "Il degrado Sonnet→Haiku scatta alla soglia €4.00 come dichiarato in ROADMAP SC4 e CONTEXT D-04"
    - "Il budget tracker legge la spesa Anthropic reale dalla tabella api_usage (somma cost_usd_estimated del mese corrente)"
    - "Il blocco hard a $4.80/$5.00 resta intatto e copre solo i purpose non-emergency"
    - "Nessun test live provoca spesa Anthropic aggiuntiva (D-04: verifica via codice + query api_usage)"
  artifacts:
    - path: "coach/utils/budget.py"
      provides: "Soglia di degrado allineata a €4.00 e select_model che declassa Sonnet→Haiku a quella soglia"
      contains: "BUDGET_DEGRADED"
    - path: "tests/test_budget.py"
      provides: "Test che select_model declassa Sonnet→Haiku a spend=4.00 e che la soglia documentata corrisponde al comportamento"
      contains: "def test_"
  key_links:
    - from: "coach/utils/budget.py select_model"
      to: "soglia di degrado €4.00"
      via: "ramo `spend < BUDGET_DEGRADED` che ritorna Haiku per preferenza Sonnet"
      pattern: "BUDGET_DEGRADED"
    - from: "coach/utils/budget.py get_month_spend_usd"
      to: "api_usage (DB)"
      via: "sum(cost_usd_estimated) where timestamp >= month_start"
      pattern: "cost_usd_estimated"
---

<objective>
Allineare e verificare il budget tracker Anthropic (VERIFY-06): la soglia di degrado Sonnet→Haiku deve scattare a €4.00 come dichiarato nel ROADMAP SC4 ("la soglia di degrado a €4.00 (Sonnet→Haiku) è configurata e verificata") e nel CONTEXT D-04, e la spesa deve riflettere il valore reale dalla tabella `api_usage`.

Slice verticale di correttezza: oggi `budget.py` ha `BUDGET_WARNING = 4.00` (solo alert, nessun declassamento) e `BUDGET_DEGRADED = 4.50` (dove avviene il vero downgrade Sonnet→Haiku in `select_model`). C'è quindi una discrepanza tra comportamento e contratto dichiarato: a €4.00 il sistema dovrebbe già declassare a Haiku, ma con la logica attuale declassa solo a €4.50. Questo plan risolve la discrepanza e blocca con test il comportamento corretto. Nessun test live (D-04).

Purpose: VERIFY-06 + D-04 richiedono che il tracker mostri la spesa reale e che il degrado scatti a €4.00. La verifica è per ispezione codice + query `api_usage`, mai con una chiamata che provochi spesa.
Output: `budget.py` con la soglia di degrado a €4.00 e `select_model` coerente; `test_budget.py` aggiornato con asserzioni sul nuovo comportamento; il verify script di 04-04 leggerà `api_usage` per mostrare la spesa reale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-behavior-verification/04-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

Symbols and files CREATED by this plan (exclude from drift verification — they are new):

- `tests/test_budget.py` — new test functions added for the €4.00 degradation threshold (test functions, not new file)

Modified (NOT created): `coach/utils/budget.py` — adjusted degradation threshold so Sonnet→Haiku occurs at €4.00.

Existing symbols reused (NOT created here): `BUDGET_OK`, `BUDGET_WARNING`, `BUDGET_DEGRADED`, `BUDGET_BLOCKED`, `BUDGET_HARD_CAP`, `select_model`, `check_budget_or_raise`, `get_month_spend_usd`, `estimate_cost`, `MODEL_IDS`, `EMERGENCY_PURPOSES`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allinea la soglia di degrado Sonnet→Haiku a €4.00 e blocca con test</name>
  <files>coach/utils/budget.py, tests/test_budget.py</files>
  <read_first>
    - coach/utils/budget.py (righe 39-44: costanti soglie `BUDGET_OK=3.00`, `BUDGET_WARNING=4.00`, `BUDGET_DEGRADED=4.50`, `BUDGET_BLOCKED=4.80`, `BUDGET_HARD_CAP=5.00`; righe 138-170 `select_model`; righe 173-229 `check_budget_or_raise`; righe 69-81 `get_month_spend_usd`) — fonte di verità del comportamento attuale
    - tests/test_budget.py (righe 45-60 `TestSelectModel`: `test_warning_still_respects` asserisce che a spend=3.50 Sonnet resta Sonnet; `test_degraded_downgrades_sonnet_to_haiku` asserisce a spend=4.10 → Haiku) — questi test codificano il comportamento da aggiornare
    - .planning/ROADMAP.md (Phase 4 SC4: "soglia di degrado a €4.00 (Sonnet→Haiku) è configurata e verificata")
    - .planning/phases/04-live-behavior-verification/04-CONTEXT.md (D-04: verifica via codice + query api_usage, nessun test live; <specifics> mostra atteso `DOWNGRADE_THRESHOLD = 4.0`)
  </read_first>
  <behavior>
    - Test 1: `select_model("sonnet", spend=4.00)` ritorna `MODEL_IDS["haiku"]` (degrado scatta esattamente a €4.00)
    - Test 2: `select_model("sonnet", spend=4.10)` ritorna `MODEL_IDS["haiku"]` (resta valido sopra soglia)
    - Test 3: `select_model("sonnet", spend=3.50)` ritorna `MODEL_IDS["sonnet"]` (sotto €4.00 nessun degrado)
    - Test 4: `select_model("opus", spend=4.00)` ritorna `MODEL_IDS["sonnet"]` (opus→sonnet a soglia degrado)
    - Test 5: `check_budget_or_raise` con spend proiettato > $4.80 e purpose non-emergency solleva `BudgetExceededError`; con purpose in `EMERGENCY_PURPOSES` NON solleva (blocco hard intatto)
  </behavior>
  <action>
    In `coach/utils/budget.py`, fai in modo che il degrado Sonnet→Haiku scatti a €4.00. Imposta `BUDGET_DEGRADED = 4.00` (era 4.50) così che il ramo `elif spend < BUDGET_DEGRADED` di `select_model` (righe 159-164) declassi opus→sonnet e sonnet/haiku→haiku a partire da €4.00; mantieni `BUDGET_WARNING = 4.00` (resta coerente: a €4.00 si entra in degrado, non solo warning). Mantieni invariati `BUDGET_OK=3.00`, `BUDGET_BLOCKED=4.80`, `BUDGET_HARD_CAP=5.00`. Verifica che in `check_budget_or_raise` (righe 209-215) il ramo `projected > BUDGET_DEGRADED` continui a restituire `"BLOCKED_NON_CRITICAL"` correttamente con la nuova soglia, e che l'ordine dei rami `if/elif` resti coerente (con `BUDGET_WARNING == BUDGET_DEGRADED == 4.00`, il ramo `projected > BUDGET_DEGRADED` precede `projected > BUDGET_WARNING`, quindi sopra €4.00 si ottiene `BLOCKED_NON_CRITICAL` e tra €3.00 e €4.00 si ottiene `WARNING`/`DEGRADED`). Aggiorna l'header docstring delle soglie (righe 6-11) per riflettere il nuovo schema. Aggiorna `tests/test_budget.py`: sostituisci/aggiungi i test del blocco behavior — in particolare `test_degraded_downgrades_sonnet_to_haiku` deve usare `spend=4.00` (o aggiungi un test esplicito a 4.00) e adegua eventuali asserzioni che davano per scontata la vecchia soglia 4.50. NON introdurre alcuna chiamata LLM reale (D-04).
  </action>
  <verify>
    <automated>PYTHONPATH=. python -m pytest tests/test_budget.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `coach/utils/budget.py` contiene `BUDGET_DEGRADED = 4.00`
    - `select_model("sonnet", spend=4.00)` ritorna `MODEL_IDS["haiku"]` (verificato da Test 1)
    - `select_model("sonnet", spend=3.50)` ritorna `MODEL_IDS["sonnet"]` (verificato da Test 3)
    - `check_budget_or_raise` solleva `BudgetExceededError` per non-emergency oltre $4.80 e NON per emergency (verificato da Test 5)
    - `tests/test_budget.py` contiene un test che asserisce il degrado a `spend=4.00`
    - `PYTHONPATH=. python -m pytest tests/test_budget.py -q` esce 0
    - `grep -n -v '^#' coach/utils/budget.py | grep -c "cost_usd_estimated"` ritorna >= 1 (get_month_spend_usd legge la spesa reale da api_usage)
  </acceptance_criteria>
  <done>La soglia di degrado Sonnet→Haiku è €4.00 coerente con ROADMAP SC4 e CONTEXT D-04; blocco hard $4.80/$5.00 intatto; test verdi senza chiamate LLM.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| api_usage (DB) → budget gating | La spesa letta dal DB governa il blocco/declassamento delle chiamate LLM a pagamento |
| budget gating → provider Anthropic | La decisione di modello determina la spesa reale verso un servizio esterno a pagamento |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Denial of Service (cost exhaustion) | budget cap €5/mese hard | mitigate | `check_budget_or_raise` blocca non-emergency quando `spend >= BUDGET_HARD_CAP` o `projected > BUDGET_BLOCKED` (budget.py riga 198); soglia di degrado a €4.00 declassa a Haiku prima del blocco — coperto da Test 5 |
| T-04-07 | Tampering | discrepanza soglia documentata vs comportamento reale | mitigate | Allineamento `BUDGET_DEGRADED = 4.00` a ROADMAP SC4; test esplicito blocca futura deriva |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Nessun nuovo package installato in questo plan |
</threat_model>

<verification>
- `PYTHONPATH=. python -m pytest tests/test_budget.py -q` esce 0
- `PYTHONPATH=. python -m pytest -q` (intera suite) nessun nuovo failure
- Spot-check (in 04-04 verify script): la spesa Anthropic del mese corrente da `api_usage` è < €4.00 e le costanti soglia stampate corrispondono (`BUDGET_DEGRADED=4.00`, `BUDGET_BLOCKED=4.80`)
</verification>

<success_criteria>
- VERIFY-06: il budget tracker riflette la spesa Anthropic reale (somma `api_usage.cost_usd_estimated` del mese) e il degrado Sonnet→Haiku scatta a €4.00 — verificato per codice + test, nessuna spesa live (D-04)
- Blocco hard a $4.80/$5.00 preservato per i purpose non-emergency
</success_criteria>

<output>
Create `.planning/phases/04-live-behavior-verification/04-03-SUMMARY.md` when done
</output>
