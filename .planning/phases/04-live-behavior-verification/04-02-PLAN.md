---
phase: 04-live-behavior-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ingest.yml
  - tests/test_live_behavior.py
autonomous: false
requirements: [VERIFY-04, VERIFY-05]
user_setup: []
must_haves:
  truths:
    - "Dopo un sync Garmin, una riga in session_analyses viene generata con testo non vuoto e model_used = 'gemini-2.5-flash'"
    - "Lo step post_session_analysis è wired in ingest.yml e gira dopo il sync Garmin riuscito"
    - "Una modulazione con status='accepted' (dal tap ✅ Telegram) transita ad 'applied' nel ciclo apply-accepted, e planned_sessions viene aggiornato"
    - "Il flusso end-to-end VERIFY-05 + DEPLOY-04 è verificato live dall'atleta con un singolo scenario tap ✅"
  artifacts:
    - path: ".github/workflows/ingest.yml"
      provides: "Step post_session_analysis e step apply-accepted-modulations wired e ordinati correttamente"
      contains: "post_session_analysis"
    - path: "tests/test_live_behavior.py"
      provides: "Test che model_used atteso = gemini-2.5-flash per session_analysis e che il flusso accepted→applied è coperto"
      contains: "def test_"
  key_links:
    - from: ".github/workflows/ingest.yml"
      to: "coach.coaching.post_session_analysis"
      via: "step `python -m coach.coaching.post_session_analysis --recent`"
      pattern: "post_session_analysis"
    - from: ".github/workflows/ingest.yml"
      to: "coach.coaching.modulation.apply_accepted_modulations"
      via: "step `python -m coach.coaching.modulation --apply-accepted`"
      pattern: "modulation --apply-accepted"
    - from: "tests/test_live_behavior.py"
      to: "coach.utils.llm_client routing (session_analysis → gemini-2.5-flash)"
      via: "assert sul modello atteso per purpose session_analysis"
      pattern: "gemini-2.5-flash"
---

<objective>
Verificare e chiudere live i due comportamenti end-to-end della pipeline: (1) VERIFY-04 — le analisi post-sessione vengono generate dopo ogni sync Garmin con `model_used='gemini-2.5-flash'` e testo non vuoto; (2) VERIFY-05 + DEPLOY-04 (bundlati per D-08) — una modulazione accettata via tap ✅ Telegram transita ad `applied` e aggiorna `planned_sessions` nel ciclo `apply-accepted` successivo.

Questa è una slice verticale operativa: il workflow ingest deve invocare l'analisi e l'apply, il routing LLM deve produrre Gemini, e un singolo scenario live (tap ✅) deve dimostrare K5 (bot → status accepted) + apply_accepted_modulations (accepted → applied → planned_sessions). Dopo questo plan, l'atleta ha osservato il ciclo completo proposta→accettazione→applicazione su dati reali.

Purpose: Phase 3 ha lasciato DEPLOY-04 e VERIFY-05 esplicitamente deferiti a Phase 4 (03-VERIFICATION.md: "nessuna modulazione accepted al momento del deploy"). D-03 del CONTEXT richiede di aggiungere lo step post_session_analysis in ingest.yml se manca; D-05/D-06/D-07/D-08 definiscono lo scenario di test modulazione.
Output: ingest.yml con gli step verificati/corretti; test unit che blocca regressioni sul routing Gemini e sul flusso accepted→applied; checkpoint umano che esegue lo scenario live tap ✅.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-behavior-verification/04-CONTEXT.md
@.planning/phases/03-deploy-pipeline-resilience/03-VERIFICATION.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

Symbols and files CREATED by this plan (exclude from drift verification — they are new):

- `tests/test_live_behavior.py` — NEW test file (VERIFY-04 routing assertion + VERIFY-05/DEPLOY-04 accepted→applied flow assertions)

Modified (NOT created): `.github/workflows/ingest.yml` — verified/corrected ordering of existing `Post-session analysis` and `Apply accepted modulations` steps.

Existing symbols reused (NOT created here): `coach.coaching.post_session_analysis.analyze_recent`, `coach.coaching.modulation.apply_accepted_modulations`, `coach.coaching.modulation.apply_modulation`, `coach.coaching.modulation._apply_single_change`, `PURPOSE_ROUTING` / `get_client_for_purpose` in `coach/utils/llm_client.py`, Telegram bot `accept_mod_` callback (workers/telegram-bot/src/index.ts riga 793).
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verifica wiring ingest.yml e blocca con test il routing Gemini + flusso accepted→applied</name>
  <files>.github/workflows/ingest.yml, tests/test_live_behavior.py</files>
  <read_first>
    - .github/workflows/ingest.yml (righe 49-101: step `Garmin sync`, `Compute daily metrics`, `Post-session analysis` riga 73-75, `Apply accepted modulations` riga 94-100) — verifica che gli step esistano e siano ordinati post-sync
    - coach/coaching/post_session_analysis.py (righe 174-198: routing `get_client_for_purpose("session_analysis")` + guard E7 su testo vuoto; riga 198 `analysis_text = result["text"]`; righe 204-211 insert in session_analyses con `model_used`)
    - coach/coaching/modulation.py (righe 100-199: `apply_modulation` accetta 'proposed'|'accepted', transizione a 'applied'; `apply_accepted_modulations` legge status='accepted'; `_apply_single_change` upsert planned_sessions)
    - coach/utils/llm_client.py (PURPOSE_ROUTING / get_client_for_purpose — mappa session_analysis → Gemini gemini-2.5-flash)
    - tests/test_audit_resilience.py (pattern dei fake Supabase `_IdempotencyFakeSupabase`/`_IdempotencyFakeQuery` e dei test `test_k1_accepted_modulation_gets_applied`, `test_d2_full_apply_accepted` — riusa lo stile fake per il flusso accepted→applied)
  </read_first>
  <behavior>
    - Test 1 (VERIFY-04 routing): il client routato per purpose `session_analysis` produce/usa il modello `gemini-2.5-flash`. Assert sul mapping in `coach/utils/llm_client.py` (es. `get_client_for_purpose("session_analysis")` risolve a provider Gemini con model id contenente `gemini-2.5-flash`), senza fare una chiamata LLM reale.
    - Test 2 (VERIFY-04 guard): `analyze_session` NON inserisce in session_analyses se il risultato LLM ha testo vuoto (replica del guard E7 riga 194) — usa un fake client che ritorna `{"text": "", "model": "gemini-2.5-flash"}` e verifica zero insert.
    - Test 3 (VERIFY-05/DEPLOY-04): con un fake Supabase contenente una riga `plan_modulations` status='accepted' con `proposed_changes` valido (date+sport+new), `apply_accepted_modulations()` porta la riga a status='applied' e produce un upsert su planned_sessions. Assert summary `{"applied": 1, ...}`.
  </behavior>
  <action>
    Prima verifica l'ordine degli step in `.github/workflows/ingest.yml`: lo step `Post-session analysis` (riga ~73) DEVE essere presente, con `if: success()` e dopo `Compute daily metrics`; lo step `Apply accepted modulations (audit K1)` (riga ~94) DEVE eseguire `python -m coach.coaching.modulation --apply-accepted` con `continue-on-error: true` e SENZA `if: always()`. Se entrambi sono già corretti (come da 03-VERIFICATION.md), NON modificare il file e annota nel SUMMARY "ingest.yml già wired correttamente (D-03 già soddisfatto in Phase 3)". Se manca o è errato, correggilo mantenendo il pattern degli step esistenti. Poi crea `tests/test_live_behavior.py` con i 3 test del blocco behavior. Per il Test 1 importa il routing da `coach.utils.llm_client` e asserisci che `session_analysis` mappa a `gemini-2.5-flash` (ispeziona `PURPOSE_ROUTING` o la funzione di routing — leggi il file per il nome esatto). Per Test 2/3 riusa lo stile fake Supabase di `tests/test_audit_resilience.py` (`_IdempotencyFakeSupabase`/`_IdempotencyFakeQuery`) con `monkeypatch` su `get_supabase` e sul client LLM; non effettuare chiamate di rete.
  </action>
  <verify>
    <automated>PYTHONPATH=. python -m pytest tests/test_live_behavior.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ingest.yml` contiene `python -m coach.coaching.post_session_analysis --recent`
    - `.github/workflows/ingest.yml` contiene `python -m coach.coaching.modulation --apply-accepted`
    - `grep -n -v '^#' .github/workflows/ingest.yml | grep -c "if: always()"` mostra che lo step apply-accepted NON usa `if: always()` (lo step apply-accepted non ha `if: always()`)
    - `tests/test_live_behavior.py` esiste e contiene almeno 3 funzioni `def test_`
    - Test 1 asserisce la presenza della stringa `gemini-2.5-flash` per il purpose `session_analysis`
    - `PYTHONPATH=. python -m pytest tests/test_live_behavior.py -q` esce 0
  </acceptance_criteria>
  <done>ingest.yml verificato/corretto (post_session_analysis + apply-accepted wired); test bloccano regressioni su routing Gemini e flusso accepted→applied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Scenario live tap ✅ — accepted→applied + planned_sessions (VERIFY-05 + DEPLOY-04)</name>
  <files>(nessuna modifica file — scenario di osservazione live su Telegram + Supabase)</files>
  <read_first>
    - .planning/phases/04-live-behavior-verification/04-CONTEXT.md (D-05/D-06/D-07/D-08 — percorso preferito, fallback INSERT sintetico, trigger immediato, bundling VERIFY-05+DEPLOY-04)
    - coach/coaching/modulation.py (`propose_modulation`, `apply_accepted_modulations`, `_apply_single_change`)
  </read_first>
  <action>
    Questo è un checkpoint umano (l'atleta esegue lo scenario sul sistema live). L'agente NON modifica file: presenta le istruzioni di `how-to-verify`, attende il segnale di ripresa, e registra l'esito nel SUMMARY. Se l'atleta segnala un fallimento (es. status rimasto 'accepted'), l'agente diagnostica via query Supabase e log, ma qualsiasi fix di codice viene tracciato come gap (non eseguito in questo task).
  </action>
  <what-built>
    Lo scenario end-to-end VERIFY-05 + DEPLOY-04 (D-08 li bundla): generazione/INSERT di una `plan_modulation`, tap ✅ su Telegram (status → accepted via bot K5), trigger ingest manuale (apply-accepted), transizione accepted → applied + aggiornamento `planned_sessions`. Tutta l'automazione (test routing, wiring ingest.yml, guard E7) è già stata eseguita in Task 1.
  </what-built>
  <how-to-verify>
    Percorso preferito (D-05, realismo):
    1. Esegui `PYTHONPATH=. python -m coach.coaching.post_session_analysis --recent --days 3` su un'attività Garmin recente. Se HRV basso + carico generano una proposta, una riga `plan_modulations` status='proposed' viene creata e un messaggio Telegram con bottoni ✅/❌ arriva in chat.
    2. Se nessuna proposta naturale emerge entro 1-2 run (D-06 fallback): INSERT sintetico diretto in `plan_modulations` con `status='pending'`, `trigger_event='manual_test'`, `proposed_changes` realistico (es. `[{"date": "<domani>", "sport": "run", "old_description": "...", "new": {"session_type": "recovery", "duration_s": 3600, "description": "Z2 recupero"}}]`), e fai inviare dal bot il messaggio con bottoni (o re-invoca `propose_modulation`).
    3. Su Telegram, premi il bottone ✅ Accetto. Verifica il messaggio di conferma del bot ("Modulazione accettata...") e che la riga `plan_modulations` passi a `status='accepted'` (query Supabase).
    4. Trigger ingest manuale immediato (D-07): esegui `PYTHONPATH=. python -m coach.coaching.modulation --apply-accepted` (oppure attiva un run `workflow_dispatch` di ingest.yml).
    5. Verifica in Supabase: la riga `plan_modulations` ora ha `status='applied'` (o 'partial'/'failed' con log esplicito), e la riga corrispondente in `planned_sessions` per la data/sport del change riflette il nuovo `description`/`duration_s`/`session_type`.
  </how-to-verify>
  <verify>
    <human-check>L'atleta conferma: tap ✅ → plan_modulations status='accepted' → dopo apply-accepted status='applied' → planned_sessions aggiornato per la data/sport del change.</human-check>
  </verify>
  <acceptance_criteria>
    - L'atleta osserva il messaggio Telegram con bottoni ✅/❌ per una modulazione
    - Dopo il tap ✅, la riga `plan_modulations` ha `status='accepted'` (verificato in Supabase)
    - Dopo `--apply-accepted`, la riga `plan_modulations` ha `status='applied'` (o esito 'partial'/'failed' con log)
    - La riga `planned_sessions` per la data/sport del change riflette i nuovi valori (`description`/`duration_s`/`session_type`)
  </acceptance_criteria>
  <resume-signal>Scrivi "approved" se accepted→applied e planned_sessions aggiornato; altrimenti descrivi cosa è andato storto (es. status rimasto 'accepted', planned_sessions non aggiornato, bot ha mostrato errore K5).</resume-signal>
  <done>Scenario tap ✅ → accepted → applied → planned_sessions aggiornato confermato dall'atleta (VERIFY-05 + DEPLOY-04 chiusi live).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram callback (tap ✅) → plan_modulations (DB) | Input dell'utente che cambia lo stato di una modulazione |
| GitHub Actions (ingest) → planned_sessions (DB) | Job ephemeral che scrive sul piano (regola CLAUDE.md §5.4: solo dopo conferma) |
| LLM provider (Gemini) → session_analyses (DB) | Testo generato salvato e inviato all'atleta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | apply_accepted_modulations scrive planned_sessions senza conferma | accept | Conferma già acquisita: il path scrive solo righe con status='accepted' settato dal tap ✅ dell'atleta (CLAUDE.md §5.4 rispettato); `_apply_single_change` fa MERGE non overwrite (audit D3) |
| T-04-04 | Repudiation | modulazione applicata su stato stantio (proposta vecchia) | mitigate | `apply_modulation` rifiuta modulazioni scadute via `expires_at` (audit D1, modulation.py righe 119-134); stati terminali non riprocessati |
| T-04-05 | Information Disclosure | analisi LLM con testo vuoto/troncato inviata all'atleta | mitigate | Guard E7 (post_session_analysis.py riga 194) salta insert/invio su testo vuoto — coperto da Test 2 |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Nessun nuovo package installato in questo plan |
</threat_model>

<verification>
- `PYTHONPATH=. python -m pytest tests/test_live_behavior.py -q` esce 0
- Checkpoint umano: scenario tap ✅ → accepted → applied → planned_sessions aggiornato confermato dall'atleta
- Spot-check (in 04-04 verify script): `session_analyses` ha righe recenti con `model_used='gemini-2.5-flash'`; `plan_modulations` mostra almeno una riga 'applied'
</verification>

<success_criteria>
- VERIFY-04: dopo sync Garmin, session_analyses ha righe con testo non vuoto e `model_used='gemini-2.5-flash'` — routing verificato da test, generazione verificata live
- VERIFY-05: modulazione proposta con bottoni ✅/❌; tap ✅ → planned_sessions aggiornato nel ciclo successivo — verificato live dal checkpoint
- DEPLOY-04: transizione accepted→applied osservata in un run reale (bundlato con VERIFY-05 per D-08) — chiude il gap deferito da Phase 3
- D-03: step post_session_analysis presente in ingest.yml (verificato/aggiunto)
</success_criteria>

<output>
Create `.planning/phases/04-live-behavior-verification/04-02-SUMMARY.md` when done
</output>
