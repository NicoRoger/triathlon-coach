---
phase: 04-live-behavior-verification
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - scripts/verify_live_behavior.py
autonomous: false
requirements: [VERIFY-03, VERIFY-04, VERIFY-05, VERIFY-06, FITNESS-04]
must_haves:
  truths:
    - "Esiste uno script read-only verify_live_behavior.py che controlla brief zone, session_analyses, plan_modulations e budget in un unico output"
    - "Lo script mostra pass/fail per ogni area con staleness check (cutoff 7 giorni) e non rompe la pipeline (nessun exit 1)"
    - "Lo script legge i dati live reali dal DB (bot_messages, session_analyses, plan_modulations, api_usage)"
    - "L'atleta conferma con un'unica esecuzione che le 5 verifiche (VERIFY-03/04/05/06 + FITNESS-04) sono OK sui dati reali"
  artifacts:
    - path: "scripts/verify_live_behavior.py"
      provides: "Verifier read-only a 4 sezioni (BRIEF ZONES, SESSION ANALYSES, PLAN MODULATIONS, BUDGET TRACKER) con riepilogo finale"
      contains: "def main"
  key_links:
    - from: "scripts/verify_live_behavior.py"
      to: "coach.utils.supabase_client.get_supabase"
      via: "from coach.utils.supabase_client import get_supabase"
      pattern: "from coach.utils.supabase_client import get_supabase"
    - from: "scripts/verify_live_behavior.py"
      to: "bot_messages / session_analyses / plan_modulations / api_usage (DB)"
      via: "query read-only per area con staleness check"
      pattern: "session_analyses"
    - from: "scripts/verify_live_behavior.py"
      to: "coach.utils.budget thresholds"
      via: "import BUDGET_DEGRADED/BUDGET_BLOCKED + get_month_spend_usd"
      pattern: "from coach.utils.budget import"
---

<objective>
Creare lo script di evidenza unico read-only `scripts/verify_live_behavior.py` (D-13/D-14/D-15) che consolida la verifica delle 4 aree di Phase 4 — brief zone, session_analyses, plan_modulations, budget tracker — e chiudere la fase con un checkpoint umano che esegue lo script sui dati reali e conferma che VERIFY-03/04/05/06 + FITNESS-04 sono soddisfatti.

Slice verticale di osservabilità: lo script interroga il DB live e produce un report pass/fail per area con staleness check, replicando il pattern di `verify_analytics.py`/`verify_physiology.py`. È read-only e informativo (nessun exit 1, D-14) — pensato per audit umano, non per CI. Dopo questo plan, l'atleta ha un singolo comando che dimostra lo stato live end-to-end del sistema.

Purpose: D-13 richiede un unico script che controlli le 4 aree; D-15 richiede staleness check per area (cutoff 7 giorni); D-14 impone read-only senza exit 1. Questo plan è in Wave 2 perché verifica i fix di 04-01 (zone nel brief), 04-02 (pipeline/modulazione) e 04-03 (budget).
Output: `scripts/verify_live_behavior.py` con 4 sezioni + riepilogo finale; checkpoint umano che esegue lo script e conferma il quadro live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-live-behavior-verification/04-CONTEXT.md
@CLAUDE.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

Symbols and files CREATED by this plan (exclude from drift verification — they are new):

- `scripts/verify_live_behavior.py` — NEW read-only verifier script with section functions:
  - `_verify_brief_zones(sb)` — checks latest `bot_messages` `purpose='morning_brief'` for inline measured-zone content + staleness
  - `_verify_session_analyses(sb)` — checks recent `session_analyses` rows, `model_used='gemini-2.5-flash'`, staleness
  - `_verify_plan_modulations(sb)` — status breakdown + latest modulation staleness
  - `_verify_budget(sb)` — current-month Anthropic spend from `api_usage` vs `BUDGET_DEGRADED`/`BUDGET_BLOCKED`
  - `main()` — orchestrates sections + final `=== Riepilogo: N/4 OK ===` line
  - `STALE_DAYS_CUTOFF = 7` module constant (D-15 / Claude's discretion)

Existing symbols reused (NOT created here): `get_supabase`, `today_rome`, `BUDGET_DEGRADED`, `BUDGET_BLOCKED`, `BUDGET_HARD_CAP`, `get_month_spend_usd`, `derive_zones_for_discipline` (from 04-01), patterns from `scripts/verify_analytics.py` and `scripts/verify_physiology.py`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Crea verify_live_behavior.py read-only con 4 sezioni e staleness check</name>
  <files>scripts/verify_live_behavior.py</files>
  <read_first>
    - scripts/verify_analytics.py (intero: pattern `load_dotenv()` PRIMA degli import coach.*, `logging`, sezioni `_verify_*` con header stampati e try/except per sezione, `main()` con `today_rome()`)
    - scripts/verify_physiology.py (righe 84-90 dedup "ultima riga per disciplina"; righe 55-79 pattern pass/fail con `[OK]`/`[FUORI RANGE]`)
    - coach/utils/budget.py (righe 39-44 costanti soglie aggiornate da 04-03: `BUDGET_DEGRADED=4.00`, `BUDGET_BLOCKED=4.80`, `BUDGET_HARD_CAP=5.00`; righe 69-81 `get_month_spend_usd`)
    - coach/planning/briefing.py (helper zone da 04-01 `_fetch_current_zones`/`_format_session_zones`; riga 689 query `bot_messages` con `purpose='morning_brief'`)
    - sql/schema.sql (righe 267-303: colonne `api_usage` `timestamp`/`cost_usd_estimated`/`provider`/`model`; `session_analyses` `model_used`/`created_at`; `plan_modulations` `status`/`proposed_at`/`resolved_at`)
    - .planning/phases/04-live-behavior-verification/04-CONTEXT.md (D-13/D-14/D-15 + sezione <specifics> con l'output di esempio esatto da replicare)
  </read_first>
  <action>
    Crea `scripts/verify_live_behavior.py` modellato su `scripts/verify_analytics.py`. Struttura: `load_dotenv()` prima di ogni import `coach.*`; `logging`; costante `STALE_DAYS_CUTOFF = 7` (D-15). Quattro funzioni di sezione, ognuna con header stampato e try/except che stampa `ERRORE sezione X:` senza far crashare lo script:
    (1) `_verify_brief_zones(sb)` — legge l'ultima riga `bot_messages` con `purpose='morning_brief'` (ordine `sent_at desc`, limit 1); stampa `[OK]` se il `content` contiene la stringa `Zone misurate:` oppure il placeholder FTP, `[ATTENZIONE]` altrimenti; stampa staleness del `sent_at` (giorni fa, `OK` se <= 7 altrimenti `STALE`).
    (2) `_verify_session_analyses(sb)` — conta righe `session_analyses` con `created_at` negli ultimi `STALE_DAYS_CUTOFF` giorni; stampa il conteggio e l'ultima data; verifica che il `model_used` delle righe recenti sia `gemini-2.5-flash` (`[OK]` se tutte, `[ATTENZIONE]` se qualcuna diversa); staleness sull'ultima `created_at`.
    (3) `_verify_plan_modulations(sb)` — stampa il breakdown per `status` (pending/accepted/applied/rejected/...) e la data dell'ultima riga (`proposed_at` o `resolved_at`); `[OK]` se esiste almeno una riga `applied` o se non ci sono modulazioni pendenti stantie; staleness.
    (4) `_verify_budget(sb)` — importa `from coach.utils.budget import BUDGET_DEGRADED, BUDGET_BLOCKED, get_month_spend_usd`; stampa la spesa Anthropic del mese corrente (`get_month_spend_usd()`), `[OK]` se < `BUDGET_DEGRADED`, `[ATTENZIONE]` se >=; stampa le costanti `BUDGET_DEGRADED`/`BUDGET_BLOCKED` per evidenza. NON effettuare alcuna chiamata LLM (D-04).
    `main()` chiama le 4 sezioni, accumula un contatore pass, e stampa `=== Riepilogo: N/4 OK ===`. NESSUN `sys.exit(1)` (D-14) — lo script ritorna sempre 0. Replica il formato d'output dell'esempio in <specifics> del CONTEXT (header `=== Live Behavior Check — <data> ===`, sezioni `[BRIEF ZONES]`, `[SESSION ANALYSES]`, `[PLAN MODULATIONS]`, `[BUDGET TRACKER]`).
  </action>
  <verify>
    <automated>PYTHONPATH=. python -c "import ast; ast.parse(open('scripts/verify_live_behavior.py',encoding='utf-8').read()); print('syntax-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_live_behavior.py` esiste e contiene `def main`
    - Contiene `def _verify_brief_zones`, `def _verify_session_analyses`, `def _verify_plan_modulations`, `def _verify_budget`
    - Contiene `from coach.utils.supabase_client import get_supabase`
    - Contiene `from coach.utils.budget import` con `BUDGET_DEGRADED` e `get_month_spend_usd`
    - Contiene la stringa `gemini-2.5-flash` (check model_used in session_analyses)
    - Contiene `STALE_DAYS_CUTOFF = 7`
    - NON contiene `sys.exit(1)` — `grep -n -v '^#' scripts/verify_live_behavior.py | grep -c "sys.exit(1)"` ritorna 0 (D-14)
    - `load_dotenv()` appare prima del primo `from coach.` (stesso vincolo di verify_analytics.py)
    - `PYTHONPATH=. python -c "import ast; ast.parse(open('scripts/verify_live_behavior.py',encoding='utf-8').read()); print('syntax-ok')"` stampa `syntax-ok`
  </acceptance_criteria>
  <done>Script read-only a 4 sezioni con staleness check creato; nessun exit 1; legge dati live dal DB.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Esegui verify_live_behavior.py sui dati reali e conferma 4/4 OK</name>
  <files>(nessuna modifica file — esecuzione dello script sui dati reali)</files>
  <read_first>
    - scripts/verify_live_behavior.py (lo script creato nel Task 1)
    - .planning/phases/04-live-behavior-verification/04-CONTEXT.md (sezione <specifics> con l'output di esempio atteso)
  </read_first>
  <action>
    Checkpoint umano. L'agente esegue/guida l'esecuzione di `PYTHONPATH=. python scripts/verify_live_behavior.py` con `.env` configurato e presenta l'output all'atleta per conferma. Non modifica file. Se una sezione non è OK (es. nessun brief recente con zone), l'agente suggerisce l'azione correttiva non-invasiva indicata in `how-to-verify` (forzare un brief) e ri-esegue. Eventuali fix di codice necessari vengono registrati come gap, non eseguiti in questo task.
  </action>
  <what-built>
    Lo script di evidenza unico `scripts/verify_live_behavior.py` che consolida le 4 verifiche di Phase 4 (brief zone, session_analyses, plan_modulations, budget) sui dati reali del DB. Tutti i fix sottostanti (04-01 zone nel brief, 04-02 pipeline/modulazione, 04-03 budget €4.00) sono già stati implementati e testati.
  </what-built>
  <how-to-verify>
    1. Esegui `PYTHONPATH=. python scripts/verify_live_behavior.py` con `.env` configurato (SUPABASE_URL + SUPABASE_SERVICE_KEY).
    2. Verifica le 4 sezioni dell'output:
       - [BRIEF ZONES]: l'ultimo brief in `bot_messages` mostra una riga `Zone misurate:` con watt/pace/HR (VERIFY-03 + FITNESS-04). Se non c'è ancora un brief recente con zone, attendi/forza un brief (`PYTHONPATH=. FORCE_SEND=true python -m coach.planning.briefing`) e ri-esegui.
       - [SESSION ANALYSES]: ci sono righe recenti con `model_used=gemini-2.5-flash` e testo non vuoto (VERIFY-04).
       - [PLAN MODULATIONS]: c'è almeno una riga `applied` dal test di 04-02 (VERIFY-05 + DEPLOY-04).
       - [BUDGET TRACKER]: la spesa Anthropic del mese è mostrata da `api_usage` e le soglie indicano `BUDGET_DEGRADED=4.0` (VERIFY-06).
    3. Conferma che il riepilogo finale mostra `4/4 OK` (o annota quali aree non sono ancora OK e perché).
  </how-to-verify>
  <verify>
    <human-check>L'atleta conferma che `verify_live_behavior.py` mostra 4/4 OK sui dati reali (BRIEF ZONES, SESSION ANALYSES, PLAN MODULATIONS, BUDGET TRACKER).</human-check>
  </verify>
  <acceptance_criteria>
    - Lo script gira senza eccezioni e stampa le 4 sezioni + riga `=== Riepilogo: N/4 OK ===`
    - [BRIEF ZONES] mostra `[OK]` con riga `Zone misurate:` (o placeholder FTP) nell'ultimo brief
    - [SESSION ANALYSES] mostra `[OK]` con `model_used=gemini-2.5-flash`
    - [PLAN MODULATIONS] mostra almeno una riga `applied`
    - [BUDGET TRACKER] mostra spesa reale + `BUDGET_DEGRADED=4.0`
  </acceptance_criteria>
  <resume-signal>Scrivi "approved" se le 4 sezioni sono OK sui dati reali (4/4); altrimenti elenca le aree non-OK con l'output osservato così da pianificare la chiusura gap.</resume-signal>
  <done>Phase 4 chiusa: VERIFY-03/04/05/06 + FITNESS-04 confermati live; riepilogo 4/4 OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB (read-only) → script di audit | Lo script legge tabelle live e stampa un report; nessuna scrittura |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | script di verifica che scrive accidentalmente sul DB | mitigate | Solo query `select` read-only; nessun `insert`/`update`/`upsert`/`delete`; `grep` di acceptance conferma assenza di scritture |
| T-04-09 | Denial of Service | exit 1 in CI che blocca pipeline | accept | D-14: script informativo, nessun `sys.exit(1)`; non wired in CI; uso manuale per audit umano |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | Nessun nuovo package installato in questo plan |
</threat_model>

<verification>
- `PYTHONPATH=. python scripts/verify_live_behavior.py` gira senza eccezioni e stampa 4 sezioni + riepilogo
- Checkpoint umano: 4/4 OK sui dati reali confermato dall'atleta
- `grep -c "insert\|update\|upsert\|delete" scripts/verify_live_behavior.py` ritorna 0 (read-only)
</verification>

<success_criteria>
- D-13: unico script read-only che controlla le 4 aree (brief zone, session_analyses, plan_modulations, budget)
- D-14: nessun exit 1, informativo, per audit umano
- D-15: staleness check per area con cutoff 7 giorni
- Chiusura Phase 4: VERIFY-03/04/05/06 + FITNESS-04 confermati live dal checkpoint
</success_criteria>

<output>
Create `.planning/phases/04-live-behavior-verification/04-04-SUMMARY.md` when done
</output>
