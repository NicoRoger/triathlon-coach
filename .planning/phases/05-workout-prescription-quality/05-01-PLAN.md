---
phase: 05-workout-prescription-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/2026-06-07-workout-prescription-quality.sql
  - tests/test_active_constraints.py
  - scripts/verify_prescription_quality.py
autonomous: true
requirements: [WORKOUT-03, WORKOUT-04]
must_haves:
  truths:
    - "La tabella active_constraints esiste con i 2 vincoli seed (spalla dx swim, fascite sx run)"
    - "La colonna mesocycles.progression_plan JSONB esiste"
    - "tests/test_active_constraints.py passa verde (source assertions sulla migration)"
    - "La migration è idempotente (ri-eseguibile senza errori né duplicati)"
  artifacts:
    - path: "migrations/2026-06-07-workout-prescription-quality.sql"
      provides: "active_constraints table + progression_plan column + seed dati D-13"
      contains: "CREATE TABLE IF NOT EXISTS active_constraints"
    - path: "tests/test_active_constraints.py"
      provides: "Source + live assertions WORKOUT-03"
      contains: "active_constraints"
    - path: "scripts/verify_prescription_quality.py"
      provides: "Read-only phase gate scaffold (WORKOUT-03 automatico + checklist manuale)"
      contains: "_verify_active_constraints"
  key_links:
    - from: "tests/test_active_constraints.py"
      to: "migrations/2026-06-07-workout-prescription-quality.sql"
      via: "read_text() source assertion"
      pattern: "active_constraints"
---

<objective>
Wave 0 foundation per Phase 5: crea la migration DB (nuova tabella `active_constraints` + colonna `mesocycles.progression_plan`), i test stub che verificano la migration (WORKOUT-03), e lo scaffold dello script di verifica read-only del phase gate.

Questi artifact DEVONO esistere prima di ogni altro plan: il Worker MCP (Plan 02) interroga `active_constraints` e `mesocycles.progression_plan`, quindi tabella e colonna devono esistere in DB; i test forniscono il primo automated verify del phase (gli altri requisiti WORKOUT sono manual-only su output LLM).

Purpose: stabilire la fonte di verità DB per i vincoli medici dinamici (D-12/D-13/D-16) e la progressione multi-sessione (D-27), sostituendo i vincoli statici di CLAUDE.md.
Output: 1 file migration SQL, 1 file test, 1 file script di verifica (scaffold).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workout-prescription-quality/05-CONTEXT.md
@.planning/phases/05-workout-prescription-quality/05-RESEARCH.md
@.planning/phases/05-workout-prescription-quality/05-PATTERNS.md
@.planning/phases/05-workout-prescription-quality/05-VALIDATION.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01)

- **File** `migrations/2026-06-07-workout-prescription-quality.sql` — migration additiva idempotente
- **DB table** `active_constraints (id UUID, type TEXT, discipline TEXT, description TEXT, severity TEXT, created_at TIMESTAMPTZ, resolved_at TIMESTAMPTZ)` — vincoli medici dinamici (D-12)
- **DB column** `mesocycles.progression_plan JSONB` — progressione qualità multi-sessione (D-27)
- **DB seed rows** 2 righe in `active_constraints`: (injury, swim, spalla dx) + (injury, run, fascite sx) (D-13)
- **File** `tests/test_active_constraints.py` — test pytest WORKOUT-03
- **Test functions** `test_migration_file_present`, `test_migration_idempotent`, `test_active_constraints_seed_has_two_rows` (live, skip se SUPABASE_URL assente)
- **File** `scripts/verify_prescription_quality.py` — script read-only phase gate (scaffold)
- **Functions** `_verify_active_constraints(sb)`, `_verify_physiology_zones_age(sb)`, `_verify_mesocycles_progression_plan(sb)`, `_print_manual_checklist()`, `main()`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — active_constraints + progression_plan + seed</name>
  <files>migrations/2026-06-07-workout-prescription-quality.sql</files>
  <read_first>
    - migrations/2026-06-01-resilience-audit.sql (pattern migration: header comment, IF NOT EXISTS, DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$, ADD COLUMN IF NOT EXISTS, ENABLE ROW LEVEL SECURITY)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `migrations/2026-06-07-workout-prescription-quality.sql` — pattern esatti per ogni blocco)
    - sql/schema.sql (schema corrente di `mesocycles` per confermare che `progression_plan` non esiste ancora)
  </read_first>
  <action>
    Crea la migration additiva e idempotente `migrations/2026-06-07-workout-prescription-quality.sql`.
    Header comment nel formato di resilience-audit.sql: dichiara "Additive e idempotente. Esegui una volta nel SQL editor di Supabase." e cita le decisioni coperte (D-12/D-13 active_constraints, D-27 progression_plan) e il requisito WORKOUT-03.
    Blocco 1 — `CREATE TABLE IF NOT EXISTS active_constraints` con colonne esatte: `id UUID PRIMARY KEY DEFAULT uuid_generate_v4()`, `type TEXT NOT NULL CHECK (type IN ('injury','medical','tactical'))`, `discipline TEXT NOT NULL CHECK (discipline IN ('swim','bike','run','all'))`, `description TEXT NOT NULL`, `severity TEXT CHECK (severity IN ('high','medium','low'))`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `resolved_at TIMESTAMPTZ` (nullable). Subito dopo: `ALTER TABLE active_constraints ENABLE ROW LEVEL SECURITY;` (pattern single-user service_role esistente — nessuna policy esplicita, accesso solo via service_role come tutte le altre tabelle, vedi RESEARCH §Don't Hand-Roll). Il blocco RLS-enable va protetto da `DO $$ BEGIN ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;` se necessario per idempotenza.
    Blocco 2 — `ALTER TABLE mesocycles ADD COLUMN IF NOT EXISTS progression_plan JSONB;`
    Blocco 3 — seed idempotente con pattern `INSERT ... SELECT ... WHERE NOT EXISTS (...)` (NON `ON CONFLICT DO NOTHING`: la tabella non ha UNIQUE key naturale — vedi RESEARCH Pitfall 5). Inserisci esattamente 2 righe: (a) `'injury','swim','borsite + tendinopatia CLB spalla destra: max Z1-Z2, zero Z4+, distanza 72h tra sessioni nuoto','high'` con guard `WHERE NOT EXISTS (SELECT 1 FROM active_constraints WHERE type='injury' AND discipline='swim' AND resolved_at IS NULL)`; (b) `'injury','run','fascite plantare sinistra: max +10% volume/settimana, cap 14-15km/settimana attuale, asintomatica da 14gg','medium'` con guard analogo su discipline='run'. Valori description testuali da D-13.
    NON aggiungere trigger `trigger_set_updated_at` ad active_constraints (lo schema non ha colonna `updated_at` — vedi RESEARCH §Supporting).
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -c "src=open('migrations/2026-06-07-workout-prescription-quality.sql',encoding='utf-8').read(); assert 'CREATE TABLE IF NOT EXISTS active_constraints' in src; assert 'ADD COLUMN IF NOT EXISTS progression_plan' in src; assert 'WHERE NOT EXISTS' in src; assert 'ENABLE ROW LEVEL SECURITY' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Il file `migrations/2026-06-07-workout-prescription-quality.sql` esiste
    - Source contiene la stringa esatta `CREATE TABLE IF NOT EXISTS active_constraints`
    - Source contiene `ADD COLUMN IF NOT EXISTS progression_plan JSONB`
    - Source contiene `ENABLE ROW LEVEL SECURITY`
    - Seed usa il pattern `WHERE NOT EXISTS` (NON `ON CONFLICT`)
    - Source contiene entrambe le description seed D-13 (substring `borsite + tendinopatia CLB` e `fascite plantare sinistra`)
    - I CHECK constraint includono `type IN ('injury', 'medical', 'tactical')` e `discipline IN ('swim', 'bike', 'run', 'all')`
  </acceptance_criteria>
  <done>La migration esiste, è idempotente (IF NOT EXISTS + WHERE NOT EXISTS), crea active_constraints con RLS, aggiunge progression_plan a mesocycles, e contiene i 2 seed D-13.</done>
</task>

<task type="auto">
  <name>Task 2: Test stub WORKOUT-03 (tests/test_active_constraints.py)</name>
  <files>tests/test_active_constraints.py</files>
  <read_first>
    - tests/test_audit_resilience.py (pattern: module header docstring, import block con `importlib.util`/`Path`, helper `_load()`, source-assertion style `read_text(encoding="utf-8")` + `assert "<keyword>" in src`, live-DB test con `pytest.skip` se `SUPABASE_URL` assente)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `tests/test_active_constraints.py` — funzioni di test verbatim)
    - migrations/2026-06-07-workout-prescription-quality.sql (il file appena creato in Task 1 — i test fanno source-assertion su questo)
  </read_first>
  <action>
    Crea `tests/test_active_constraints.py` seguendo lo stile di `tests/test_audit_resilience.py`.
    Module docstring: dichiara copertura WORKOUT-03 (vincoli medici da DB, non da CLAUDE.md statico).
    `ROOT = Path(__file__).resolve().parent.parent`.
    Implementa almeno 3 test:
    1. `test_migration_file_present()`: legge `ROOT / "migrations" / "2026-06-07-workout-prescription-quality.sql"` con `read_text(encoding="utf-8")` e asserisce le substring `active_constraints`, `progression_plan`, `WHERE NOT EXISTS`.
    2. `test_migration_idempotent()`: asserisce le substring `CREATE TABLE IF NOT EXISTS active_constraints` e `ADD COLUMN IF NOT EXISTS progression_plan`.
    3. `test_active_constraints_seed_has_two_rows()`: live test — `import os; if not os.getenv("SUPABASE_URL"): pytest.skip("SUPABASE_URL non configurato — test live skippato")`. Poi `load_dotenv()`, `from coach.utils.supabase_client import get_supabase`, query `active_constraints` filtrata su `resolved_at IS NULL`, asserisce che le discipline includano sia `swim` (spalla dx) sia `run` (fascite sx).
    Tutti i test devono essere collezionabili da pytest senza dipendenze esterne quando SUPABASE_URL non è settato (i live test skippano gracefully).
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -m pytest tests/test_active_constraints.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_active_constraints.py -v` termina senza errori di collezione/import
    - `test_migration_file_present` e `test_migration_idempotent` passano (source assertions)
    - `test_active_constraints_seed_has_two_rows` passa o è SKIPPED (mai ERROR) quando SUPABASE_URL non è settato
    - Il file usa `ROOT = Path(__file__).resolve().parent.parent` come root del repo
    - Il file usa `pytest.skip(...)` per il test live quando manca `SUPABASE_URL`
  </acceptance_criteria>
  <done>Il file test esiste, è collezionabile da pytest, i due test di source assertion passano e il test live skippa correttamente senza SUPABASE_URL.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold script verify_prescription_quality.py</name>
  <files>scripts/verify_prescription_quality.py</files>
  <read_first>
    - scripts/verify_analytics.py (pattern: header docstring, `load_dotenv()` PRIMA di ogni import `coach.*`, `from coach.utils.supabase_client import get_supabase`, funzioni `_verify_<topic>(sb) -> None` con try/except + print sezioni, `main()` con `logging.basicConfig`)
    - scripts/verify_physiology.py (pattern checklist manuale e bounds reference)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `scripts/verify_prescription_quality.py` — `_verify_active_constraints` verbatim e struttura `main()`)
  </read_first>
  <action>
    Crea `scripts/verify_prescription_quality.py` come script read-only informativo (nessun exit automatico, l'operatore legge l'output).
    Header docstring: "Verifica live della qualità delle prescrizioni Phase 5". `from __future__ import annotations`, `import logging`, `import os`, `from dotenv import load_dotenv`, `load_dotenv()` (DEVE precedere ogni import `coach.*` per il constraint lru_cache), poi `from coach.utils.supabase_client import get_supabase`.
    Implementa le sezioni:
    1. `_verify_active_constraints(sb) -> None`: query `active_constraints` select `id,type,discipline,description,severity,created_at,resolved_at`, stampa totali vs attivi (`resolved_at is None`), elenca i vincoli attivi con `[SEVERITY] DISCIPLINE: description[:80]`, stampa ATTENZIONE se attivi < 2. Wrappa in try/except con `print(f"ERRORE sezione active_constraints: {exc}")`.
    2. `_verify_physiology_zones_age(sb) -> None`: query `physiology_zones`, per ogni disciplina stampa `valid_from` e l'età in giorni (calcolata in Python da `valid_from`), evidenzia se età > 42 giorni. try/except analogo.
    3. `_verify_mesocycles_progression_plan(sb) -> None`: query `mesocycles` attivo, stampa se `progression_plan` è presente/null. try/except analogo.
    4. `_print_manual_checklist() -> None`: stampa la checklist manuale per WORKOUT-01/02/04/05 (4 item `[ ]` come in PATTERNS.md).
    `main()`: `logging.basicConfig(level=logging.INFO, ...)`, `sb = get_supabase()`, chiama le 4 sezioni in ordine. `if __name__ == "__main__": main()`.
    NOTA: questo è lo scaffold; lo script viene eseguito come phase gate nel Plan 04. In Wave 0 deve solo essere importabile/eseguibile senza crash su DB vuoto (try/except gestisce tutto).
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -c "import ast; ast.parse(open('scripts/verify_prescription_quality.py',encoding='utf-8').read()); src=open('scripts/verify_prescription_quality.py',encoding='utf-8').read(); assert '_verify_active_constraints' in src; assert '_print_manual_checklist' in src; assert 'load_dotenv()' in src; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Il file `scripts/verify_prescription_quality.py` esiste ed è sintatticamente valido (ast.parse passa)
    - Contiene le funzioni `_verify_active_constraints`, `_verify_physiology_zones_age`, `_verify_mesocycles_progression_plan`, `_print_manual_checklist`, `main`
    - `load_dotenv()` appare PRIMA di qualsiasi `from coach.` import
    - La checklist manuale elenca WORKOUT-01, WORKOUT-02, WORKOUT-04, WORKOUT-05
    - Ogni `_verify_*` usa try/except con messaggio `ERRORE`
  </acceptance_criteria>
  <done>Lo script scaffold esiste, è sintatticamente valido, contiene le 4 sezioni verify + checklist manuale, e load_dotenv precede gli import coach.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL editor Supabase → DB | Migration eseguita manualmente nell'editor SQL Supabase; input trusted (operatore) |
| Script Python → Supabase REST | `get_supabase()` usa service_key da env; read-only nel verify script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | active_constraints table | mitigate | `ENABLE ROW LEVEL SECURITY` — accesso solo via service_role (pattern single-user esistente, V4 ASVS L1) |
| T-05-02 | Tampering | Seed migration ri-eseguita | mitigate | `WHERE NOT EXISTS` impedisce righe duplicate (idempotenza) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 non installa nuovi pacchetti (RESEARCH §Package Legitimacy Audit = N/A); nessun nuovo supply-chain risk |
</threat_model>

<verification>
- `python -m pytest tests/test_active_constraints.py -v` → verde (source assertions) o skip (live)
- `python -m pytest tests/ -x -q` → suite esistente resta verde (nessuna regressione)
- Migration applicata manualmente in Supabase SQL editor (checkpoint Plan 04): `active_constraints` con 2 righe, `mesocycles.progression_plan` esiste
</verification>

<success_criteria>
- Migration idempotente esiste con active_constraints + progression_plan + 2 seed
- Test WORKOUT-03 verde/skip senza errori di collezione
- Script verify scaffold sintatticamente valido con tutte le sezioni
</success_criteria>

<output>
Create `.planning/phases/05-workout-prescription-quality/05-01-SUMMARY.md` when done
</output>
