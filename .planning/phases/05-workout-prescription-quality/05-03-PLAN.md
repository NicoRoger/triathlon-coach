---
phase: 05-workout-prescription-quality
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - skills/propose_session.md
  - skills/generate_mesocycle.md
  - skills/fitness_test.md
autonomous: true
requirements: [WORKOUT-01, WORKOUT-02, WORKOUT-03, WORKOUT-04, WORKOUT-05]
must_haves:
  truths:
    - "propose_session.md impone get_physiology_zones come Step 0 GATE FISIOLOGICO obbligatorio prima di ogni prescrizione (D-02)"
    - "Ogni sessione proposta ha struttura completa warmup/main set con intervalli/cooldown obbligatoria nel template (D-03, WORKOUT-01)"
    - "I target numerici provengono sempre da physiology_zones (ftp_w/threshold_pace_s_per_km/css_pace_s_per_100m), mai hard-coded (D-02, WORKOUT-02)"
    - "I vincoli medici sono letti da get_weekly_context.active_constraints, NON da CLAUDE.md statico (D-15/D-16, WORKOUT-03)"
    - "propose_session.md legge le ultime 3 sessioni della disciplina via get_activity_history e riduce il volume se RPE medio >= 8.0 (D-17/D-18)"
    - "Le zone diventano perceived effort quando >= 2 fattori avversi (T>25C, TSB<-10, sleep<65) (D-19/D-20)"
    - "Ogni prescrizione include la sezione Contesto mesociclo con TSS accumulato/target e ruolo della sessione (D-21/D-22, WORKOUT-04)"
    - "Drill tecnici specifici per Nicolo (nuoto/bici/corsa) sono parte integrante della sessione (D-23/D-24)"
    - "Le sessioni race-pace sono calibrate da race_prediction + get_race_context per Lavarone (D-25/D-26)"
    - "generate_mesocycle.md legge current_progression_step e avanza solo se RPE medio ultime 3 sessioni <= 7.5 (D-27/D-28, WORKOUT-05)"
    - "fitness_test.md controlla age_days da get_physiology_zones e propone test se null o >42 giorni (D-06)"
  artifacts:
    - path: "skills/propose_session.md"
      provides: "Skill prompt prescrizione sessione con gate fisiologico, vincoli dinamici, drill, contesto mesociclo, contestualizzazione zone"
      contains: "GATE FISIOLOGICO"
    - path: "skills/generate_mesocycle.md"
      provides: "Skill prompt mesociclo con gate fisiologico, active_constraints, progression_plan, structured JSONB per sessione"
      contains: "progression_plan"
    - path: "skills/fitness_test.md"
      provides: "Skill prompt fitness test con check FTP age_days data-driven"
      contains: "age_days"
  key_links:
    - from: "skills/propose_session.md"
      to: "get_weekly_context.active_constraints"
      via: "lettura vincoli medici dinamici (non CLAUDE.md)"
      pattern: "active_constraints"
    - from: "skills/propose_session.md"
      to: "get_physiology_zones"
      via: "Step 0 gate per target numerici"
      pattern: "get_physiology_zones"
    - from: "skills/propose_session.md"
      to: "get_activity_history"
      via: "ultime 3 sessioni disciplina per riduzione volume su RPE"
      pattern: "get_activity_history"
    - from: "skills/generate_mesocycle.md"
      to: "current_progression_step"
      via: "lettura passo corrente progressione qualita"
      pattern: "current_progression_step"
    - from: "skills/fitness_test.md"
      to: "get_physiology_zones age_days"
      via: "check FTP obsolescenza data-driven"
      pattern: "age_days"
---

<objective>
Riscrivere i 3 skill prompts (`skills/propose_session.md`, `skills/generate_mesocycle.md`, `skills/fitness_test.md`) affinche il coach AI (Claude.ai Opus via MCP) generi prescrizioni strutturate, calibrate sulla fisiologia misurata e sui vincoli medici dinamici, contestualizzate al mesociclo e dotate di drill specifici per Nicolo.

Questi prompt sono lo strato che USA l'infrastruttura DB/MCP costruita in Wave 1-2: senza questa riscrittura WORKOUT-01 ha zero copertura (le prescrizioni restano "60min Z2" generiche) e i campi MCP nuovi (age_days, active_constraints, current_progression_step) non vengono mai letti.

Purpose: ogni sessione proposta e un allenamento professionale completo — warmup/main set con intervalli/cooldown — con target numerici da `physiology_zones`, vincoli da `active_constraints` (non da CLAUDE.md statico, D-16), e razionale di mesociclo esplicito.
Output: 3 file skill markdown estesi. Solo testo — nessuna logica eseguibile. La verifica live (output LLM) e demandata al phase gate (Plan 04).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workout-prescription-quality/05-CONTEXT.md
@.planning/phases/05-workout-prescription-quality/05-RESEARCH.md
@.planning/phases/05-workout-prescription-quality/05-PATTERNS.md
@.planning/phases/05-workout-prescription-quality/05-02-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

- **File** `skills/propose_session.md` (esteso) — nuova `Procedura obbligatoria` con Step 0 GATE FISIOLOGICO (D-02), Step vincoli medici dinamici (D-15/D-16), Step storico disciplina (D-17/D-18), Step contestualizzazione zone (D-19/D-20), sezione `Drill tecnici Nicolo` per disciplina (D-23/D-24), sezione race-pace Lavarone (D-25/D-26), template output obbligatorio con `Contesto mesociclo` (D-21/D-22) e struttura warmup/main/cooldown (D-03), istruzione commit `structured` JSONB (D-08/D-09/D-10)
- **File** `skills/generate_mesocycle.md` (esteso) — Step 0 GATE FISIOLOGICO (D-02), estensione Step 8 con `active_constraints` (D-15/D-16) e `current_progression_step` (D-28), regola avanzamento progressione su RPE medio <= 7.5 (D-28), commit `progression_plan` in `commit_mesocycle` (D-27), `structured` JSONB per ogni `commit_plan_change` (D-09), nota espansione sessioni nel template
- **File** `skills/fitness_test.md` (esteso) — nuova sezione `Step 0 — Check FTP age` data-driven da `get_physiology_zones('bike').age_days` (D-05/D-06)
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Riscrittura propose_session.md (gate fisiologico, vincoli dinamici, drill, contesto mesociclo, race-pace)</name>
  <files>skills/propose_session.md</files>
  <read_first>
    - skills/propose_session.md (STATO CORRENTE — leggere INTERAMENTE prima di editare: frontmatter name/description righe 1-4 da preservare verbatim; `## Procedura` righe 13-31 da sostituire; `## Template output` righe 32-44 da sostituire; `## Citation obbligatoria` righe 49-60 da PRESERVARE verbatim; `## Cosa NON fare` righe 62-66 da estendere)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `skills/propose_session.md`: STEP 0 gate wording verbatim righe 51-59, output template righe 87-109, "Cosa NON fare" estensione riga 85, shared pattern "Dynamic constraint reading" righe 600-608)
    - .planning/phases/05-workout-prescription-quality/05-RESEARCH.md (Code Examples — propose_session.md: struttura step obbligatoria righe 578-605 e template output righe 607-634; Pitfall 4 — ordine step obbligatorio con gate; Pitfall 7 — target_value per nuoto)
    - .planning/phases/05-workout-prescription-quality/05-CONTEXT.md (decisioni D-02, D-03, D-15..D-26; §Specifics formati zona contestualizzata, contesto mesociclo, drill nel main set, step JSONB)
  </read_first>
  <action>
    Riscrivi `skills/propose_session.md` PRESERVANDO il frontmatter `name: propose_session` / `description:` (righe 1-4 verbatim) e l'intera sezione `## Citation obbligatoria (Fase 2.4)` (righe 49-60 verbatim, include il pattern `[source: ...]` e `[athlete-belief: ...]`).

    Sostituisci la sezione `## Procedura` (righe 13-31) con una nuova sezione intitolata `## Procedura obbligatoria (NON saltare nessuno step)` strutturata in step numerati, ricalcando il formato numerato di generate_mesocycle.md:

    - `### Step 0 — GATE FISIOLOGICO (obbligatorio, nessuna prescrizione senza questo)` (D-02): testo verbatim da PATTERNS righe 53-58 — chiama `get_physiology_zones(discipline)`; se `zones: []` o `note: "Nessuna zona..."` NON prescrivere e proponi test; se `age_days > 42` segnala obsolescenza e suggerisci test; estrai `ftp_w` / `threshold_pace_s_per_km` / `css_pace_s_per_100m`; TUTTI i target numerici DEVONO venire da questi valori. Dichiara esplicitamente (Pitfall 4): "NON procedere con la prescrizione finche non hai ricevuto il response di get_physiology_zones."
    - `### Step 1 — Sessione pianificata`: chiama `get_planned_session(today)`.
    - `### Step 2 — Contesto settimanale + vincoli medici` (D-15/D-16): chiama `get_weekly_context()`. Estrai `active_constraints` (solo resolved_at IS NULL) — dichiara che QUESTI SOSTITUISCONO i vincoli hardcoded in CLAUDE.md, la fonte di verita e il DB. Estrai `active_mesocycle` + `current_progression_step` (passo corrente progressione). Estrai `daily_metrics` (TSB, HRV z-score, readiness) e `daily_wellness` (sleep score, weather/temperatura).
    - `### Step 3 — Storico disciplina (ultime 3 sessioni, 14gg)` (D-17/D-18): chiama `get_activity_history(sport=<disciplina>, days=14)`, leggi RPE delle ultime 3 sessioni (e le `session_analyses` corrispondenti per pattern fatica). Se RPE medio >= 8.0 o pattern "fatica neuromuscolare" (HR drift > 15bpm nelle ultime 20min di intensita): riduci il volume del main set di 1 step (es. 5x6min -> 4x6min) e aggiungi nota esplicita "Volume ridotto: RPE medio ultimi 14gg = X.X/10."
    - `### Step 4 — Adatta a readiness e condizioni`: conserva la logica readiness a 3 livelli gia presente (>=75 come piano; 50-74 riduci 1 step intensita; <50 recovery via `propose_plan_change`). Aggiungi la contestualizzazione zone (D-19/D-20): se >= 2 fattori avversi tra { temperatura prevista >25C, TSB <-10, sleep score <65 } sono presenti, esprimi il target come PERCEIVED EFFORT (non pace/watt assoluti) con nota esplicita nel formato §Specifics — es. "Condizioni avverse: caldo 28C + TSB -15 -> corri a sensazione Z4, pace di riferimento ~4:30-4:35/km invece di 4:23/km."
    - `### Step 5 — Race-pace Lavarone (solo per sessioni race-specific)` (D-25/D-26): per sessioni di tipo race-pace, chiama `race_prediction` + `get_race_context` (dati Lavarone: distanza, dislivello, tipo fondo cross) PRIMA di prescrivere; calibra i target sulla fitness corrente — non hardcodare. I target si aggiornano automaticamente.

    Aggiungi una nuova sezione `## Drill tecnici Nicolo` (D-23/D-24) — drill come parte INTEGRANTE della sessione (nel warmup o nel main set), mai aggiunta opzionale; ogni sessione include almeno 1-2 drill rilevanti per la disciplina:
    - **Nuoto**: pull drill con pull buoy (shoulder-safe), DPS count, kick con pinne (shoulder relief), fingertip drag (fase catch)
    - **Bici**: big gear intervals (50-55rpm) per muscular endurance, cadenza drill (100rpm+ 3x30sec), climbing position
    - **Corsa**: strides (8x80m) neuromuscolare/meccanica post-fascite, marcia di attivazione tibiali, cadenza drill

    Sostituisci `## Template output` (righe 32-44) con `## Template output (OBBLIGATORIO — mai deviare da questa struttura)` (D-03, D-21/D-22, WORKOUT-01) usando il template verbatim da PATTERNS righe 89-109: header sport/tipo/durata; sezione `Contesto mesociclo` (settimana N/3, TSS accumulato/target, sessioni qualita questa settimana, ruolo di oggi specifico); riga `Vincoli attivi:` da active_constraints; `Warm-up:`, `Drill block:`, `Main set:` (NxXmin @ zona/target numerico preciso, rec Ymin), `Cool-down:`; `Target TSS:`; `Zone di riferimento:` con valori da physiology_zones; riga opzionale condizioni avverse. Dopo il template, aggiungi l'istruzione commit (D-08/D-09/D-10): "Committa con `commit_plan_change` includendo `target_tss` e `structured` come flat steps list `[{name, duration_s, zone, target_value, reps?, notes?}]` con almeno un `warmup`, uno o piu `main_set` (con intervalli), un `cooldown`; `target_value` e il valore numerico preciso da physiology_zones (watt / s-km / s-100m). Per il nuoto, includi `notes` umano-leggibile (es. 'CSS-5: 1:35/100m') oltre al `target_value` numerico (Pitfall 7)."

    Estendi `## Cosa NON fare` (righe 62-66, preserva i 3 bullet esistenti) aggiungendo: "Mai leggere vincoli medici da CLAUDE.md statico — usa `get_weekly_context.active_constraints` (D-16)." e "Mai prescrivere 'X min Z2' come unica riga — ogni sessione ha warmup/main set/cooldown espliciti (D-03)."
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -c "src=open('skills/propose_session.md',encoding='utf-8').read(); req=['name: propose_session','GATE FISIOLOGICO','get_physiology_zones','active_constraints','get_activity_history','Contesto mesociclo','Drill tecnici Nicolo','race_prediction','perceived effort','commit_plan_change','structured','[source:']; missing=[s for s in req if s not in src]; assert not missing, 'MANCANTI: '+repr(missing); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Il frontmatter `name: propose_session` e preservato (riga 1-4 invariata)
    - La sezione `## Citation obbligatoria (Fase 2.4)` e preservata verbatim (substring `[source:` e `[athlete-belief:` presenti)
    - Source contiene `GATE FISIOLOGICO` e l'istruzione di non procedere senza il response di `get_physiology_zones`
    - Source contiene `get_weekly_context` con estrazione di `active_constraints` e la frase che SOSTITUISCONO i vincoli di CLAUDE.md (D-16)
    - Source contiene `get_activity_history` e la regola di riduzione volume su RPE medio >= 8.0 (D-18)
    - Source contiene `perceived effort` e i 3 fattori avversi (temperatura >25, TSB <-10, sleep <65) (D-19)
    - Source contiene la sezione `Contesto mesociclo` con TSS accumulato/target (D-21/D-22)
    - Source contiene la sezione `Drill tecnici Nicolo` con i drill per nuoto (pull buoy/fingertip drag), bici (big gear), corsa (strides) (D-23)
    - Source contiene `race_prediction` e `get_race_context` per le sessioni race-pace (D-25)
    - Il template output contiene `Warm-up`, `Main set`, `Cool-down` come sezioni obbligatorie (D-03, WORKOUT-01)
    - Source contiene l'istruzione di commit `structured` come flat steps list con warmup/main_set/cooldown (D-09/D-10)
    - `## Cosa NON fare` include il divieto di leggere vincoli da CLAUDE.md statico (D-16)
  </acceptance_criteria>
  <done>propose_session.md ha la procedura obbligatoria con gate fisiologico, lettura vincoli dinamici, storico disciplina, contestualizzazione zone, drill specifici, race-pace; template con Contesto mesociclo e struttura warmup/main/cooldown; commit structured JSONB; citation preservata.</done>
</task>

<task type="auto">
  <name>Task 2: Estensione generate_mesocycle.md (gate fisiologico, active_constraints, progression_plan, structured JSONB)</name>
  <files>skills/generate_mesocycle.md</files>
  <read_first>
    - skills/generate_mesocycle.md (STATO CORRENTE — leggere INTERAMENTE: frontmatter righe 1-4 da preservare; `## Procedura` righe 19-35 da ristrutturare (step 7 physiology_zones -> Step 0, step 8 get_weekly_context da estendere); `## Commit in DB` righe 74-79 da estendere; `## Citation obbligatoria` righe 107-126 da PRESERVARE; `## Output prediction` righe 128-146 da PRESERVARE; `## Cosa NON fare` righe 148-151 da estendere)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `skills/generate_mesocycle.md`: step requiring change righe 117-123, commit flow extension righe 125-134, JSONB structured format righe 136-146, output template note riga 148-149; shared pattern GATE righe 590-598 e Dynamic constraint reading righe 600-608)
    - .planning/phases/05-workout-prescription-quality/05-CONTEXT.md (decisioni D-02, D-03, D-09, D-15/D-16, D-27, D-28)
    - .planning/phases/05-workout-prescription-quality/05-RESEARCH.md (sezione generate_mesocycle.md Stato Attuale vs Required righe 155-172; Pitfall 6 — current_progression_step null fallback)
  </read_first>
  <action>
    Modifica `skills/generate_mesocycle.md` PRESERVANDO frontmatter (righe 1-4), `## Citation obbligatoria (Fase 2.4 Cognitive MVP)` (righe 107-126) e `## Output prediction (Fase 2.1)` (righe 128-146) verbatim.

    Ristruttura la `## Procedura` (righe 19-35):
    - Inserisci un nuovo `### Step 0 — GATE FISIOLOGICO (obbligatorio)` PRIMA dello step 1 (D-02): stesso pattern di propose_session.md — chiama `get_physiology_zones` per ogni disciplina coinvolta; se zones vuoto o `age_days > 42` segnala e proponi test; tutti i target numerici dei mesocicli DEVONO derivare da questi valori. Aggiorna lo step 7 esistente (`Leggi physiology_zones per zone correnti`) sostituendolo con "gia fatto in Step 0".
    - Estendi lo step 8 `**Multi-race awareness**: chiama get_weekly_context` (D-15/D-16, D-28): oltre a `upcoming_races`, estrai `active_constraints` (vincoli medici dinamici — SOSTITUISCONO CLAUDE.md, non prescrivere sessioni in contrasto) e `current_progression_step` (passo corrente progressione qualita). Aggiungi la regola D-28: avanza al passo successivo della progressione SOLO se RPE medio delle ultime 3 sessioni di quella tipologia <= 7.5; altrimenti CONSOLIDA il passo corrente. Fallback Pitfall 6: se `current_progression_step` e null (nessun mesociclo attivo), usa progressione conservativa (volume minore della media).

    Estendi `## Commit in DB` (righe 74-79):
    - Step 1 `commit_mesocycle`: aggiungi il campo `progression_plan` JSONB (D-27) nel formato `{"run_threshold": {"week1": "4x6min", "week2": "5x6min", "week3": "6x6min"}, ...}` — un'entry per ogni tipo di sessione di qualita con il volume previsto per settimana.
    - Step 2 `commit_plan_change`: dichiara che OGNI sessione DEVE includere il campo `structured` come flat steps list (D-09) nel formato `[{name, duration_s, zone, target_value, reps?, notes?}]` con almeno un `warmup`, uno o piu `main_set`, un `cooldown` (D-10). Riporta l'esempio JSONB canonical da PATTERNS righe 137-145.

    Nota espansione template (PATTERNS riga 148-149): NON riscrivere la tabella di pianificazione settimanale (righe 38-72). Aggiungi solo, sotto il template output, la nota: "Ogni riga sessione (es. 'Z2 corsa 60min') sara espansa con warmup/main set/cooldown nel `structured` JSONB via `commit_plan_change` — la sintesi nel template e solo per l'overview settimanale."

    Estendi `## Cosa NON fare` (righe 148-151, preserva i bullet esistenti) aggiungendo: "Mai leggere vincoli medici da CLAUDE.md statico — usa `get_weekly_context.active_constraints` (D-16)." e "Mai committare un mesociclo senza `progression_plan` e senza `structured` JSONB per ogni sessione (D-27/D-09)."
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -c "src=open('skills/generate_mesocycle.md',encoding='utf-8').read(); req=['name: generate_mesocycle','GATE FISIOLOGICO','active_constraints','current_progression_step','progression_plan','structured','[source:','record_prediction']; missing=[s for s in req if s not in src]; assert not missing, 'MANCANTI: '+repr(missing); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Il frontmatter `name: generate_mesocycle` e preservato
    - La sezione `## Citation obbligatoria (Fase 2.4 Cognitive MVP)` e preservata (substring `[source:` presente)
    - La sezione `## Output prediction (Fase 2.1)` e preservata (substring `record_prediction` presente)
    - Source contiene `GATE FISIOLOGICO` come Step 0 prima dello step 1 (D-02)
    - Source contiene l'estrazione di `active_constraints` da `get_weekly_context` con la frase che SOSTITUISCONO CLAUDE.md (D-16)
    - Source contiene `current_progression_step` e la regola di avanzamento su RPE medio <= 7.5 (D-28)
    - Source contiene il fallback per `current_progression_step` null (progressione conservativa) (Pitfall 6)
    - Source contiene `progression_plan` nel commit_mesocycle (D-27) con esempio `week1/week2/week3`
    - Source contiene `structured` come flat steps list per commit_plan_change (D-09)
    - `## Cosa NON fare` include il divieto vincoli statici e l'obbligo progression_plan + structured
  </acceptance_criteria>
  <done>generate_mesocycle.md ha Step 0 gate fisiologico, legge active_constraints e current_progression_step da get_weekly_context con regola RPE <= 7.5 e fallback null, committa progression_plan e structured JSONB; citation e output prediction preservate.</done>
</task>

<task type="auto">
  <name>Task 3: Estensione fitness_test.md (check FTP age_days data-driven)</name>
  <files>skills/fitness_test.md</files>
  <read_first>
    - skills/fitness_test.md (STATO CORRENTE — leggere INTERAMENTE: frontmatter righe 1-4 da preservare; `## Quando proporre un test` righe 8-20 da PRESERVARE verbatim; `## Come proporre (OBBLIGATORIO — NON NEGOZIABILE)` righe 22-32 da PRESERVARE verbatim; nuova sezione `Step 0` da inserire PRIMA di `## Quando proporre`)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `skills/fitness_test.md`: FTP age check addition verbatim righe 169-180)
    - .planning/phases/05-workout-prescription-quality/05-RESEARCH.md (Code Examples — fitness_test.md FTP age check righe 636-647; decisioni D-05/D-06)
  </read_first>
  <action>
    Modifica `skills/fitness_test.md` PRESERVANDO frontmatter (righe 1-4) e tutte le sezioni esistenti (`## Quando proporre un test`, `## Come proporre`, `## Messaggio template`, `## Ciclo test consigliato`, `## Aggiornamento automatico CLAUDE.md`, `## Cosa NON fare`).

    Inserisci una NUOVA sezione `## Step 0 — Check FTP age (SEMPRE per sessioni bici)` (D-05/D-06) subito DOPO il titolo `# Skill: Fitness Test` (riga 6) e PRIMA di `## Quando proporre un test` (riga 8), con il testo verbatim da PATTERNS righe 171-178:
    1. Chiama `get_physiology_zones('bike')`
    2. Leggi `zones[0].age_days` nel response
    3. Se `age_days > 42` (6 settimane) o zones e vuoto: "FTP non aggiornato da [age_days] giorni (>6 settimane). Propongo un test FTP bici. Data ottimale: [calcola: post 1-2gg Z2/recovery, non in settimana di carico max, non entro 10gg da gara A]. TSB target al test: >0 per almeno 2 giorni."

    Aggiungi una riga esplicita che la condizione `age_days > 42` DEVE usare il valore numerico dal response del DB (data-driven), NON il testo statico "6 settimane" gia presente nella sezione `## Quando proporre`. Questo rende lo skill data-driven (D-06).
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -c "src=open('skills/fitness_test.md',encoding='utf-8').read(); req=['name: fitness_test','Step 0 — Check FTP age','age_days','get_physiology_zones(\'bike\')','Come proporre (OBBLIGATORIO']; missing=[s for s in req if s not in src]; assert not missing, 'MANCANTI: '+repr(missing); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Il frontmatter `name: fitness_test` e preservato
    - Le sezioni esistenti `## Quando proporre un test` e `## Come proporre (OBBLIGATORIO — NON NEGOZIABILE)` sono preservate verbatim
    - Source contiene la nuova sezione `## Step 0 — Check FTP age` inserita PRIMA di `## Quando proporre`
    - Source contiene `get_physiology_zones('bike')` e `zones[0].age_days`
    - Source contiene la condizione `age_days > 42` come check numerico data-driven
    - Source contiene l'istruzione di calcolo data ottimale (post 1-2gg Z2/recovery, non in carico max, non entro 10gg da gara A)
  </acceptance_criteria>
  <done>fitness_test.md ha la sezione Step 0 che legge age_days da get_physiology_zones('bike') e propone test se >42gg o null, data-driven dal DB; sezioni esistenti preservate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill prompt -> coach AI (Claude.ai Opus) | I file markdown sono caricati come system prompt; input trusted (operatore committa il testo) |
| Coach AI -> MCP Worker | Le istruzioni nei prompt dirigono i tool call MCP (get_physiology_zones, get_weekly_context, ...) — auth invariata (Phase 11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Information Disclosure | Vincoli medici letti da fonte statica obsoleta | mitigate | D-16: i prompt leggono `active_constraints` dal DB (resolved_at IS NULL) — un vincolo risolto sparisce automaticamente; mai da CLAUDE.md statico |
| T-05-07 | Tampering | Prescrizione con zone hard-coded errate | mitigate | D-02 gate fisiologico: nessuna prescrizione senza il response di `get_physiology_zones`; tutti i target numerici dal DB |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 non installa nuovi pacchetti (RESEARCH §Package Legitimacy Audit = N/A); modifica solo testo markdown |
</threat_model>

<verification>
- `python -c "..."` source assertion su ognuno dei 3 file (vedi `<verify>` per task) → tutte le substring chiave presenti
- `python -m pytest tests/ -x -q` → suite Python invariata (Phase 5 Wave 3 non tocca codice Python — solo markdown)
- Verifica live (output LLM: warmup/main/cooldown, zone numeriche, vincoli da DB, drill, contesto mesociclo) → demandata al phase gate manuale (Plan 04)
</verification>

<success_criteria>
- I 3 skill prompts contengono tutte le decisioni D-02/D-03/D-06/D-15..D-28 come da acceptance criteria
- Le sezioni preservate (citation, output prediction, quando/come proporre) restano verbatim
- Nessuna regressione nella suite pytest
</success_criteria>

<output>
Create `.planning/phases/05-workout-prescription-quality/05-03-SUMMARY.md` when done
</output>
