---
phase: 05-workout-prescription-quality
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - scripts/verify_prescription_quality.py
autonomous: false
requirements: [WORKOUT-01, WORKOUT-02, WORKOUT-03, WORKOUT-04, WORKOUT-05]
must_haves:
  truths:
    - "La migration 2026-06-07-workout-prescription-quality.sql e applicata in Supabase (active_constraints con 2 seed + mesocycles.progression_plan)"
    - "scripts/verify_prescription_quality.py e completo: le sezioni verify ritornano bool e main stampa un riepilogo N/N OK (WORKOUT-03 automatico)"
    - "La suite pytest e verde (tests/test_active_constraints.py incluso, live test passa con DB applicato)"
    - "Un checkpoint umano verifica l'output LLM per WORKOUT-01/02/04/05 generando una sessione reale in Claude.ai"
  artifacts:
    - path: "scripts/verify_prescription_quality.py"
      provides: "Phase gate read-only completo: WORKOUT-03 automatico (active_constraints + age_days + progression_plan) con bool e riepilogo"
      contains: "Riepilogo"
  key_links:
    - from: "scripts/verify_prescription_quality.py"
      to: "active_constraints table"
      via: "get_supabase query resolved_at IS NULL"
      pattern: "active_constraints"
    - from: "scripts/verify_prescription_quality.py main"
      to: "_verify_active_constraints / _verify_physiology_zones_age / _verify_mesocycles_progression_plan"
      via: "aggregazione bool + riepilogo passed/total"
      pattern: "passed"
---

<objective>
Phase gate finale di Phase 5: applicare la migration DB in Supabase, completare lo script di verifica `scripts/verify_prescription_quality.py` (da scaffold a strumento di gate con risultati bool + riepilogo), eseguire l'intera suite pytest, e validare manualmente l'output LLM delle prescrizioni (WORKOUT-01/02/04/05 sono verificabili solo osservando l'output del coach AI).

Questo plan chiude la fase: senza l'apply della migration i tool MCP di Plan 02 interrogano tabelle/colonne inesistenti, e senza il checkpoint umano non c'e prova che i skill prompts di Plan 03 producano effettivamente sessioni strutturate con zone numeriche.

Purpose: dimostrare end-to-end che il sistema produce prescrizioni di qualita elite — struttura completa, zone misurate, vincoli dinamici, TSS coerente col mesociclo, distribuzione 80/20.
Output: `verify_prescription_quality.py` completo + migration live + suite verde + checkpoint umano firmato.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workout-prescription-quality/05-CONTEXT.md
@.planning/phases/05-workout-prescription-quality/05-RESEARCH.md
@.planning/phases/05-workout-prescription-quality/05-VALIDATION.md
@.planning/phases/05-workout-prescription-quality/05-01-SUMMARY.md
@.planning/phases/05-workout-prescription-quality/05-02-SUMMARY.md
@.planning/phases/05-workout-prescription-quality/05-03-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 04)

- **File** `scripts/verify_prescription_quality.py` (completato) — `_verify_active_constraints(sb) -> bool`, `_verify_physiology_zones_age(sb) -> bool`, `_verify_mesocycles_progression_plan(sb) -> bool` (firme aggiornate da `-> None`), `main()` con aggregazione `results = [...]`, `passed = sum(...)` e riga `=== Riepilogo: passed/total OK ===` (pattern verify_live_behavior.py)
- **DB state** migration `2026-06-07-workout-prescription-quality.sql` applicata in Supabase: `active_constraints` con 2 righe attive (swim + run), `mesocycles.progression_plan` esistente
- **Verifica live** output dello script: WORKOUT-03 PASS automatico
- **Checkpoint firmato** verifica umana WORKOUT-01/02/04/05 su output LLM reale
</artifacts_produced>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Checkpoint 1: Applica la migration in Supabase (SQL editor)</name>
  <what-built>
    La migration `migrations/2026-06-07-workout-prescription-quality.sql` (Plan 01) e il Worker MCP esteso (Plan 02) sono committati e (il Worker) deployato. La migration crea `active_constraints` (2 seed: spalla dx swim + fascite sx run) e aggiunge `mesocycles.progression_plan`. Per constraint di progetto ("No server Python persistente" + migrazioni manuali nel SQL editor Supabase), la migration deve essere applicata a mano nel SQL editor di Supabase — Claude non ha accesso interattivo all'editor SQL.
  </what-built>
  <how-to-verify>
    Applica la migration in Supabase e verifica che lo schema sia live:
    1. Apri il SQL editor di Supabase (progetto del coach).
    2. Incolla ed esegui il contenuto di `migrations/2026-06-07-workout-prescription-quality.sql`.
    3. Verifica idempotenza: ri-esegui la migration una seconda volta — deve completare senza errori e senza creare righe duplicate (WHERE NOT EXISTS).
    4. Query di conferma: `SELECT type, discipline, severity FROM active_constraints WHERE resolved_at IS NULL;` deve ritornare esattamente 2 righe (injury/swim/high + injury/run/medium).
    5. Query di conferma colonna: `SELECT column_name FROM information_schema.columns WHERE table_name='mesocycles' AND column_name='progression_plan';` deve ritornare 1 riga.
  </how-to-verify>
  <resume-signal>Digita "migration applicata" con il conteggio righe active_constraints, oppure descrivi gli errori SQL riscontrati.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Completa verify_prescription_quality.py (bool + riepilogo phase gate)</name>
  <files>scripts/verify_prescription_quality.py</files>
  <read_first>
    - scripts/verify_prescription_quality.py (SCAFFOLD da Plan 01 — leggere INTERAMENTE: funzioni `_verify_active_constraints`, `_verify_physiology_zones_age`, `_verify_mesocycles_progression_plan` attualmente `-> None`, `_print_manual_checklist`, `main`)
    - scripts/verify_live_behavior.py (PATTERN GATE COMPLETO: sezioni `_verify_*(sb) -> bool` con flag `ok = True`, set `ok = False` su anomalia, `try/except ... ok = False`, `return ok`; `main()` con `results = [...]`, `passed = sum(1 for r in results if r)`, `print(f"=== Riepilogo: {passed}/N OK ===")`)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezione `scripts/verify_prescription_quality.py`: `_verify_active_constraints` verbatim righe 529-548, main pattern righe 552-567, manual checklist righe 572-583)
    - .planning/phases/05-workout-prescription-quality/05-RESEARCH.md (Validation Architecture — WORKOUT-03 automatico, WORKOUT-01/02/04/05 manuali)
  </read_first>
  <action>
    Completa `scripts/verify_prescription_quality.py` trasformando lo scaffold informativo in uno strumento di phase gate con esito aggregato, ricalcando il pattern di `verify_live_behavior.py`.

    Cambia la firma delle 3 sezioni verify da `-> None` a `-> bool`:
    - `_verify_active_constraints(sb) -> bool` (WORKOUT-03): inizializza `ok = True`. Query `active_constraints` (select id,type,discipline,description,severity,created_at,resolved_at). Conta `active = [r for r in rows if r.get("resolved_at") is None]`. Stampa totali/attivi ed elenca gli attivi `[SEVERITY] DISCIPLINE: description[:80]`. Se `len(active) < 2`: stampa ATTENZIONE e `ok = False` (i 2 seed devono esistere). Verifica anche che le discipline attive includano sia `swim` sia `run`; se manca una delle due, `ok = False`. `except Exception: ... ok = False`. `return ok`.
    - `_verify_physiology_zones_age(sb) -> bool`: inizializza `ok = True`. Query `physiology_zones`, per ogni disciplina calcola l'eta in giorni da `valid_from` (in Python) e la stampa. Questa sezione e informativa sull'obsolescenza: se una zona ha eta > 42 giorni, stampa ATTENZIONE ma NON forzare fail (eta alta e un segnale per il coach, non un errore di sistema) — mantieni `ok = True` salvo eccezioni. `except: ok = False`. `return ok`.
    - `_verify_mesocycles_progression_plan(sb) -> bool`: inizializza `ok = True`. Query `mesocycles` (mesociclo attivo: start_date <= today <= end_date, oppure il piu recente). Stampa se `progression_plan` e presente o null. Se nessun mesociclo esiste, stampa nota informativa (stato valido — Pitfall 6) e mantieni `ok = True`. `except: ok = False`. `return ok`.

    Mantieni `_print_manual_checklist() -> None` invariata (resta informativa, elenca WORKOUT-01/02/04/05).

    Riscrivi `main()` sul pattern di verify_live_behavior.py: dopo `sb = get_supabase()` e l'header, costruisci `results = [_verify_active_constraints(sb), _verify_physiology_zones_age(sb), _verify_mesocycles_progression_plan(sb)]`, poi `passed = sum(1 for r in results if r)` e `print(f"=== Riepilogo: {passed}/{len(results)} OK ===")`. Chiama `_print_manual_checklist()` DOPO il riepilogo (la checklist e per la verifica umana del checkpoint successivo). NON aggiungere `sys.exit` (script informativo, l'operatore legge l'output — coerente con verify_live_behavior.py).
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -c "import ast; src=open('scripts/verify_prescription_quality.py',encoding='utf-8').read(); ast.parse(src); req=['def _verify_active_constraints(sb) -> bool','def _verify_physiology_zones_age(sb) -> bool','def _verify_mesocycles_progression_plan(sb) -> bool','passed','Riepilogo','_print_manual_checklist']; missing=[s for s in req if s not in src]; assert not missing, 'MANCANTI: '+repr(missing); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Lo script e sintatticamente valido (ast.parse passa)
    - Le 3 funzioni verify hanno firma `-> bool` e ritornano un bool
    - `_verify_active_constraints` ritorna False se gli attivi sono < 2 o se manca swim/run
    - `_verify_physiology_zones_age` segnala eta > 42 giorni senza forzare fail
    - `_verify_mesocycles_progression_plan` gestisce il caso "nessun mesociclo" senza fail (Pitfall 6)
    - `main()` aggrega `results`, calcola `passed` e stampa `=== Riepilogo: passed/total OK ===`
    - `_print_manual_checklist()` resta presente e viene chiamata
    - `load_dotenv()` resta PRIMA di ogni import `coach.*`
  </acceptance_criteria>
  <done>verify_prescription_quality.py e un phase gate completo: WORKOUT-03 valutato con bool, riepilogo passed/total, checklist manuale per gli SC LLM-dipendenti, nessun crash su DB applicato.</done>
</task>

<task type="auto">
  <name>Task 2: Esegui la suite pytest e lo script di verifica (gate automatico)</name>
  <files>scripts/verify_prescription_quality.py</files>
  <read_first>
    - .planning/phases/05-workout-prescription-quality/05-VALIDATION.md (Sampling Rate: full suite `pytest tests/ -v`, phase gate `python scripts/verify_prescription_quality.py`)
    - tests/test_active_constraints.py (test WORKOUT-03 da Plan 01 — il live test passa ora che la migration e applicata)
  </read_first>
  <action>
    Esegui i gate automatici (nessuna modifica di codice in questo task — solo esecuzione e verifica esito):
    1. Suite completa: `python -m pytest tests/ -v`. Deve essere verde. In particolare `tests/test_active_constraints.py::test_active_constraints_seed_has_two_rows` ora NON skippa (SUPABASE_URL configurato + migration applicata) e PASSA (discipline includono swim + run).
    2. Phase gate script: `python scripts/verify_prescription_quality.py`. Deve stampare `=== Riepilogo: 3/3 OK ===` (WORKOUT-03 automatico verde) con i 2 vincoli attivi elencati.
    Se la suite ha failure o il riepilogo non e 3/3, NON proseguire al checkpoint: documenta l'anomalia e correggi la causa (es. migration non applicata correttamente, env var mancante).
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/ -v` termina con 0 failures
    - `tests/test_active_constraints.py` passa (live test non skippato: swim + run presenti)
    - `python scripts/verify_prescription_quality.py` stampa `=== Riepilogo: 3/3 OK ===`
    - Lo script elenca i 2 vincoli attivi (swim spalla dx, run fascite sx)
  </acceptance_criteria>
  <done>Suite pytest verde, test_active_constraints live passa, verify_prescription_quality stampa 3/3 OK — WORKOUT-03 dimostrato end-to-end.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint 2: Verifica output LLM (WORKOUT-01/02/03/04/05) in Claude.ai</name>
  <what-built>
    I skill prompts (Plan 03) sono aggiornati e il Worker MCP (Plan 02) e live con i nuovi campi (age_days, active_constraints, current_progression_step). WORKOUT-01/02/04/05 dipendono dall'output del coach AI (Claude.ai Opus) e sono verificabili solo osservando una prescrizione reale — non automatizzabili (RESEARCH §Validation Architecture).
  </what-built>
  <how-to-verify>
    In Claude.ai (MCP connector attivo), genera una prescrizione reale e verifica i 4 criteri LLM-dipendenti. Usa la checklist stampata da `verify_prescription_quality.py`:

    1. WORKOUT-01 (struttura completa): chiedi al coach "Dettagliami la sessione di oggi". Verifica che l'output contenga TUTTE e 3 le sezioni — Warm-up (durata + intensita), Main set (intervalli specifici con target, es. "6x4min @ 105% FTP, rec 2min"), Cool-down. Mai solo "60 min Z2".
    2. WORKOUT-02 (zone misurate): conferma che i target numerici (watt bici / pace corsa / s-100m nuoto) corrispondano ai valori di `physiology_zones` (FTP 200W -> i watt sono coerenti; soglia 4:23/km -> pace coerente). Opzionale: chiedi `get_physiology_zones` e confronta. Nessun valore hard-coded o di default.
    3. WORKOUT-04 (TSS coerente col mesociclo): verifica che la sezione "Contesto mesociclo" mostri TSS accumulato vs target settimanale e che il Target TSS della sessione sia coerente col passo del mesociclo (non arbitrario).
    4. WORKOUT-05 (80/20): chiedi un piano settimanale (`generate_mesocycle` o "pianifica la settimana"). Conta le sessioni: >= 80% Z1-Z2, blocchi qualita Z4-Z5 su giorni NON consecutivi, Z3 minimizzato.
    5. WORKOUT-03 (vincoli dinamici, conferma manuale del comportamento prompt): verifica che la prescrizione nuoto resti Z1-Z2 (vincolo spalla dx da active_constraints) e che la corsa rispetti il cap volume — e che il coach citi i vincoli letti da `get_weekly_context.active_constraints`, non da CLAUDE.md.
  </how-to-verify>
  <resume-signal>Digita "approvato" se tutti e 5 i criteri sono soddisfatti, oppure elenca quali falliscono e l'output osservato.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL editor Supabase -> DB | Migration applicata manualmente; input trusted (operatore) |
| Script Python -> Supabase REST | `get_supabase()` con service_key; read-only nel verify script |
| Claude.ai -> MCP Worker | Verifica live tramite tool call autenticati (auth invariata, Phase 11) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Migration ri-applicata | mitigate | Idempotenza verificata nel checkpoint (re-run senza duplicati grazie a WHERE NOT EXISTS / IF NOT EXISTS) |
| T-05-09 | Information Disclosure | verify script legge dati training | accept | Script read-only, eseguito localmente dall'operatore single-user; nessuna scrittura, nessun output esterno |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Phase 5 non installa nuovi pacchetti (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- Checkpoint migration: `SELECT ... FROM active_constraints WHERE resolved_at IS NULL` ritorna 2 righe; `progression_plan` esiste in mesocycles
- `python -m pytest tests/ -v` → verde (test_active_constraints live passa)
- `python scripts/verify_prescription_quality.py` → `=== Riepilogo: 3/3 OK ===`
- Checkpoint umano: output LLM soddisfa WORKOUT-01/02/03/04/05 in Claude.ai
</verification>

<success_criteria>
- Migration applicata e idempotente (2 vincoli attivi + progression_plan)
- verify_prescription_quality.py completo con riepilogo 3/3 OK
- Suite pytest verde
- WORKOUT-01..05 verificati (WORKOUT-03 automatico + tutti e 5 nel checkpoint umano)
</success_criteria>

<output>
Create `.planning/phases/05-workout-prescription-quality/05-04-SUMMARY.md` when done
</output>
