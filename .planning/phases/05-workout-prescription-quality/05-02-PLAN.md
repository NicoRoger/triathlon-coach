---
phase: 05-workout-prescription-quality
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - workers/mcp-server/src/index.ts
autonomous: false
requirements: [WORKOUT-02, WORKOUT-03, WORKOUT-04, WORKOUT-05]
must_haves:
  truths:
    - "get_physiology_zones espone age_days per ogni disciplina (calcolato da valid_from)"
    - "get_weekly_context restituisce active_constraints (resolved_at IS NULL) come array"
    - "get_weekly_context restituisce current_progression_step derivato da mesocycles.progression_plan"
    - "Esiste il tool update_constraint(id, resolved_at) che valida l'UUID prima della PATCH"
    - "commit_mesocycle persiste progression_plan (non più ignorato silenziosamente)"
    - "Il Worker aggiornato è live via wrangler deploy"
  artifacts:
    - path: "workers/mcp-server/src/index.ts"
      provides: "MCP Worker esteso: age_days, active_constraints, current_progression_step, update_constraint, progression_plan"
      contains: "update_constraint"
  key_links:
    - from: "workers/mcp-server/src/index.ts getWeeklyContext"
      to: "active_constraints table"
      via: "sb(env, 'active_constraints?resolved_at=is.null')"
      pattern: "active_constraints\\?resolved_at=is\\.null"
    - from: "workers/mcp-server/src/index.ts updateConstraint"
      to: "active_constraints table"
      via: "PATCH fetch con isUuid guard"
      pattern: "isUuid"
    - from: "workers/mcp-server/src/index.ts getPhysiologyZones"
      to: "valid_from"
      via: "age_days computed field"
      pattern: "age_days"
---

<objective>
Estendere il Worker MCP (`workers/mcp-server/src/index.ts`) con tutto ciò che i skill prompts (Plan 03) leggono dinamicamente dal DB:
1. `get_physiology_zones` → campo calcolato `age_days` per disciplina (D-05) — abilita il check FTP age di fitness_test.md (D-06).
2. `get_weekly_context` → `active_constraints` (D-15) + `current_progression_step` (D-28).
3. Nuovo tool `update_constraint(id, resolved_at)` (D-14) con validazione UUID (ASVS V5).
4. `commit_mesocycle` → persiste `progression_plan` (D-27) — fix del bug "silently ignored".
5. `wrangler deploy` per rendere live le modifiche.

Purpose: la fonte di verità per zone, vincoli medici e progressione deve essere il DB esposto via MCP, mai CLAUDE.md statico (D-16). Senza questi campi i skill prompts non hanno i dati per prescrivere correttamente.
Output: `index.ts` esteso + Worker deployato live.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workout-prescription-quality/05-CONTEXT.md
@.planning/phases/05-workout-prescription-quality/05-RESEARCH.md
@.planning/phases/05-workout-prescription-quality/05-PATTERNS.md
@.planning/phases/05-workout-prescription-quality/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

- **MCP tool** `update_constraint(id: string, resolved_at?: string)` — TOOLS array entry + callTool case + `async function updateConstraint(args, env)`
- **Worker helper** `function deriveProgressionStep(mesocycle, today): { week_number, steps } | null` — pure helper
- **Field** `get_physiology_zones` response: `zones[].age_days: number | null` (calcolato da `valid_from`)
- **Field** `get_weekly_context` response: `active_constraints: any[]` (resolved_at IS NULL)
- **Field** `get_weekly_context` response: `current_progression_step: { week_number, steps } | null`
- **Field** `commit_mesocycle` payload: `progression_plan` (ora persistito) + inputSchema property `progression_plan`
- **Deploy** Worker `mcp-server` live via `wrangler deploy`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: get_physiology_zones age_days + commit_mesocycle progression_plan</name>
  <files>workers/mcp-server/src/index.ts</files>
  <read_first>
    - workers/mcp-server/src/index.ts (funzioni `getPhysiologyZones` ~riga 768-790, `commitMesocycle` ~riga 827-883, helper `todayRomeISO` ~riga 462, entry TOOLS `commit_mesocycle` ~riga 195-211; leggere lo stato attuale, NON assumere)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezioni `getPhysiologyZones age_days extension`, `commit_mesocycle inputSchema extension`, `commitPlanChange payload builder pattern`)
    - .planning/phases/05-workout-prescription-quality/05-RESEARCH.md (Pitfall 1 — valid_from è DATE non TIMESTAMPTZ; Pitfall 2 — commitMesocycle ignora progression_plan)
  </read_first>
  <action>
    Modifica `getPhysiologyZones`: dopo aver costruito l'array `current` e PRIMA del `return`, calcola `age_days` per ogni zona. Usa `const todayDate = new Date(todayRomeISO());` come baseline. Per ogni `zone` in `current`: se `zone.valid_from` esiste, `const validFrom = new Date(zone.valid_from);` poi `zone.age_days = Math.max(0, Math.floor((todayDate.getTime() - validFrom.getTime()) / (1000*60*60*24)));` altrimenti `zone.age_days = null;`. Sia `todayRomeISO()` ("YYYY-MM-DD") sia `new Date("YYYY-MM-DD")` parsano come UTC midnight, quindi la differenza è in giorni esatti (vedi Pitfall 1).
    Modifica `commitMesocycle` payload builder: dopo la riga `if (args.notes !== undefined) payload.notes = args.notes;` aggiungi `if (args.progression_plan !== undefined) payload.progression_plan = args.progression_plan;` (stesso pattern conditional dei campi opzionali, vedi PATTERNS commitPlanChange). Questo persiste il campo sia nel ramo INSERT che nel ramo PATCH (entrambi usano lo stesso `payload`).
    Modifica l'entry `commit_mesocycle` nell'array TOOLS: aggiungi alla `inputSchema.properties` la property `progression_plan: { type: "object", description: "JSONB: {run_threshold: {week1: '4x6min', week2: '5x6min', week3: '6x6min'}, ...}" }`. NON aggiungerla a `required`.
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach/workers/mcp-server; npx tsc --noEmit -p . 2>&1 | Select-String -Pattern "error" -Quiet; if ($?) { exit 1 } else { Write-Output "no tsc errors" }</automated>
  </verify>
  <acceptance_criteria>
    - `getPhysiologyZones` assegna `zone.age_days` (numero >= 0) quando `valid_from` è presente, `null` altrimenti
    - `age_days` è calcolato come differenza intera in giorni tra `todayRomeISO()` e `valid_from`
    - `commitMesocycle` payload include `progression_plan` solo se `args.progression_plan !== undefined`
    - L'entry TOOLS `commit_mesocycle` ha `progression_plan` in `inputSchema.properties` e NON in `required`
    - `npx tsc --noEmit` non produce errori di tipo
  </acceptance_criteria>
  <done>get_physiology_zones espone age_days, commit_mesocycle persiste progression_plan, inputSchema aggiornato, tsc pulito.</done>
</task>

<task type="auto">
  <name>Task 2: get_weekly_context active_constraints + current_progression_step + update_constraint tool</name>
  <files>workers/mcp-server/src/index.ts</files>
  <read_first>
    - workers/mcp-server/src/index.ts (funzione `getWeeklyContext` ~riga 532-580: Promise.all destructuring e return object; helper `isUuid` ~riga 490; TOOLS array entries ~riga 53-211; switch `callTool` ~riga 415-445; pattern PATCH in `commitPlanChange` ~riga 703-762)
    - .planning/phases/05-workout-prescription-quality/05-PATTERNS.md (sezioni `getWeeklyContext Promise.all extension`, `Return object extension`, `deriveProgressionStep helper`, `update_constraint tool`, `callTool switch case`, `isUuid validation`, `Supabase PATCH pattern`)
    - .planning/phases/05-workout-prescription-quality/05-RESEARCH.md (Pattern 2/3/4; Pitfall 6 — deriveProgressionStep null gracefully; Security Domain — UUID validation V5)
  </read_first>
  <action>
    Aggiungi helper puro `deriveProgressionStep(mesocycle: any, today: string): any` accanto agli altri helper puri (area `todayRomeISO`/`daysAgoISO`/`clampInt`, ~riga 462-492): se `!mesocycle || !mesocycle.progression_plan || !mesocycle.start_date` ritorna `null`; altrimenti calcola `weekNumber = Math.floor((new Date(today).getTime() - new Date(mesocycle.start_date).getTime()) / (7*24*60*60*1000)) + 1`, itera `Object.entries(mesocycle.progression_plan)` costruendo `result[sessionType] = weeks['week'+weekNumber] || null`, ritorna `{ week_number: weekNumber, steps: result }`.
    Estendi `getWeeklyContext`: aggiungi come 12° elemento della `Promise.all` la query `sb(env, \`active_constraints?resolved_at=is.null&order=created_at.asc\`)` e aggiorna il destructuring aggiungendo `constraints` come 12ª variabile. Nel return object aggiungi due campi: `active_constraints: constraints || []` e `current_progression_step: deriveProgressionStep(mesocycles?.[0] || null, today)` (la variabile `today` è già in scope).
    Aggiungi il tool `update_constraint` (D-14):
    - TOOLS array entry (inserisci dopo l'entry `commit_mesocycle`): `{ name: "update_constraint", description: "Marca un vincolo medico come risolto (resolved_at = now). Chiamare dopo valutazione clinica.", inputSchema: { type: "object", required: ["id"], properties: { id: { type: "string", description: "UUID del vincolo da risolvere" }, resolved_at: { type: "string", format: "date-time", description: "Timestamp risoluzione (default: now)" } } } }`.
    - callTool switch case (prima di `default:`): `case "update_constraint": return updateConstraint(args || {}, env);`.
    - `async function updateConstraint(args: any, env: Env): Promise<any>`: PRIMA validare l'UUID — `if (!args.id || !isUuid(args.id)) throw new Error("Invalid constraint id: must be a valid UUID");` (usa la funzione esistente `isUuid`, ASVS V5). Poi `const resolvedAt = args.resolved_at || new Date().toISOString();` ed esegui PATCH a `active_constraints?id=eq.${args.id}` con headers apikey/Authorization/Content-Type/Prefer=return=representation e body `JSON.stringify({ resolved_at: resolvedAt })` (stesso pattern PATCH di commitPlanChange). Se `!resp.ok` throw `Update failed: ${resp.status}`. Ritorna `{ status: "resolved", id: args.id, resolved_at: resolvedAt }`.
  </action>
  <verify>
    <automated>cd C:/dev/triathlon-coach/workers/mcp-server; npx tsc --noEmit -p . 2>&1 | Select-String -Pattern "error" -Quiet; if ($?) { exit 1 } else { Write-Output "no tsc errors" }</automated>
  </verify>
  <acceptance_criteria>
    - `getWeeklyContext` Promise.all ha una 12ª query `active_constraints?resolved_at=is.null&order=created_at.asc` e il destructuring ha `constraints`
    - Il return object di `getWeeklyContext` contiene `active_constraints` e `current_progression_step`
    - `deriveProgressionStep` ritorna `null` (non crash) quando mesocycle o progression_plan o start_date sono assenti
    - L'array TOOLS contiene un'entry `update_constraint` con `required: ["id"]`
    - `updateConstraint` chiama `isUuid(args.id)` e throw se invalido PRIMA della fetch PATCH
    - Lo switch `callTool` ha `case "update_constraint"`
    - `npx tsc --noEmit` non produce errori di tipo
  </acceptance_criteria>
  <done>get_weekly_context espone active_constraints + current_progression_step, esiste update_constraint con guard UUID, deriveProgressionStep gestisce null, tsc pulito.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Il Worker MCP `index.ts` è esteso con: age_days in get_physiology_zones, active_constraints + current_progression_step in get_weekly_context, tool update_constraint, progression_plan persistito in commit_mesocycle. Il codice committato NON è live finché non viene deployato (constraint: "Deploy Workers richiede wrangler").
  </what-built>
  <how-to-verify>
    Esegui il deploy del Worker MCP e verifica che sia live:
    1. `cd workers/mcp-server`
    2. `wrangler deploy` (richiede credenziali Cloudflare; se non autenticato, esegui `wrangler login` prima)
    3. Annota l'output version ID del deploy (es. `Current Deployment ID: <id>`).
    4. Verifica live: invoca da Claude.ai (MCP connector) il tool `get_weekly_context` e conferma che il response includa la chiave `active_constraints` (array con i 2 vincoli seed: swim + run) e la chiave `current_progression_step`.
    5. Invoca `get_physiology_zones('run')` e conferma che ogni zona abbia il campo `age_days`.
    Nota: il deploy richiede `wrangler` CLI e credenziali Cloudflare — è l'unico step che Claude non può eseguire autonomamente senza auth interattiva. Se l'errore è di autenticazione, Claude crea un auth gate e ritenta dopo `wrangler login`.
  </how-to-verify>
  <resume-signal>Digita "deployed" con il version ID, oppure descrivi gli errori riscontrati.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude.ai → MCP Worker | Richieste MCP autenticate via MCP_BEARER_TOKEN (auth invariata in Phase 5; hardening → Phase 11) |
| MCP Worker → Supabase REST | service_key in env; query parametrizzate via URL (no raw SQL) |
| `update_constraint(id)` input | `id` fornito dal coach AI — untrusted finché non validato |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | update_constraint(id) | mitigate | `isUuid(args.id)` guard PRIMA della query PATCH (ASVS V5 Input Validation) — usa helper esistente |
| T-05-04 | Tampering | resolved_at via PATCH | mitigate | Supabase REST API con parametri URL-encoded e body JSON — nessun raw SQL injection possibile |
| T-05-05 | Elevation of Privilege | Risoluzione vincolo medico non autorizzata | accept | Tool dietro la stessa auth del Worker; sistema single-user (Nicolò) — basso rischio |
| T-05-SC | Tampering | npm/pip installs | accept | Nessun nuovo pacchetto (RESEARCH §Package Legitimacy Audit = N/A) |
</threat_model>

<verification>
- `npx tsc --noEmit` nel Worker → nessun errore di tipo
- `wrangler deploy` → Worker live (checkpoint Task 3)
- Live MCP: `get_weekly_context` include `active_constraints` (2 righe) + `current_progression_step`; `get_physiology_zones` include `age_days`
- `python -m pytest tests/ -x -q` → suite Python invariata (nessuna regressione — Phase 5 non tocca Python)
</verification>

<success_criteria>
- index.ts esteso, tsc pulito
- Worker deployato live con i nuovi campi/tool verificabili da Claude.ai
- update_constraint valida UUID prima della query
</success_criteria>

<output>
Create `.planning/phases/05-workout-prescription-quality/05-02-SUMMARY.md` when done
</output>
