---
phase: 06-physiological-adaptation-intelligence
plan: 02
type: tdd
wave: 2
depends_on: ["06-01"]
files_modified:
  - coach/analytics/readiness.py
  - coach/coaching/post_session_analysis.py
autonomous: true
requirements: [ADAPT-01]
must_haves:
  truths:
    - "classify_fatigue_type() distingue muscolare vs cardiovascolare da HR drift + pace drop + RPE (D-03)"
    - "La funzione ritorna None con confidence bassa quando i dati sono insufficienti (sessione <30min o no splits con RPE basso)"
    - "post_session_analysis.py chiama classify_fatigue_type prima del call Gemini e scrive fatigue_type, fatigue_confidence, sport in session_analyses"
  artifacts:
    - path: "coach/analytics/readiness.py"
      provides: "FatigueResult dataclass + classify_fatigue_type() + _compute_hr_drift() + _compute_pace_drop()"
      contains: "def classify_fatigue_type("
    - path: "coach/coaching/post_session_analysis.py"
      provides: "Hook classificazione + record esteso con fatigue_type/fatigue_confidence/sport"
      contains: "classify_fatigue_type"
  key_links:
    - from: "coach/coaching/post_session_analysis.py"
      to: "coach.analytics.readiness.classify_fatigue_type"
      via: "import + chiamata prima del call Gemini"
      pattern: "classify_fatigue_type\\("
    - from: "coach/coaching/post_session_analysis.py"
      to: "session_analyses"
      via: "record dict con fatigue_type/fatigue_confidence/sport → insert"
      pattern: "fatigue_type"
---

<objective>
Vertical slice ADAPT-01: dal dato Garmin reale alla classificazione del cedimento salvata in DB.

Implementa `classify_fatigue_type()` deterministica (zero LLM, D-01/D-02/D-03) in `coach/analytics/readiness.py` e la collega a `post_session_analysis.py` in modo che, dopo ogni sync Garmin, una riga di `session_analyses` riporti `fatigue_type`, `fatigue_confidence` e `sport`. Questo rende i test RED del plan 01 (test_cardiovascular_signal, test_muscular_signal, test_fallback_rpe_only_no_splits, test_insufficient_data_short_session, test_missing_splits_low_rpe) GREEN.

Purpose: La distinzione muscolare/cardiovascolare è la base che alimenta sia l'esposizione MCP (plan 03) sia il job belief (plan 04). Deve essere deterministica e disponibile anche a budget LLM esaurito.
Output: funzione di classificazione testata + hook nel flusso post-sessione che persiste il risultato.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-physiological-adaptation-intelligence/06-CONTEXT.md
@.planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md
@.planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Vedi 06-01-PLAN.md §artifacts_this_phase_produces per l'elenco completo. Simboli rilevanti per questo plan (CREATI qui, esclusi dal drift-check):
- `FatigueResult` (dataclass in coach/analytics/readiness.py)
- `classify_fatigue_type(activity, splits, debrief_rpe) -> FatigueResult`
- `_compute_hr_drift(activity, splits) -> Optional[float]`
- `_compute_pace_drop(sport, splits) -> Optional[float]`
- colonne session_analyses `fatigue_type`, `fatigue_confidence`, `sport` (già create dalla migration plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classify_fatigue_type() + helper in readiness.py (GREEN sui test unit)</name>
  <files>coach/analytics/readiness.py</files>
  <read_first>
    - coach/analytics/readiness.py (file da estendere: imports linee 11-16, dataclass ReadinessReport linee 57-63, pattern divisore # ===, helper hrv_z_score linee 69-80 con guard return None)
    - tests/test_fatigue_classification.py (i test RED scritti nel plan 01 — sono il contratto: assert esatti su failure_type/confidence/signal_used)
    - .planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md §Architecture Patterns (Pattern 1-4: FatigueResult, decision tree, _compute_hr_drift, _compute_pace_drop con soglie esatte)
    - .planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md (readiness.py self-extension: dove inserire FatigueResult e i divisori sezione)
  </read_first>
  <behavior>
    - classify_fatigue_type(activity={'sport':'run','duration_s':1200}, splits=[], debrief_rpe=6) → FatigueResult(failure_type=None, ...) [sessione <30min = insufficiente]
    - splits=None, debrief_rpe=9 → failure_type='muscular', confidence=0.4, signal_used='rpe_only'
    - splits=None, debrief_rpe=5 → failure_type=None, signal_used='rpe_only'
    - run, HR 145→160 (drift +15bpm), pace stabile, RPE 7 → failure_type='cardiovascular', confidence>=0.6
    - run, HR stabile 150→152, pace 263→285 (drop ~8%), RPE 8 → failure_type='muscular', confidence>=0.6
    - cardiovascular + muscular signal entrambi veri → failure_type='mixed'
  </behavior>
  <action>
    Estendi `coach/analytics/readiness.py` (NON creare nuovo modulo — D-01 dice readiness.py). Aggiungi dopo `ReadinessReport` (linea 63) la dataclass `FatigueResult` con campi: `failure_type: Optional[str]`, `confidence: float`, `signal_used: str`, `notes: Optional[str] = None`.

    Aggiungi una sezione con divisore `# ===...===` intitolata "Fatigue type classification (ADAPT-01)". Implementa tre funzioni deterministiche (zero import di llm_client — vincolo analytics layer):

    `_compute_hr_drift(activity, splits) -> Optional[float]`: estrae HR per split con `s.get("avg_hr") or s.get("hr")`, scarta i None; se < 4 valori ritorna None; divide a metà (`mid = len//2`), ritorna `fmean(seconda_metà) - fmean(prima_metà)`.

    `_compute_pace_drop(sport, splits) -> Optional[float]`: discipline-specific. run → key `avg_pace_s_per_km`, ritorna `(second-first)/first` (positivo = pace peggiora). bike → key `avg_power_w`, ritorna `(first-second)/first` (positivo = potenza scende). swim → key `avg_pace_s_per_100m`, come run. Usa `s.get(key)` con guard, scarta None, se first half vuoto/zero ritorna None. Claude's Discretion bici: usa soglia muscolare standard 5% (>0.05) coerente con run/swim per non introdurre falsi negativi; documenta la scelta in commento.

    `classify_fatigue_type(activity, splits, debrief_rpe) -> FatigueResult`: applica D-03 nell'ordine del decision tree (06-RESEARCH.md Pattern 2):
    1. guard `duration_s < 1800` → FatigueResult(None, 0.0, 'insufficient', notes='Sessione < 30min')
    2. se non splits → fallback RPE-only: RPE>=8 → ('muscular', 0.4, 'rpe_only', 'Dati insufficienti: solo RPE'); altrimenti (None, 0.3, 'rpe_only', notes)
    3. calcola hr_drift e pace_drop. cardiovascular_signal = hr_drift is not None and hr_drift > 10.0. muscular_signal = (hr_drift None o <=10) and RPE>=8 and pace_drop>0.05.
    4. entrambi → ('mixed', 0.65, 'hr_drift+pace'); solo cardio → conf=min(0.9, 0.6+(hr_drift-10)*0.02) round 2; solo muscolare → conf=0.7 se RPE>=9 else 0.6; nessuno → (None, 0.3, 'hr_drift+pace', notes 'Segnali sotto soglia').

    La funzione NON deve mai sollevare eccezione: ogni accesso a chiavi splits usa `.get()` con default (Pitfall 1). Usa `statistics.fmean` (già importato).
  </action>
  <verify>
    <automated>python -m pytest tests/test_fatigue_classification.py::test_cardiovascular_signal tests/test_fatigue_classification.py::test_muscular_signal tests/test_fatigue_classification.py::test_fallback_rpe_only_no_splits tests/test_fatigue_classification.py::test_insufficient_data_short_session tests/test_fatigue_classification.py::test_missing_splits_low_rpe -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `coach/analytics/readiness.py` contiene `class FatigueResult` (dataclass) con i 4 campi e `def classify_fatigue_type(`, `def _compute_hr_drift(`, `def _compute_pace_drop(`
    - `readiness.py` NON contiene alcun `import` di `llm_client` o `anthropic`/`google` (vincolo analytics zero-LLM)
    - `classify_fatigue_type({'sport':'run','duration_s':3600}, splits_hrdrift, 7).failure_type == 'cardiovascular'` con confidence >= 0.6
    - `classify_fatigue_type({'sport':'run','duration_s':3600}, splits_pacedrop, 8).failure_type == 'muscular'` con confidence >= 0.6
    - `classify_fatigue_type({'sport':'run','duration_s':3600}, None, 9)` ritorna failure_type=='muscular', confidence==0.4, signal_used=='rpe_only'
    - `classify_fatigue_type({'sport':'run','duration_s':1200}, [], 6).failure_type is None`
    - I 5 test unit del comando verify escono 0
  </acceptance_criteria>
  <done>I 5 test unit su classify_fatigue_type sono GREEN; la funzione è deterministica, gestisce splits None/vuoti/campi mancanti senza eccezioni.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Hook in post_session_analysis.py — persisti fatigue_type/confidence/sport</name>
  <files>coach/coaching/post_session_analysis.py</files>
  <read_first>
    - coach/coaching/post_session_analysis.py (flusso analyze_session linee 115-211: lookup activity linea 132, sport linea 143, zone_compliance linea 150, debrief linea 148, record dict linee 204-210, insert linea 211)
    - coach/analytics/readiness.py (firma classify_fatigue_type da Task 1)
    - .planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md (post_session_analysis self-extension: injection point dopo zone_compliance, record dict esteso con sport)
  </read_first>
  <behavior>
    - Dopo aver calcolato zone_compliance (linea 150) e PRIMA di costruire context_parts/call Gemini, analyze_session chiama classify_fatigue_type(activity, splits_or_None, debrief_rpe).
    - Il record salvato in session_analyses include fatigue_type (failure_type o 'insufficient_data'), fatigue_confidence (float), sport.
    - Se splits assenti o RPE assente, la classificazione non crasha: scrive fatigue_type='insufficient_data' e confidence bassa.
  </behavior>
  <action>
    Estendi `analyze_session()` in `coach/coaching/post_session_analysis.py`. Dopo la riga `zone_compliance = _compute_zone_compliance(...)` (linea 150) inserisci:
    - import lazy `from coach.analytics.readiness import classify_fatigue_type` (pattern lazy import già usato per modulation linee 218-222)
    - `splits = activity.get("splits") or None`
    - `debrief_rpe = next((int(d["rpe"]) for d in debrief if d.get("rpe") is not None), None)`
    - `fatigue_result = classify_fatigue_type(activity, splits, debrief_rpe)`
    Questo blocco gira PRIMA del try/except del call Gemini (la classificazione non solleva mai — Pattern 5 RESEARCH.md).

    Estendi il `record` dict (linee 204-210) con tre chiavi, mantenendo le esistenti (activity_id, analysis_text, suggested_actions, model_used, cost_usd):
    - `"fatigue_type": fatigue_result.failure_type or "insufficient_data"`
    - `"fatigue_confidence": fatigue_result.confidence`
    - `"sport": sport` (variabile `sport` già disponibile da linea 143; risolve Open Question 2 — popola la colonna sport per getLastFatigueBySport del plan 03)

    NON cambiare il routing LLM (resta session_analysis → Gemini). NON toccare planned_sessions. NON spostare la classificazione dentro il try/except del call Gemini.
  </action>
  <verify>
    <automated>python -c "import ast; src=open('coach/coaching/post_session_analysis.py',encoding='utf-8').read(); assert 'classify_fatigue_type' in src; assert '\"fatigue_type\"' in src and '\"fatigue_confidence\"' in src and '\"sport\": sport' in src; ast.parse(src); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `coach/coaching/post_session_analysis.py` contiene `from coach.analytics.readiness import classify_fatigue_type`
    - Contiene la chiamata `classify_fatigue_type(activity, splits, debrief_rpe)` con `splits = activity.get("splits") or None` e l'estrazione `debrief_rpe` da `debrief`
    - Il record dict contiene le chiavi `"fatigue_type"`, `"fatigue_confidence"`, `"sport"` e mantiene `"activity_id"`, `"analysis_text"`, `"model_used"`, `"cost_usd"`
    - La chiamata a classify_fatigue_type è posizionata fuori (prima) dal blocco `try: ... except BudgetExceededError` del call Gemini
    - `ast.parse` del file riesce (nessun errore di sintassi)
    - `python -m pytest tests/test_fatigue_classification.py -q` resta GREEN (nessuna regressione)
  </acceptance_criteria>
  <done>analyze_session classifica il cedimento prima del call LLM e salva fatigue_type/fatigue_confidence/sport in ogni riga session_analyses generata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| activities.splits (Garmin raw JSONB) → classify_fatigue_type | Input non normalizzato; struttura variabile per disciplina/dispositivo |
| classify_fatigue_type output → session_analyses insert | Valori scritti su DB devono rispettare il CHECK constraint della migration plan 01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering / Denial of Service | classify_fatigue_type su splits malformati | mitigate | Ogni accesso a chiavi splits usa `.get()` con default; guard su <4 valori e first-half vuoto ritorna None; la funzione non solleva mai (Pitfall 1) |
| T-06-05 | Tampering | valore fatigue_type non valido in insert | mitigate | failure_type proviene da set chiuso ('muscular'/'cardiovascular'/'mixed'/None→'insufficient_data'); CHECK constraint DB (migration plan 01) rifiuta valori fuori dominio (V5 ASVS L1) |
| T-06-06 | Information disclosure | nessun nuovo dato esposto esternamente | accept | La classificazione resta in DB single-athlete con RLS service_role esistente; nessuna nuova superficie di rete |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nessun pacchetto nuovo (solo stdlib statistics) — nessun rischio supply-chain |
</threat_model>

<verification>
- `python -m pytest tests/test_fatigue_classification.py -v` → 5 test unit classify_fatigue_type GREEN
- `python -m pytest tests/ -v` → suite intera verde (nessuna regressione su readiness/post_session)
- grep: readiness.py non importa llm_client
</verification>

<success_criteria>
- classify_fatigue_type implementata, deterministica, testata (5 test GREEN)
- post_session_analysis.py persiste fatigue_type/fatigue_confidence/sport in session_analyses
- Nessuna regressione nella suite pytest completa
</success_criteria>

<output>
Create `.planning/phases/06-physiological-adaptation-intelligence/06-02-SUMMARY.md` when done
</output>
