---
phase: 06-physiological-adaptation-intelligence
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - workers/mcp-server/src/index.ts
  - skills/propose_session.md
autonomous: false
requirements: [ADAPT-02]
user_setup:
  - service: cloudflare-wrangler
    why: "Il Worker MCP modificato non è live finché non viene deployato con wrangler deploy"
    dashboard_config:
      - task: "wrangler deploy del Worker mcp-server"
        location: "workers/mcp-server/ — wrangler deploy"
must_haves:
  truths:
    - "get_weekly_context espone active_beliefs (confidence>=0.55) e last_fatigue_by_sport (per run/swim/bike)"
    - "propose_session.md legge active_beliefs dentro Step 2 esistente (senza seconda chiamata a get_weekly_context) e cita i beliefs inline con [athlete-belief: key]"
    - "Il belief endurance_failure_type seedato compare in active_beliefs e può essere citato in una prescrizione"
  artifacts:
    - path: "workers/mcp-server/src/index.ts"
      provides: "active_beliefs + last_fatigue_by_sport nel return di getWeeklyContext + helper getLastFatigueBySport"
      contains: "last_fatigue_by_sport"
    - path: "skills/propose_session.md"
      provides: "Step 2 esteso con lettura active_beliefs + last_fatigue_by_sport e tag inline"
      contains: "active_beliefs"
  key_links:
    - from: "workers/mcp-server/src/index.ts getWeeklyContext"
      to: "beliefs table"
      via: "query PostgREST status=neq.retired&confidence=gte.0.55"
      pattern: "beliefs\\?status=neq.retired"
    - from: "workers/mcp-server/src/index.ts getLastFatigueBySport"
      to: "session_analyses.sport + fatigue_type"
      via: "query per sport ordinata per created_at desc limit 1"
      pattern: "session_analyses\\?sport=eq"
    - from: "skills/propose_session.md"
      to: "get_weekly_context.active_beliefs"
      via: "Step 2 lettura + tag [athlete-belief: key]"
      pattern: "\\[athlete-belief:"
---

<objective>
Vertical slice ADAPT-02: dal belief in DB alla citazione inline nella prescrizione.

Estende il Worker MCP `getWeeklyContext()` con due campi (`active_beliefs`, `last_fatigue_by_sport` — D-09) e aggiorna `skills/propose_session.md` perché legga i beliefs attivi (dentro lo Step 2 esistente, senza doppia chiamata — Pitfall 6) e li citi inline con `[athlete-belief: key]` (D-10/D-11). Il campo `last_fatigue_by_sport` segue il formato per disciplina `{type, confidence, date}` o null (D-05). Dopo il deploy, una prescrizione può citare il belief `endurance_failure_type` seedato nel plan 01.

Purpose: I beliefs fisiologici devono raggiungere il LLM che propone le sessioni. Senza l'esposizione MCP e l'istruzione skill, il belief resta inerte nel DB. Questo plan chiude il ciclo seed→prescrizione.
Output: Worker esteso (deploy con wrangler) + skill che rende obbligatoria la citazione dei beliefs pertinenti.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-physiological-adaptation-intelligence/06-CONTEXT.md
@.planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md
@.planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md
@workers/mcp-server/src/index.ts
@skills/propose_session.md
</context>

<artifacts_this_phase_produces>
Vedi 06-01-PLAN.md §artifacts_this_phase_produces. Simboli CREATI in questo plan (esclusi dal drift-check):
- Campi MCP `active_beliefs`, `last_fatigue_by_sport` nel return di getWeeklyContext
- Helper TypeScript `getLastFatigueBySport(env, since)`
- Sotto-step in propose_session.md Step 2 per active_beliefs + last_fatigue_by_sport
- Tag `[athlete-belief: <belief_key>]` reso obbligatorio quando active_beliefs è rilevante
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Estendi getWeeklyContext() con active_beliefs + last_fatigue_by_sport + helper</name>
  <files>workers/mcp-server/src/index.ts</files>
  <read_first>
    - workers/mcp-server/src/index.ts linee 613-664 (getWeeklyContext: Promise.all destructuring, query active_constraints linea 635 con .catch(() => []), return object linee 638-663 con active_constraints/current_progression_step)
    - workers/mcp-server/src/index.ts linee 511-520 (helper sb(env, path): fetch PostgREST, throw se !resp.ok)
    - workers/mcp-server/src/index.ts linee 666+ (getRaceContext: pattern di funzione helper async esistente da imitare)
    - .planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md (getWeeklyContext self-extension: aggiunta al Promise.all, return object, helper getLastFatigueBySport)
    - .planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md §Pattern 7 + Pitfall 2 (perché filtrare per session_analyses.sport invece del JOIN)
  </read_first>
  <action>
    Estendi `getWeeklyContext()` in `workers/mcp-server/src/index.ts`. NON modificare i campi esistenti del return (D-09: solo aggiunte).

    1. Aggiungi due entry al `Promise.all()` (linee 622-636) e alla destructuring array. Nuove query, entrambe con `.catch(() => [])`:
       - `beliefs`: query PostgREST su `beliefs?status=neq.retired&confidence=gte.0.55&order=confidence.desc&select=belief_key,belief_text,status,confidence`
       - `fatigueBySport`: `getLastFatigueBySport(env, since)` (helper definito sotto)

    2. Aggiungi due campi al return object (dopo `active_constraints`, prima/dopo `current_progression_step`):
       - `active_beliefs: beliefs || []`
       - `last_fatigue_by_sport: fatigueBySport`

    3. Definisci la nuova funzione helper `async function getLastFatigueBySport(env: Env, since: string): Promise<Record<string, any>>` (pattern come getRaceContext/getHealth — interface-first, JSDoc breve). Restituisce il formato D-05 (oggetto per disciplina con `{type, confidence, date}` o `null`). Logica:
       - `const sports = ["run", "swim", "bike"];`
       - `const result: Record<string, any> = { run: null, swim: null, bike: null };`
       - per ogni sport: query `session_analyses?sport=eq.<sport>&fatigue_type=not.is.null&order=created_at.desc&limit=1&select=fatigue_type,fatigue_confidence,created_at` con `.catch(() => [])`
       - se `rows?.[0]` esiste: `result[sport] = { type: rows[0].fatigue_type, confidence: rows[0].fatigue_confidence, date: rows[0].created_at?.split("T")[0] }` (formato esatto D-05)
       - ritorna result.
       Questa query usa la colonna `sport` aggiunta a session_analyses dalla migration plan 01 e popolata dal plan 02 — evita il JOIN problematico session_analyses↔activities (Pitfall 2).

    Mantieni lo stile esistente: arrow/named functions come nel file, interface PascalCase, JSDoc sui simboli esportati.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('workers/mcp-server/src/index.ts','utf8'); if(!/active_beliefs/.test(s)) throw 'no active_beliefs'; if(!/last_fatigue_by_sport/.test(s)) throw 'no last_fatigue_by_sport'; if(!/async function getLastFatigueBySport/.test(s)) throw 'no helper'; if(!/session_analyses\?sport=eq/.test(s)) throw 'no sport query'; if(!/beliefs\?status=neq\.retired/.test(s)) throw 'no beliefs query'; console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `workers/mcp-server/src/index.ts` contiene `active_beliefs` e `last_fatigue_by_sport` nel return di getWeeklyContext
    - Contiene `async function getLastFatigueBySport(env: Env, since: string)`
    - Contiene la query `session_analyses?sport=eq.` (filtro per colonna sport, non JOIN)
    - Contiene la query `beliefs?status=neq.retired&confidence=gte.0.55`
    - L'helper restituisce per ogni sport `{type, confidence, date}` o null (formato D-05)
    - Le due nuove query usano `.catch(() => [])` (degradazione graceful)
    - I campi esistenti del return (active_constraints, current_progression_step, ecc.) restano invariati
    - `npx tsc --noEmit` nel dir del worker non produce nuovi errori sul file modificato (se tsconfig presente); altrimenti `node --check` o build wrangler dry-run riesce
  </acceptance_criteria>
  <done>getWeeklyContext ritorna active_beliefs e last_fatigue_by_sport (formato D-05); l'helper interroga session_analyses per sport usando la colonna sport.</done>
</task>

<task type="auto">
  <name>Task 2: Estendi propose_session.md Step 2 con active_beliefs + tag [athlete-belief]</name>
  <files>skills/propose_session.md</files>
  <read_first>
    - skills/propose_session.md linee 27-33 (Step 2 esistente: get_weekly_context, bullet active_constraints, active_mesocycle, daily_metrics)
    - skills/propose_session.md linee 125-138 (sezione citation tags: [source: ...] e "Quando applichi una belief: [athlete-belief: <descrizione>]")
    - .planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md (propose_session.md self-extension: bullet da aggiungere in Step 2, formato tag)
    - .planning/phases/06-physiological-adaptation-intelligence/06-CONTEXT.md (D-10/D-11 + esempio inline nel main set)
  </read_first>
  <action>
    Estendi lo Step 2 esistente di `skills/propose_session.md` (NON creare uno Step 2.5 separato — Pitfall 6: eviterebbe una seconda chiamata a get_weekly_context). Aggiungi due bullet dopo il bullet `active_constraints`:

    - bullet `active_beliefs` (confidence >= 0.55, D-10): istruisce a leggere i beliefs fisiologici attivi dal response di get_weekly_context. Per ogni belief rilevante alla disciplina del giorno, applicarlo e citarlo inline con tag `[athlete-belief: <belief_key>] — <motivazione specifica applicata alla sessione>`. Il tag appare nella riga del main set o del razionale dove il belief ha influenzato la scelta, NON in sezione separata (D-11).
    - bullet `last_fatigue_by_sport`: la fatica dell'ultima sessione per la disciplina del giorno. Se `type == 'muscular'` e `confidence >= 0.6`, verificare che il main set non superi la soglia neuromuscolare, citando il belief `endurance_failure_type`.

    Aggiorna la sezione citation tags (linea ~132) rendendo ESPLICITO che, quando `active_beliefs` contiene un belief pertinente alla disciplina, la citazione `[athlete-belief: <belief_key>]` è OBBLIGATORIA (non opzionale) (D-11). Usa il `belief_key` come identificatore nel tag (es. `[athlete-belief: endurance_failure_type]`), coerente con i dati esposti da get_weekly_context.

    NON modificare gli Step 0/1/3/4/5 esistenti. NON rimuovere la guida [source: ...] esistente.
  </action>
  <verify>
    <automated>python -m pytest tests/test_physio_adaptation.py::test_skill_active_beliefs_step -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `skills/propose_session.md` Step 2 contiene un bullet con `active_beliefs` e il riferimento confidence `0.55`
    - Contiene un bullet con `last_fatigue_by_sport`
    - Contiene la stringa `[athlete-belief:` come tag inline da usare nelle prescrizioni
    - La citazione `[athlete-belief: ...]` è descritta come obbligatoria quando active_beliefs è pertinente alla disciplina
    - Lo Step 2 resta un singolo step (nessuno Step 2.5 separato che richiama di nuovo get_weekly_context)
    - `python -m pytest tests/test_physio_adaptation.py::test_skill_active_beliefs_step -x -q` esce 0
  </acceptance_criteria>
  <done>propose_session.md istruisce a leggere active_beliefs/last_fatigue_by_sport dentro Step 2 e a citare i beliefs pertinenti inline con [athlete-belief: belief_key].</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Deploy Worker MCP + verifica active_beliefs e citazione belief in Claude.ai</name>
  <what-built>
    Worker MCP esteso (active_beliefs + last_fatigue_by_sport in get_weekly_context) e skill propose_session.md che cita i beliefs inline. Le modifiche al codice sono committate ma NON live finché il Worker non è deployato con wrangler (vincolo PROJECT.md: "il codice committato non è live finché non viene deployato").
  </what-built>
  <how-to-verify>
    1. Deploy del Worker: dalla cartella `workers/mcp-server/` esegui `wrangler deploy`. Conferma che il deploy riporti una nuova version id senza errori.
    2. Verifica il campo nel response MCP: da Claude.ai (connettore MCP) chiama `get_weekly_context` e controlla che il JSON contenga:
       - `active_beliefs`: un array che include almeno `{"belief_key": "endurance_failure_type", "confidence": 0.75, "status": "validated_belief", ...}` (il belief seedato nel plan 01)
       - `last_fatigue_by_sport`: un oggetto `{run, swim, bike}` (i valori sono `null` finché non esistono session_analyses con fatigue_type/sport — atteso se non ci sono ancora attività classificate)
    3. Verifica la citazione: chiedi a Claude "cosa faccio oggi?" per una sessione corsa o bici. Il razionale o il main set della prescrizione deve contenere un tag inline `[athlete-belief: endurance_failure_type] — ...` quando il belief è pertinente.
  </how-to-verify>
  <resume-signal>Scrivi "approved" se active_beliefs contiene endurance_failure_type e la prescrizione cita il belief inline; altrimenti descrivi cosa manca (es. active_beliefs vuoto, tag assente).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude.ai → MCP Worker (/mcp) | Richieste autenticate con MCP_BEARER_TOKEN (esistente, Phase 11) |
| MCP Worker → Supabase PostgREST | Query con service_role key; nuove query su beliefs e session_analyses ereditano RLS esistente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information disclosure | active_beliefs/last_fatigue_by_sport esposti via MCP | mitigate | Endpoint MCP già protetto da MCP_BEARER_TOKEN (Phase 11, deployato); nessuna nuova superficie auth introdotta (V2/V4 ASVS L1) |
| T-06-08 | Tampering | SQL injection via parametri query PostgREST | mitigate | sport è da lista statica `["run","swim","bike"]` (non input utente); since è generato server-side da daysAgoISO; nessun input atleta concatenato nelle nuove query |
| T-06-09 | Denial of Service | 3 query sequenziali in getLastFatigueBySport | accept | 3 query limit=1 su tabella indicizzata single-athlete; latenza trascurabile; ogni query ha .catch(() => []) per non bloccare il response |
| T-06-SC | Tampering | npm/cargo installs | mitigate | Nessun pacchetto npm nuovo nel Worker (RESEARCH.md: no new packages) — nessun rischio supply-chain |
</threat_model>

<verification>
- node grep: index.ts contiene active_beliefs, last_fatigue_by_sport, getLastFatigueBySport, query sport, query beliefs
- `python -m pytest tests/test_physio_adaptation.py::test_skill_active_beliefs_step -x -q` verde
- Checkpoint umano: wrangler deploy + get_weekly_context mostra endurance_failure_type + prescrizione cita il belief
</verification>

<success_criteria>
- get_weekly_context espone active_beliefs (incl. endurance_failure_type) e last_fatigue_by_sport
- propose_session.md cita i beliefs pertinenti inline con [athlete-belief: belief_key]
- Worker deployato e verificato live via Claude.ai (checkpoint)
</success_criteria>

<output>
Create `.planning/phases/06-physiological-adaptation-intelligence/06-03-SUMMARY.md` when done
</output>
