---
phase: 06-physiological-adaptation-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - migrations/2026-06-08-physiological-adaptation.sql
  - tests/test_fatigue_classification.py
  - tests/test_physio_adaptation.py
autonomous: true
requirements: [ADAPT-01, ADAPT-02, ADAPT-03]
must_haves:
  truths:
    - "La migration aggiunge fatigue_type, fatigue_confidence e sport a session_analyses con CHECK constraints"
    - "La migration seeda il belief endurance_failure_type in modo idempotente (ON CONFLICT DO NOTHING)"
    - "Esistono test (RED) per classify_fatigue_type e per gli artefatti statici ADAPT-02/03"
  artifacts:
    - path: "migrations/2026-06-08-physiological-adaptation.sql"
      provides: "ALTER TABLE session_analyses + seed belief endurance_failure_type"
      contains: "endurance_failure_type"
    - path: "tests/test_fatigue_classification.py"
      provides: "Unit test scaffold per classify_fatigue_type + belief update job"
      contains: "def test_cardiovascular_signal"
    - path: "tests/test_physio_adaptation.py"
      provides: "Static test su migration SQL + skill file"
      contains: "ON CONFLICT (belief_key) DO NOTHING"
  key_links:
    - from: "migrations/2026-06-08-physiological-adaptation.sql"
      to: "session_analyses"
      via: "ALTER TABLE ADD COLUMN IF NOT EXISTS"
      pattern: "fatigue_type"
    - from: "migrations/2026-06-08-physiological-adaptation.sql"
      to: "beliefs"
      via: "INSERT ... ON CONFLICT (belief_key) DO NOTHING"
      pattern: "endurance_failure_type"
---

<objective>
Fondazione Phase 6: crea la migration DB (Wave 0 schema) e lo scheletro di test (Wave 0 RED) che sbloccano tutti i plan successivi.

La migration aggiunge le colonne `fatigue_type`, `fatigue_confidence`, `sport` a `session_analyses` (D-04 + risoluzione Open Question 2) e seeda il belief `endurance_failure_type` (D-06). Solo questo belief viene seedato — gli altri emergono dall'evidenza accumulata nel plan 04 (D-07) — ed è un belief mutabile come ogni altro, aggiornabile da reinforce/contradict (D-08). I due file di test definiscono le aspettative di comportamento PRIMA dell'implementazione (Nyquist Wave 0): `test_fatigue_classification.py` (unit per ADAPT-01/03) e `test_physio_adaptation.py` (static per ADAPT-02).

Purpose: Ogni plan a valle (02 classificazione, 03 MCP+skill, 04 job belief) dipende da queste colonne DB e da questi test. Senza Wave 0, i `<verify>` dei plan successivi non hanno target.
Output: 1 migration SQL idempotente + 2 file di test (RED finché i plan 02-04 non implementano il codice).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-physiological-adaptation-intelligence/06-CONTEXT.md
@.planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md
@.planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Simboli e file CREATI in Phase 6 (esclusi dal drift-check — non sono pre-esistenti):

**File nuovi:**
- `migrations/2026-06-08-physiological-adaptation.sql`
- `tests/test_fatigue_classification.py`
- `tests/test_physio_adaptation.py`

**Colonne DB nuove (session_analyses):** `fatigue_type` (TEXT, CHECK in muscular/cardiovascular/mixed/insufficient_data), `fatigue_confidence` (FLOAT, CHECK 0-1), `sport` (TEXT)

**Belief seedato (beliefs):** `belief_key='endurance_failure_type'`, `value/belief_text='muscular-first ...'`, `evidence_n=8`, `confidence=0.75`, `status='validated_belief'`

**Funzioni Python nuove (definite nei plan 02/04):** `classify_fatigue_type()`, `_compute_hr_drift()`, `_compute_pace_drop()`, `FatigueResult` (dataclass), `update_beliefs_from_session_patterns()`

**Test nuovi (nomi):** `test_cardiovascular_signal`, `test_muscular_signal`, `test_fallback_rpe_only_no_splits`, `test_insufficient_data_short_session`, `test_missing_splits_low_rpe`, `test_belief_update_minimum_sessions`, `test_belief_update_skips_null_session_type`, `test_migration_session_analyses_columns`, `test_migration_belief_seed_idempotent`, `test_skill_active_beliefs_step`

**Campi MCP nuovi (get_weekly_context, definiti nel plan 03):** `active_beliefs`, `last_fatigue_by_sport`; helper TS `getLastFatigueBySport()`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Migration DB — colonne session_analyses + seed belief endurance_failure_type</name>
  <files>migrations/2026-06-08-physiological-adaptation.sql</files>
  <read_first>
    - migrations/2026-06-07-workout-prescription-quality.sql (pattern migration: header comment, ADD COLUMN IF NOT EXISTS, RLS policy, idempotenza)
    - migrations/2026-05-14-cognitive-mvp.sql (schema esatto tabella beliefs: belief_key UNIQUE, belief_text, confidence, evidence_n, status CHECK, source, source_metadata JSONB, last_reinforced_at, first_observed_at)
    - sql/schema.sql linee 282-291 (schema corrente session_analyses: activity_id TEXT, analysis_text TEXT, suggested_actions JSONB, model_used TEXT, cost_usd NUMERIC)
  </read_first>
  <action>
    Crea `migrations/2026-06-08-physiological-adaptation.sql` come file additivo idempotente (pattern da 2026-06-07). Header comment che cita Phase 6, ADAPT-01 (colonne session_analyses) e ADAPT-02 (seed belief).

    Blocco 1 — ALTER TABLE session_analyses (D-04 + Open Question 2): tre `ADD COLUMN IF NOT EXISTS` in un singolo ALTER:
    - `fatigue_type TEXT CHECK (fatigue_type IN ('muscular', 'cardiovascular', 'mixed', 'insufficient_data'))`
    - `fatigue_confidence FLOAT CHECK (fatigue_confidence IS NULL OR (fatigue_confidence >= 0 AND fatigue_confidence <= 1))`
    - `sport TEXT` (popolato da post_session_analysis.py nel plan 02; serve a getLastFatigueBySport nel plan 03 per evitare il JOIN problematico — Pitfall 2)

    Blocco 2 — seed belief ADAPT-02 (D-06, Pitfall 3 + Pitfall 5): INSERT in `beliefs` mappando i campi al VERO schema (NON usare `value`/`evidence_note`/`n` che non esistono). Colonne reali: `belief_key`, `belief_text`, `confidence`, `evidence_n`, `status`, `source`, `source_metadata`, `last_reinforced_at`, `first_observed_at`. Valori esatti:
    - `belief_key = 'endurance_failure_type'`
    - `belief_text = 'Nicolò è atleta endurance puro: primo cedimento muscolare, non cardiovascolare. HR rimane stabile anche ad alta intensità; il cedimento è al tono muscolare.'`
    - `confidence = 0.75`, `evidence_n = 8`, `status = 'validated_belief'`
    - `source = 'manual_seed'`
    - `source_metadata = '{"evidence_note": "Basato su CLAUDE.md §2: profilo atleta, confermato da storico élite 2021-2022 (114 sessioni)"}'::jsonb`
    - `last_reinforced_at = NOW()`, `first_observed_at = NOW()`
    Termina l'INSERT con `ON CONFLICT (belief_key) DO NOTHING` per idempotenza (Pitfall 5: belief_key è UNIQUE, secondo run non deve fallire).

    Solo questo singolo belief viene seedato: gli altri beliefs di risposta fisiologica emergono dall'evidenza reale nel plan 04 (D-07), non sono pre-inseriti. Il belief seedato NON è immutabile — entra nel normale lifecycle Bayesian e sarà aggiornato da reinforce_belief/contradict_belief come ogni altro belief (D-08); per questo è seedato come riga standard in `beliefs`, senza flag speciali.

    NON modificare planned_sessions. NON creare nuove tabelle. La migration è solo additiva.
  </action>
  <verify>
    <automated>python -c "import pathlib,re; s=pathlib.Path('migrations/2026-06-08-physiological-adaptation.sql').read_text(encoding='utf-8'); assert 'fatigue_type' in s and 'fatigue_confidence' in s and ('ADD COLUMN IF NOT EXISTS sport' in s.replace('  ',' ') or 'sport TEXT' in s); assert 'endurance_failure_type' in s; assert 'ON CONFLICT (belief_key) DO NOTHING' in s; assert 'source_metadata' in s and 'evidence_note' not in s.split('source_metadata')[0]; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `migrations/2026-06-08-physiological-adaptation.sql` esiste
    - Contiene `ALTER TABLE session_analyses` con `ADD COLUMN IF NOT EXISTS fatigue_type TEXT CHECK (` e i valori `'muscular', 'cardiovascular', 'mixed', 'insufficient_data'`
    - Contiene `ADD COLUMN IF NOT EXISTS fatigue_confidence FLOAT` con CHECK 0-1
    - Contiene `ADD COLUMN IF NOT EXISTS sport TEXT`
    - Contiene `INSERT INTO beliefs` con `belief_key`, `belief_text`, `confidence`, `evidence_n`, `status`, `source`, `source_metadata` (NON `value`, NON colonna `evidence_note`, NON `n`)
    - L'INSERT termina con `ON CONFLICT (belief_key) DO NOTHING`
    - `'evidence_note'` appare SOLO dentro il JSON di `source_metadata`, non come colonna SQL
    - Solo il belief `endurance_failure_type` è seedato (nessun INSERT di altri belief_key — D-07)
    - Nessun riferimento a `planned_sessions` nel file
  </acceptance_criteria>
  <done>La migration crea le 3 colonne su session_analyses con CHECK constraints e seeda il belief in modo idempotente, mappando evidence_note dentro source_metadata JSONB.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test scaffold RED — test_fatigue_classification.py + test_physio_adaptation.py</name>
  <files>tests/test_fatigue_classification.py, tests/test_physio_adaptation.py</files>
  <read_first>
    - tests/test_readiness.py (pattern pytest analytics: import da coach.analytics.readiness, factory helper make_*, assert su dataclass fields)
    - tests/test_active_constraints.py (pattern static test: ROOT = Path(__file__).resolve().parent.parent, read_text, assert "..." in src)
    - .planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md §Code Examples (corpo dei test pytest per classify_fatigue_type e make_splits_run)
    - .planning/phases/06-physiological-adaptation-intelligence/06-VALIDATION.md (Per-Task Verification Map: nomi esatti dei test richiesti)
  </read_first>
  <behavior>
    test_fatigue_classification.py — definisce le aspettative su classify_fatigue_type (implementata nel plan 02) e update_beliefs_from_session_patterns (implementata nel plan 04):
    - test_cardiovascular_signal: sport=run, durata 3600s, splits con HR 145→160 e pace stabile 280→282, RPE 7 → failure_type=='cardiovascular', confidence>=0.6
    - test_muscular_signal: sport=run, durata 3600s, HR stabile 150→152, pace degrada 263→285, RPE 8 → failure_type=='muscular', confidence>=0.6
    - test_fallback_rpe_only_no_splits: splits=None, RPE 9 → failure_type=='muscular', confidence==0.4, signal_used=='rpe_only'
    - test_insufficient_data_short_session: durata 1200s (20min) → failure_type is None
    - test_missing_splits_low_rpe: sport=bike, splits=None, RPE 5 → failure_type is None, signal_used=='rpe_only'
    - test_belief_update_minimum_sessions: il job NON crea belief quando un gruppo session_type ha < 3 sessioni (n<3 → skip)
    - test_belief_update_skips_null_session_type: il job NON crea belief responds_well_None/struggles_with_None per attività senza planned_session (session_type=None → skip, Pitfall 4)

    test_physio_adaptation.py — static checks sulla migration e sul skill:
    - test_migration_session_analyses_columns: migration contiene 'fatigue_type', 'fatigue_confidence', 'IF NOT EXISTS'
    - test_migration_belief_seed_idempotent: migration contiene 'ON CONFLICT (belief_key) DO NOTHING' e 'endurance_failure_type'
    - test_skill_active_beliefs_step: propose_session.md contiene 'active_beliefs' e '[athlete-belief:'
  </behavior>
  <action>
    Crea `tests/test_fatigue_classification.py` (pattern da test_readiness.py): `from coach.analytics.readiness import classify_fatigue_type`. Helper `make_splits_run(hr_first, hr_second, pace_first, pace_second, n=5)` che genera split sintetici (prima metà HR/pace primo valore, seconda metà secondo valore, key `avg_hr` e `avg_pace_s_per_km`). Implementa i 5 test unit su classify_fatigue_type elencati in <behavior> con gli assert esatti.

    Per i 2 test del job belief (`test_belief_update_minimum_sessions`, `test_belief_update_skips_null_session_type`): `from coach.coaching.pattern_extraction import update_beliefs_from_session_patterns`. Poiché il job legge da Supabase, usa monkeypatch su `coach.coaching.pattern_extraction.get_supabase` con un fake che restituisce dati controllati (segui il pattern di fake query già usato in tests/test_regressions.py per i guard di idempotency, se presente; altrimenti costruisci un fake minimale con `.table().select().gte()...execute()` che ritorna `.data`). Verifica che con n<3 e con session_type=None NON venga chiamato create_belief/reinforce_belief (monkeypatch su quelle funzioni con un contatore).

    Crea `tests/test_physio_adaptation.py` (pattern da test_active_constraints.py): `ROOT = Path(__file__).resolve().parent.parent`. Leggi `migrations/2026-06-08-physiological-adaptation.sql` e `skills/propose_session.md` con read_text(encoding='utf-8'). Implementa i 3 static test elencati in <behavior>.

    Questi test sono RED ora (classify_fatigue_type e update_beliefs_from_session_patterns non esistono ancora; il tag active_beliefs nel skill arriva nel plan 03). È atteso e corretto: definiscono il contratto Wave 0. NON marcarli xfail/skip — devono fallire con ImportError/AssertionError finché i plan 02/03/04 non li rendono GREEN.
  </action>
  <verify>
    <automated>python -m pytest tests/test_physio_adaptation.py::test_migration_session_analyses_columns tests/test_physio_adaptation.py::test_migration_belief_seed_idempotent -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_fatigue_classification.py` esiste e definisce: test_cardiovascular_signal, test_muscular_signal, test_fallback_rpe_only_no_splits, test_insufficient_data_short_session, test_missing_splits_low_rpe, test_belief_update_minimum_sessions, test_belief_update_skips_null_session_type
    - `tests/test_fatigue_classification.py` contiene `from coach.analytics.readiness import classify_fatigue_type` e helper `def make_splits_run(`
    - `tests/test_physio_adaptation.py` esiste e definisce: test_migration_session_analyses_columns, test_migration_belief_seed_idempotent, test_skill_active_beliefs_step
    - `python -m pytest tests/test_physio_adaptation.py::test_migration_session_analyses_columns tests/test_physio_adaptation.py::test_migration_belief_seed_idempotent -x -q` esce 0 (i due test static sono GREEN perché la migration del Task 1 esiste già)
    - Eseguendo `python -m pytest tests/test_fatigue_classification.py -q` i test unit su classify_fatigue_type FALLISCONO con ImportError o AttributeError (atteso: funzione non ancora implementata — RED Wave 0)
    - Nessun test marcato `@pytest.mark.skip` o `xfail`
  </acceptance_criteria>
  <done>Esistono i due file di test con i nomi esatti del Per-Task Verification Map; i test static sulla migration sono verdi, i test unit su classify_fatigue_type sono RED (funzione non ancora implementata).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI/SQL editor → Supabase | La migration viene eseguita manualmente nel SQL editor Supabase con service_role; input controllato dal developer |
| Migration re-run (retry CI / rideploy) | Una seconda esecuzione non deve corrompere dati né fallire |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | session_analyses.fatigue_type values | mitigate | CHECK constraint SQL limita i valori a ('muscular','cardiovascular','mixed','insufficient_data'); fatigue_confidence CHECK 0-1 (V5 ASVS L1) |
| T-06-02 | Tampering | beliefs seed (doppia esecuzione migration) | mitigate | `ON CONFLICT (belief_key) DO NOTHING` rende il seed idempotente; nessun duplicato né errore al re-run (Pitfall 5) |
| T-06-03 | Denial of Service | migration ALTER su tabella in uso | accept | session_analyses è tabella append-only a basso volume single-athlete; ALTER ADD COLUMN IF NOT EXISTS è non-bloccante in Postgres |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nessun pacchetto nuovo installato in Phase 6 (RESEARCH.md §Package Legitimacy: non si applica) — nessun rischio supply-chain |
</threat_model>

<verification>
- `python -c` sulla migration: presenti le 3 colonne, il seed belief, ON CONFLICT, evidence_note solo dentro source_metadata
- `python -m pytest tests/test_physio_adaptation.py -q` verde (3 static test)
- `python -m pytest tests/test_fatigue_classification.py -q` rosso atteso (classify_fatigue_type non implementata — Wave 0 RED)
</verification>

<success_criteria>
- Migration idempotente esiste con colonne ADAPT-01 + seed belief ADAPT-02
- Test scaffold Wave 0 esiste per ADAPT-01 (unit) e ADAPT-02 (static) e ADAPT-03 (unit job)
- I test static sulla migration passano; i test unit sono RED in attesa dei plan 02-04
</success_criteria>

<output>
Create `.planning/phases/06-physiological-adaptation-intelligence/06-01-SUMMARY.md` when done
</output>
