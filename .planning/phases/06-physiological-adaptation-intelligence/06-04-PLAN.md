---
phase: 06-physiological-adaptation-intelligence
plan: 04
type: tdd
wave: 4
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - coach/coaching/pattern_extraction.py
autonomous: false
requirements: [ADAPT-03]
must_haves:
  truths:
    - "update_beliefs_from_session_patterns() raggruppa session_analyses per session_type e chiama reinforce/contradict belief solo con n>=3 (zero LLM)"
    - "Il job salta i gruppi con session_type=None (attività fuori piano) — nessun belief responds_well_None"
    - "Quando un belief responds_well_* raggiunge validated_belief, il job aggiorna mesocycles.progression_plan incrementando lo step della tipologia"
    - "Il job gira incondizionatamente in extract_patterns(), anche con budget LLM esaurito"
  artifacts:
    - path: "coach/coaching/pattern_extraction.py"
      provides: "update_beliefs_from_session_patterns() + call site in extract_patterns + update progression_plan"
      contains: "def update_beliefs_from_session_patterns("
  key_links:
    - from: "coach/coaching/pattern_extraction.py update_beliefs_from_session_patterns"
      to: "coach.analytics.belief_engine reinforce_belief/contradict_belief/create_belief"
      via: "lazy import + chiamata condizionale su n>=3"
      pattern: "reinforce_belief\\(|contradict_belief\\("
    - from: "coach/coaching/pattern_extraction.py update_beliefs_from_session_patterns"
      to: "mesocycles.progression_plan"
      via: "update quando belief responds_well_* è validated_belief"
      pattern: "progression_plan"
    - from: "coach/coaching/pattern_extraction.py extract_patterns"
      to: "update_beliefs_from_session_patterns"
      via: "call site prima/fuori dal try LLM"
      pattern: "update_beliefs_from_session_patterns\\("
---

<objective>
Vertical slice ADAPT-03: dai pattern di sessione accumulati all'aggiornamento automatico dei beliefs e della progressione.

Aggiunge `update_beliefs_from_session_patterns()` a `pattern_extraction.py` (estensione del job settimanale già in produzione, D-12): deterministico, zero LLM, raggruppa le `session_analyses` per `session_type`, e per ogni tipologia con ≥3 sessioni rinforza/contraddice il belief di risposta (D-13/D-14). Quando un belief `responds_well_*` diventa `validated_belief`, aggiorna `mesocycles.progression_plan` (D-15). Il job gira anche a budget LLM esaurito. Questo rende GREEN i test test_belief_update_minimum_sessions e test_belief_update_skips_null_session_type del plan 01.

Purpose: Chiude il loop adattativo: le classificazioni del plan 02 e i beliefs del plan 03 vengono ora aggiornati automaticamente sui pattern osservati, e la progressione del mesociclo si adatta. È l'ultimo plan della phase — include il phase gate.
Output: job belief update testato + aggiornamento progression_plan + phase gate checkpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-physiological-adaptation-intelligence/06-CONTEXT.md
@.planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md
@.planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md
@coach/coaching/pattern_extraction.py
@coach/analytics/belief_engine.py
</context>

<artifacts_this_phase_produces>
Vedi 06-01-PLAN.md §artifacts_this_phase_produces. Simboli CREATI in questo plan (esclusi dal drift-check):
- `update_beliefs_from_session_patterns(days=14) -> dict` in coach/coaching/pattern_extraction.py
- Helper privato opzionale `_advance_progression_for_belief(...)`
- Beliefs generati a runtime: naming `responds_well_{session_type}` / `struggles_with_{session_type}` (D-14)
- Call site di update_beliefs_from_session_patterns dentro extract_patterns()
- Logica di update di mesocycles.progression_plan quando belief responds_well_* è validated
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: update_beliefs_from_session_patterns() — job deterministico belief update</name>
  <files>coach/coaching/pattern_extraction.py</files>
  <read_first>
    - coach/coaching/pattern_extraction.py linee 1-26 (imports: defaultdict, statistics, today_rome, get_supabase, BudgetExceededError) e linee 46-100 (extract_biometric_patterns come modello: divisore # ===, fetch Supabase, groupby defaultdict, statistics.fmean)
    - coach/coaching/pattern_extraction.py linee 185-271 (extract_patterns: call site biometric linea 195, try/except BudgetExceededError linee 261-271)
    - coach/analytics/belief_engine.py linee 155-318 (firme: create_belief(belief_key, belief_text, initial_confidence=0.4, ..., source=None, metadata=None) -> str; reinforce_belief(belief_key, related_outcome_id=None, reason=None) -> Optional[Belief]; contradict_belief(belief_key, related_outcome_id=None, reason=None); list_beliefs(min_status='weak_belief')); thresholds VALIDATED_MIN_N=8, VALIDATED_MIN_CONFIDENCE=0.7 linee 38-39)
    - tests/test_fatigue_classification.py (i 2 test RED test_belief_update_minimum_sessions e test_belief_update_skips_null_session_type — contratto)
    - .planning/phases/06-physiological-adaptation-intelligence/06-PATTERNS.md (pattern_extraction self-extension: struttura funzione, lazy import belief_engine, guard session_type None, call site)
    - .planning/phases/06-physiological-adaptation-intelligence/06-RESEARCH.md §Pattern 6 + Open Question 3 (come ottenere session_type: due query Python via activities.external_id->activities.id->planned_sessions.completed_activity_id; oppure leggere sport gia su session_analyses dal plan 02)
  </read_first>
  <behavior>
    - Con un gruppo session_type 'threshold_run' che ha 2 sole session_analyses → il job NON crea ne rinforza beliefs (n<3 → skip). create_belief/reinforce_belief non vengono chiamati.
    - Con session_analyses la cui session_type risolve a None (attivita senza planned_session) → il job salta il gruppo (continue), nessun belief responds_well_None/struggles_with_None creato (Pitfall 4).
    - Con un gruppo 'threshold_run' di 3+ sessioni e avg_rpe <= 7.5 senza prevalenza fatigue_type='cardiovascular' → chiama reinforce_belief('responds_well_threshold_run', ...) o create_belief se non esiste.
    - Con avg_rpe > 7.5 o prevalenza degradazione → chiama contradict_belief o crea struggles_with_*.
    - Il job ritorna un dict {'updated': int, 'created': int, 'skipped': int, 'errors': int}.
  </behavior>
  <action>
    Aggiungi a `coach/coaching/pattern_extraction.py` una nuova sezione con divisore `# ===...===` "ADAPT-03 — Belief update from session patterns (rule-based, zero LLM cost)" e la funzione `update_beliefs_from_session_patterns(days: int = 14) -> dict`.

    Lazy import dentro la funzione (non a livello modulo, pattern PATTERNS.md): `from coach.analytics.belief_engine import reinforce_belief, contradict_belief, create_belief, list_beliefs`.

    Logica deterministica (zero call a llm_client — vincolo):
    1. `sb = get_supabase()`; `since = (today_rome() - timedelta(days=days)).isoformat()`.
    2. Leggi session_analyses recenti: `sb.table("session_analyses").select("activity_id,sport,fatigue_type,fatigue_confidence,created_at").gte("created_at", since).not_.is_("fatigue_type", None).execute().data or []`. (La colonna sport esiste dal plan 02; usala per arricchire i raggruppamenti.)
    3. Per ogni analysis, risolvi il `session_type` (Open Question 3): query `activities` con `external_id == activity_id` per ottenere `activities.id`, poi `planned_sessions` con `completed_activity_id == activities.id` per leggere `session_type`. Se non risolve (nessuna planned_session) → `session_type = None`.
    4. Raggruppa per `session_type` con `defaultdict(list)`. **Guard obbligatorio: `if not session_type: continue`** (Pitfall 4 — niente belief con session_type None).
    5. Per ogni gruppo con `len >= 3`:
       - calcola `avg_rpe` (recupera RPE dalla subjective_log collegata all'attivita, o salta il fattore RPE se assente) e la prevalenza di `fatigue_type` ('cardiovascular' vs 'muscular').
       - pattern positivo (D-13): `avg_rpe <= 7.5` (quando disponibile) e nessuna prevalenza di degradazione/cardiovascular → `reinforce_belief(f"responds_well_{session_type}", reason=f"n={n}, avg_rpe={avg_rpe}")`; se reinforce ritorna None (belief inesistente) → `create_belief(f"responds_well_{session_type}", belief_text=..., initial_confidence=0.4, source='pattern_extraction_job', metadata={...})`.
       - pattern negativo → `create_belief`/`contradict_belief` su `f"struggles_with_{session_type}"`.
       Usa naming convention `responds_well_{session_type}` / `struggles_with_{session_type}` (D-14). evidence_note (Claude's Discretion): breve, formato `n=X, avg_rpe=Y, periodo=Ngg`.
    6. Mantieni un contatore e ritorna `{'updated', 'created', 'skipped', 'errors'}`. Avvolgi ogni operazione belief in try/except che incrementa 'errors' e logga, senza interrompere il loop.

    NON chiamare create_belief bypassando i guardrail (l'API pubblica li chiama gia — anti-pattern RESEARCH.md). NON scrivere su planned_sessions. NON usare LLM.
  </action>
  <verify>
    <automated>python -m pytest tests/test_fatigue_classification.py::test_belief_update_minimum_sessions tests/test_fatigue_classification.py::test_belief_update_skips_null_session_type -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `coach/coaching/pattern_extraction.py` contiene `def update_beliefs_from_session_patterns(days: int = 14) -> dict`
    - Contiene il lazy import `from coach.analytics.belief_engine import reinforce_belief, contradict_belief, create_belief, list_beliefs` dentro la funzione
    - Contiene il guard `if not session_type:` con `continue` (skip session_type None)
    - Usa il naming `responds_well_` e `struggles_with_` per i belief_key generati
    - La funzione NON contiene import o chiamate a `llm_client`/`get_client_for_purpose`
    - Ritorna un dict con chiavi `updated`, `created`, `skipped`, `errors`
    - `python -m pytest tests/test_fatigue_classification.py::test_belief_update_minimum_sessions tests/test_fatigue_classification.py::test_belief_update_skips_null_session_type -x -q` esce 0
  </acceptance_criteria>
  <done>Il job deterministico raggruppa per session_type, salta i gruppi <3 e quelli con session_type None, e rinforza/crea beliefs responds_well_/struggles_with_ via belief_engine.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update progression_plan + call site in extract_patterns()</name>
  <files>coach/coaching/pattern_extraction.py</files>
  <read_first>
    - coach/coaching/pattern_extraction.py linee 185-271 (extract_patterns: call site biometric linea 195, blocco try LLM, except BudgetExceededError linee 261-271)
    - coach/analytics/belief_engine.py linee 304-326 (list_beliefs per leggere status validated_belief) e linee 38-39 (VALIDATED_MIN_N=8, VALIDATED_MIN_CONFIDENCE=0.7)
    - migrations/2026-06-07-workout-prescription-quality.sql linee 33-39 (struttura mesocycles.progression_plan JSONB: { "run_threshold": { "week1": "4x6min", "week2": "5x6min" } })
    - .planning/phases/06-physiological-adaptation-intelligence/06-CONTEXT.md (D-15: incremento step quando responds_well_* validated; coerenza con Phase 5 D-27/D-28)
  </read_first>
  <behavior>
    - Quando un belief `responds_well_{session_type}` raggiunge status validated_belief (n>=8, conf>0.7), il job legge mesocycles.progression_plan del mesociclo attivo e avanza lo step per quella tipologia (es. da week1 verso week2/week3 del piano esistente), poi scrive su mesocycles.progression_plan.
    - Se il mesociclo attivo non ha progression_plan o non contiene la chiave tipologia → no-op (non inventa step).
    - extract_patterns() chiama update_beliefs_from_session_patterns() incondizionatamente, anche se il call LLM solleva BudgetExceededError.
  </behavior>
  <action>
    1. Dentro `update_beliefs_from_session_patterns()` (o in un helper privato `_advance_progression_for_belief(sb, session_type)` chiamato da essa), aggiungi la logica D-15: dopo aver rinforzato un belief `responds_well_{session_type}`, verifica via `list_beliefs(min_status='validated_belief')` (o rilettura della riga) se quel belief e `validated_belief`. Se si:
       - leggi il mesociclo attivo: `sb.table("mesocycles").select("id,progression_plan").lte("start_date", today).gte("end_date", today)` con `.limit(1)`.
       - se `progression_plan` contiene una chiave coerente con `session_type` (match per substring/normalizzazione, es. 'run_threshold' <-> 'threshold_run'): avanza lo step corrente alla settimana successiva del piano esistente (week1->week2->week3, senza superare l'ultima chiave definita).
       - scrivi il progression_plan aggiornato: `sb.table("mesocycles").update({"progression_plan": updated}).eq("id", meso_id).execute()`.
       - se non c'e mesociclo attivo o la chiave non esiste → no-op (NON inventare step, NON scrivere planned_sessions).

    2. Aggiungi il call site in `extract_patterns()`: subito dopo `biometric = extract_biometric_patterns(days)` (linea 195) e PRIMA del blocco try del call LLM, inserisci:
       - `belief_update_result = update_beliefs_from_session_patterns(days=14)`
       - `logger.info("Belief update: %s", belief_update_result)`
       Posizionandolo prima del try, il job gira anche quando il call LLM solleva BudgetExceededError (vincolo: aggiornamento beliefs affidabile a budget esaurito — RESEARCH.md State of the Art).

    NON spostare il call site dentro il blocco LLM. NON toccare planned_sessions. La scrittura riguarda solo mesocycles.progression_plan (metadato).
  </action>
  <verify>
    <automated>python -m pytest tests/test_fatigue_classification.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `coach/coaching/pattern_extraction.py` `extract_patterns()` contiene la chiamata `update_beliefs_from_session_patterns(days=14)` posizionata dopo `biometric = extract_biometric_patterns(days)` e prima del blocco `try` del call LLM
    - Contiene `logger.info("Belief update: %s", belief_update_result)`
    - La logica di update legge e scrive `mesocycles.progression_plan` (presente la stringa `progression_plan` nel codice della funzione/helper)
    - La scrittura su mesocycles avviene solo quando il belief responds_well_* e validated_belief e la chiave tipologia esiste gia nel progression_plan (no-op altrimenti)
    - Nessuna scrittura su `planned_sessions` nel codice aggiunto
    - `python -m pytest tests/test_fatigue_classification.py -q` esce 0 (tutti i test ADAPT-01/03 GREEN)
  </acceptance_criteria>
  <done>extract_patterns() esegue il job belief update incondizionatamente; il job avanza progression_plan quando un belief responds_well_* diventa validated, senza toccare planned_sessions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Phase gate Phase 6 — suite verde + job belief + 3 success criteria ADAPT end-to-end</name>
  <what-built>
    Job belief update deterministico in pattern_extraction.py (settimanale, zero LLM): raggruppa le session_analyses per session_type, rinforza/crea beliefs responds_well_/struggles_with_, e avanza mesocycles.progression_plan quando un belief diventa validated. Phase gate: questo e l'ultimo plan di Phase 6 — verifica end-to-end dei 3 success criteria della phase.
  </what-built>
  <how-to-verify>
    1. Suite completa verde: esegui `python -m pytest tests/ -v`. Tutti i test devono passare (incl. test_fatigue_classification.py e test_physio_adaptation.py — Nyquist phase gate).
    2. Esecuzione job a secco: esegui `python -m coach.coaching.pattern_extraction`. Nel log deve comparire la riga `Belief update: {...}` con i contatori (updated/created/skipped/errors) — conferma che il job gira nel flusso settimanale.
    3. ADAPT-01 end-to-end (success criterion 1): dopo un sync Garmin reale con almeno una sessione >30min con splits, verifica in Supabase che la riga `session_analyses` piu recente abbia `fatigue_type` valorizzato (muscular/cardiovascular/mixed/insufficient_data), `fatigue_confidence` e `sport` non null.
    4. ADAPT-02 end-to-end (success criterion 2): conferma (dal checkpoint del plan 03 gia approvato) che `get_weekly_context.active_beliefs` contiene `endurance_failure_type` con confidence 0.75 e che una prescrizione lo cita con `[athlete-belief: endurance_failure_type]`.
    5. ADAPT-03 end-to-end (success criterion 3): se ci sono >=3 sessioni della stessa tipologia negli ultimi 14gg, verifica in Supabase che esista un belief `responds_well_<tipologia>` o `struggles_with_<tipologia>` in `beliefs`. Se non ci sono ancora 3 sessioni della stessa tipologia, conferma che NON esistono beliefs spuri (es. responds_well_None) e che il job ha loggato lo skip — comportamento corretto.
  </how-to-verify>
  <resume-signal>Scrivi "approved" se la suite e verde, il job logga Belief update, e i 3 success criteria ADAPT sono verificati (o, per ADAPT-03, confermato l'assenza di beliefs spuri quando n<3). Altrimenti descrivi cosa manca.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session_analyses + subjective_log (dati DB) → job belief update | Input dal DB single-athlete; il job non riceve input esterno non fidato |
| job belief update → beliefs table (reinforce/contradict/create) | Le scritture passano per l'API belief_engine che applica i guardrail |
| job belief update → mesocycles.progression_plan | Unica scrittura ammessa; planned_sessions resta intoccato (vincolo inviolabile §5.4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10 | Tampering | belief manipulation via job (overfitting su pochi dati) | mitigate | Soglia n>=3 per gruppo; create_belief chiama check_belief_admissible (belief_guardrails) internamente — anti-overfitting gia codificato (V5 ASVS L1) |
| T-06-11 | Tampering | belief spuri responds_well_None da attivita fuori piano | mitigate | Guard `if not session_type: continue` salta i gruppi senza planned_session (Pitfall 4) |
| T-06-12 | Tampering | scrittura non autorizzata su planned_sessions | mitigate | Il job scrive SOLO su mesocycles.progression_plan (metadato); nessun path di scrittura verso planned_sessions nel codice (vincolo §5.4 inviolabile) |
| T-06-13 | Denial of Service | errore su un belief blocca l'intero job settimanale | mitigate | Ogni operazione belief in try/except con contatore errors; il loop prosegue; il job gira fuori dal try LLM e non e gated da BudgetExceededError |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | Nessun pacchetto nuovo (solo stdlib + belief_engine esistente) — nessun rischio supply-chain |
</threat_model>

<verification>
- `python -m pytest tests/test_fatigue_classification.py -q` → test_belief_update_minimum_sessions + test_belief_update_skips_null_session_type GREEN
- `python -m pytest tests/ -v` → suite intera verde (phase gate Nyquist)
- `python -m coach.coaching.pattern_extraction` → log "Belief update: {...}"
- Checkpoint umano: 3 success criteria ADAPT verificati end-to-end
</verification>

<success_criteria>
- update_beliefs_from_session_patterns implementata, deterministica, testata (2 test GREEN)
- extract_patterns esegue il job incondizionatamente; progression_plan avanza su belief validated
- Suite pytest completa verde
- 3 success criteria di Phase 6 (ROADMAP) verificati al checkpoint
</success_criteria>

<output>
Create `.planning/phases/06-physiological-adaptation-intelligence/06-04-SUMMARY.md` when done
</output>
