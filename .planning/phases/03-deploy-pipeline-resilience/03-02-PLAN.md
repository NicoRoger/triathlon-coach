---
phase: 03-deploy-pipeline-resilience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ingest.yml
autonomous: true
requirements: [DEPLOY-04, PIPELINE-01]
must_haves:
  truths:
    - "The Apply-accepted-modulations step in ingest.yml runs only on ingest success, not on Garmin failure (D-06)"
    - "apply_accepted_modulations transitions a real accepted modulation to applied (accepted → applied)"
    - "The Garmin sync step propagates exit 1 after 3 failed attempts (no silent green on stale data)"
  artifacts:
    - path: ".github/workflows/ingest.yml"
      provides: "ingest pipeline with apply-accepted step gated on success and exit-1-on-failure Garmin retry loop"
      contains: "python -m coach.coaching.modulation --apply-accepted"
  key_links:
    - from: ".github/workflows/ingest.yml"
      to: "coach.coaching.modulation.apply_accepted_modulations"
      via: "python -m coach.coaching.modulation --apply-accepted CLI step"
      pattern: "modulation --apply-accepted"
    - from: ".github/workflows/ingest.yml Garmin step"
      to: "GitHub Actions step result"
      via: "exit 1 after 3 failed attempts"
      pattern: "exit 1"
---

## Phase Goal

**As** Nicolò, **I want** accepted plan modulations to be applied only when fresh Garmin data is present and a failed Garmin sync to surface as a red pipeline run, **so that** my plan is never changed on stale data and a broken sync never hides behind a green checkmark.

> Note: deploy/verification phase, no UI. User-story framing adapted to the pipeline domain (see plan 03-01 for the ROADMAP goal-line discrepancy note).

<objective>
Fix the one-line `ingest.yml` bug where the "Apply accepted modulations (audit K1)" step uses `if: always()` (line 98) contrary to D-06 — it must run only on ingest success. Then prove DEPLOY-04 (the wired `--apply-accepted` step drives a real `accepted → applied` transition) and PIPELINE-01 (the L1 exit-code fix is intact so a failed Garmin sync exits 1). This is the modulation-wiring + exit-code vertical slice: one deploy+verify unit.

Purpose: D-05/D-06 require modulations to apply only on fresh data and to never block ingest. The current `if: always()` would apply a stale accepted modulation even when Garmin sync failed (RESEARCH.md Pitfall 2). PIPELINE-01's L1 fix is already committed; this plan verifies it observably rather than assuming it works (REQUIREMENTS.md principle: "non dare per scontato che i fix committati funzionino").

Output: `.github/workflows/ingest.yml` with `if: always()` removed from the apply-accepted step (keeping `continue-on-error: true`), plus passing evidence for DEPLOY-04 and PIPELINE-01.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deploy-pipeline-resilience/03-CONTEXT.md
@.planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md
@.planning/phases/03-deploy-pipeline-resilience/03-PATTERNS.md
@.github/workflows/ingest.yml
@coach/coaching/modulation.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove if: always() from the Apply-accepted-modulations step in ingest.yml (D-06)</name>
  <files>.github/workflows/ingest.yml</files>
  <read_first>
    - .github/workflows/ingest.yml (current state — the target step is "Apply accepted modulations (audit K1)" at lines 95-100, with `if: always()` on line 98)
    - .planning/phases/03-deploy-pipeline-resilience/03-PATTERNS.md §"ingest.yml" (current vs target YAML; reference step "Pre-test prediction (Phase 2.6)" at lines 85-93 uses `if: success()` + `continue-on-error: true`)
    - .planning/phases/03-deploy-pipeline-resilience/03-CONTEXT.md (D-04, D-05, D-06: separate step after Garmin block; log-and-continue on failure; no if: always())
  </read_first>
  <action>
    In `.github/workflows/ingest.yml`, locate the step named `Apply accepted modulations (audit K1)` (currently lines 95-100). Delete the line `    if: always()` (line 98). Keep `continue-on-error: true` and keep `run: python -m coach.coaching.modulation --apply-accepted` unchanged. With `if:` removed, the step defaults to `if: success()` per GitHub Actions semantics — it runs only when all prior steps (including the Garmin sync that exits 1 on failure) succeeded, satisfying D-06 ("senza if: always() per evitare esecuzione su ingest fallito"). The `continue-on-error: true` stays so that a modulation-apply failure (or zero accepted modulations) logs and continues without blocking ingest, satisfying D-05.

    Do NOT change the step name, the run command, the order of the step, or any other step in the file. This is exactly a one-line deletion. Do NOT inline apply_accepted_modulations into a Python module — D-04 mandates it stays a separate ingest.yml step.
  </action>
  <verify>
    <automated>python -c "import re; s=open('.github/workflows/ingest.yml',encoding='utf-8').read(); i=s.index('Apply accepted modulations'); blk=s[i:i+250]; assert 'if: always()' not in blk, 'if: always() still present on apply step'; assert 'continue-on-error: true' in blk; assert '--apply-accepted' in blk; print('ingest-step-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - The 250-char block following "Apply accepted modulations" in ingest.yml contains NO `if: always()`.
    - That block still contains `continue-on-error: true` and `python -m coach.coaching.modulation --apply-accepted`.
    - The Garmin sync step still contains its `exit 1` (no accidental edit to other steps) — confirm `exit 1` still present in the file.
    - The file still parses as valid YAML (no structural breakage from the deletion).
  </acceptance_criteria>
  <done>The apply-accepted-modulations step no longer has if: always(), retains continue-on-error: true and the --apply-accepted run command, and ingest.yml remains valid YAML.</done>
</task>

<task type="auto">
  <name>Task 2: Verify DEPLOY-04 (accepted → applied) and PIPELINE-01 (exit 1) via regression tests and source assertions</name>
  <files>.github/workflows/ingest.yml</files>
  <read_first>
    - coach/coaching/modulation.py (apply_accepted_modulations at line 175 — selects status='accepted', applies, recounts final status; CLI main() at line 358 with --apply-accepted at line 366)
    - tests/test_audit_resilience.py (test_k1_accepted_modulation_gets_applied at line 826 covers the accepted→applied transition; test_d2_full_apply_accepted at line 488)
    - .github/workflows/ingest.yml Garmin sync step (lines 49-64 — the L1 ok=0 / exit 1 retry loop for PIPELINE-01)
  </read_first>
  <action>
    DEPLOY-04 evidence (automated, deterministic): run `pytest tests/test_audit_resilience.py::test_k1_accepted_modulation_gets_applied tests/test_audit_resilience.py::test_d2_full_apply_accepted -x -q` and confirm both pass. These exercise `apply_accepted_modulations()` driving a row from `status='accepted'` to `status='applied'`. Also assert by source inspection that ingest.yml contains the literal step `python -m coach.coaching.modulation --apply-accepted` (the wiring required by DEPLOY-04) and that `coach/coaching/modulation.py` defines `apply_accepted_modulations` and a `--apply-accepted` argparse flag.

    PIPELINE-01 evidence (source assertion, since a live failed Actions run cannot be forced deterministically here): assert the Garmin sync step in ingest.yml contains the `ok=0` initialization, the `for i in 1 2 3` retry loop, and the terminal `if [ "$ok" -ne 1 ]; then ... exit 1; fi` block — i.e., the last command on total failure is `exit 1`, NOT `sleep`. Record in the SUMMARY that full PIPELINE-01 confirmation (a real failed run propagating exit 1 in the Actions log) is performed in plan 03-04 / Phase 4 against a live run; this task's bar is "the exit-1 code path is present and intact".

    Do NOT modify modulation.py or the Garmin step — this task is verification only.
  </action>
  <verify>
    <automated>pytest tests/test_audit_resilience.py::test_k1_accepted_modulation_gets_applied tests/test_audit_resilience.py::test_d2_full_apply_accepted -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_audit_resilience.py::test_k1_accepted_modulation_gets_applied tests/test_audit_resilience.py::test_d2_full_apply_accepted -x -q` reports 2 passed, 0 failed.
    - `coach/coaching/modulation.py` contains `def apply_accepted_modulations` and the argparse flag `--apply-accepted`.
    - `.github/workflows/ingest.yml` Garmin step contains `ok=0`, `for i in 1 2 3`, and `exit 1` (the L1 code path).
    - SUMMARY records that the live-Actions-log confirmation for PIPELINE-01 is deferred to plan 03-04 / Phase 4.
  </acceptance_criteria>
  <done>Both apply-accepted regression tests pass, the --apply-accepted wiring exists in ingest.yml, and the L1 exit-1 retry path is confirmed present in the Garmin step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → Supabase | apply_accepted_modulations writes plan changes to planned_sessions; must only fire on fresh data |
| Telegram bot tap → plan_modulations.status='accepted' | Athlete acceptance crosses into a DB status that this step later acts on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | planned_sessions written by apply_accepted_modulations on stale data | mitigate | Removing `if: always()` (D-06) gates the apply step on ingest success, so a failed Garmin sync (which now exits 1, PIPELINE-01) blocks the step — no plan write on stale data (RESEARCH.md Pitfall 2) |
| T-03-05 | Denial of Service | ingest pipeline blocked by a modulation-apply error | accept | `continue-on-error: true` is intentionally retained (D-05) so a modulation failure logs and continues; the more-critical Garmin ingest is never blocked by the apply step |
| T-03-06 | Repudiation | silent-green pipeline hiding a failed Garmin sync | mitigate | L1 exit-1 retry loop (PIPELINE-01) makes a 3-times-failed sync turn the run red; this plan asserts the exit-1 path is intact |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH.md §Package Legitimacy Audit: none); no install task, no slopcheck gate needed |
</threat_model>

<verification>
- `python -c "...assert 'if: always()' not in <apply-step-block>..."` confirms the one-line fix (prints "ingest-step-ok").
- `pytest tests/test_audit_resilience.py::test_k1_accepted_modulation_gets_applied tests/test_audit_resilience.py::test_d2_full_apply_accepted -x -q` → 2 passed.
- Source assertion: ingest.yml Garmin step retains `exit 1`; apply step retains `--apply-accepted` and `continue-on-error: true`.
</verification>

<success_criteria>
- DEPLOY-04: the `--apply-accepted` step is wired in ingest.yml (gated on success) and the accepted→applied transition is proven by passing regression tests.
- PIPELINE-01: the Garmin sync exit-1-on-failure path is confirmed present; live-log confirmation handed off to plan 03-04 / Phase 4.
- D-06 honored: `if: always()` removed; D-05 honored: `continue-on-error: true` retained.
</success_criteria>

## Artifacts this phase produces

- `.github/workflows/ingest.yml` (MODIFIED) — "Apply accepted modulations (audit K1)" step with `if: always()` removed (now defaults to `if: success()`), `continue-on-error: true` retained, `run: python -m coach.coaching.modulation --apply-accepted` retained.
- No new symbols; `apply_accepted_modulations()` and the `--apply-accepted` CLI flag in `coach/coaching/modulation.py` already exist and are verified, not created.

<output>
Create `.planning/phases/03-deploy-pipeline-resilience/03-02-SUMMARY.md` when done
</output>
