---
phase: 03-deploy-pipeline-resilience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_migrations.py
autonomous: false
requirements: [DEPLOY-01, DEPLOY-02]
must_haves:
  truths:
    - "Running scripts/verify_migrations.py exits 0 when all audit constraints are live in Supabase"
    - "Running scripts/verify_migrations.py exits 1 and names the missing constraint when any is absent"
    - "Every pending migration listed in docs/OPEN_ISSUES.md is executed in Supabase"
    - "The resilience-audit.sql constraints (UNIQUE, FK ON DELETE, CHECK, expires_at column) are present in information_schema"
  artifacts:
    - path: "scripts/verify_migrations.py"
      provides: "information_schema constraint verifier with PASS/FAIL output and exit code contract"
      contains: "EXPECTED_UNIQUE_CONSTRAINTS"
      min_lines: 60
  key_links:
    - from: "scripts/verify_migrations.py"
      to: "information_schema.table_constraints"
      via: "sb.schema('information_schema').table(...) with sb.rpc fallback"
      pattern: "information_schema"
    - from: "scripts/verify_migrations.py"
      to: "coach.utils.supabase_client.get_supabase"
      via: "import + get_supabase()"
      pattern: "from coach.utils.supabase_client import get_supabase"
---

## Phase Goal

**As** Nicolò, **I want** every pending SQL migration to be live in Supabase and machine-verifiable, **so that** the schema constraints (UNIQUE, FK ON DELETE, CHECK, expires_at) that the bot and pipeline depend on are guaranteed present before any code is deployed.

> Note for planner/checker: the ROADMAP `**Goal:**` line for Phase 3 is task-shaped, not a user story. This is a deploy + verification phase with no user-facing UI. The user-story framing above is adapted to the deploy domain (the "capability" is trustworthy schema state). Run `/gsd mvp-phase 3` if a canonical ROADMAP user-story goal line is required.

<objective>
Create `scripts/verify_migrations.py` — a deterministic verifier that queries `information_schema` and reports PASS/FAIL per expected constraint — then execute all pending migrations in Supabase via the SQL Editor (D-02) and prove they landed using the new script. This is the migrations vertical slice: a single deploy+verify unit covering DEPLOY-01 (all pending migrations in `docs/OPEN_ISSUES.md`) and DEPLOY-02 (the `2026-06-01-resilience-audit.sql` content live).

Purpose: Migrations are the hard prerequisite (D-01) for the bot deploy in plan 03-04 — fix K3 inserts `kind='pattern_correction'` which the DB rejects until the `subjective_log.kind` CHECK is widened. The verifier turns "did the migration run?" from a manual guess into an exit-code gate.

Output: `scripts/verify_migrations.py` (new file) + all migrations live in Supabase confirmed by `python scripts/verify_migrations.py` exiting 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deploy-pipeline-resilience/03-CONTEXT.md
@.planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md
@.planning/phases/03-deploy-pipeline-resilience/03-PATTERNS.md
@docs/OPEN_ISSUES.md
@migrations/2026-06-01-resilience-audit.sql
@scripts/db_cleanup.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create scripts/verify_migrations.py information_schema verifier</name>
  <files>scripts/verify_migrations.py</files>
  <read_first>
    - scripts/db_cleanup.py (analog for imports, logging.basicConfig format, get_supabase usage, sys.exit(1) on failure — PATTERNS.md §verify_migrations.py)
    - migrations/2026-06-01-resilience-audit.sql (authoritative list of constraint names to verify)
    - docs/OPEN_ISSUES.md §"Pending migrations" table (the full list of migration files that must be live)
    - .planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md §"scripts/verify_migrations.py — New File" and §"Pitfall 4" (information_schema access method + fallback)
  </read_first>
  <behavior>
    - When all expected constraints/columns exist in information_schema, main() logs "PASS: <name>" for each and exits 0.
    - When any expected constraint/column is absent, main() logs "FAIL: <name>" for the missing one(s), logs a summary "N constraint(s) missing", and calls sys.exit(1).
    - When sb.schema("information_schema").table(...) raises (PostgREST does not expose information_schema), the script falls back to sb.rpc("get_public_constraints") / sb.rpc("get_public_columns") without crashing.
  </behavior>
  <action>
    Create `scripts/verify_migrations.py` following the `scripts/db_cleanup.py` analog exactly: module docstring (Italian, mirroring dr_snapshot.py style), `import logging`, `import sys`, `from coach.utils.supabase_client import get_supabase`, `logger = logging.getLogger(__name__)`, and `logging.basicConfig(level=logging.INFO, format="%(asctime)s %(levelname)s %(message)s")` inside `main()`.

    Define four module-level constants with the EXACT constraint names from `migrations/2026-06-01-resilience-audit.sql`:
    - `EXPECTED_UNIQUE_CONSTRAINTS = [("races","races_name_date_unique"), ("physiology_zones","physiology_zones_disc_validfrom_method_unique"), ("planned_sessions","unique_planned_date_sport_type")]`
    - `EXPECTED_FK_CONSTRAINTS = [("mesocycles","mesocycles_target_race_fk"), ("planned_sessions","planned_sessions_completed_activity_id_fkey"), ("physiology_zones","physiology_zones_test_activity_id_fkey")]`
    - `EXPECTED_CHECK_CONSTRAINTS = [("plan_modulations","plan_modulations_status_check"), ("subjective_log","subjective_log_kind_check")]`
    - `EXPECTED_COLUMNS = [("plan_modulations","expires_at")]`

    Implement constraint lookup with the primary approach `sb.schema("information_schema").table("table_constraints").select("constraint_name,table_name,constraint_type").eq("table_schema","public").execute()`, collecting the set of `constraint_name` values. Wrap in `try/except Exception` and fall back to `sb.rpc("get_public_constraints").execute()` (this approach is marked [ASSUMED] in RESEARCH.md A1/A4 — the fallback is mandatory, not optional). Implement column existence the same way against `information_schema.columns` filtered by `table_schema='public'`, `table_name`, `column_name`, with an `sb.rpc("get_public_columns")` fallback.

    Build a `results` list of `(label, ok)` tuples across all four expected lists. Iterate results: on `not ok` append the label to `failures` and `logger.error("FAIL: %s", label)`, else `logger.info("PASS: %s", label)`. After the loop: if `failures`, `logger.error("%d constraint(s) missing — migrations may not be applied", len(failures))` then `sys.exit(1)`; else `logger.info("All %d constraints verified OK", len(results))`. Guard the whole body with the db_cleanup.py `except Exception: logger.exception(...); sys.exit(1)` pattern (L2 style). End with `if __name__ == "__main__": main()`.

    Do NOT hand-roll raw psql connections or custom Postgres drivers — use only the existing `supabase-py` client via `get_supabase()` (Don't Hand-Roll table, RESEARCH.md).
  </action>
  <verify>
    <automated>python -c "import ast,sys; ast.parse(open('scripts/verify_migrations.py',encoding='utf-8').read()); print('syntax-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_migrations.py` exists and parses without SyntaxError (the python -c ast.parse command prints "syntax-ok").
    - Source contains the literal token `EXPECTED_UNIQUE_CONSTRAINTS`, `EXPECTED_FK_CONSTRAINTS`, `EXPECTED_CHECK_CONSTRAINTS`, and `EXPECTED_COLUMNS`.
    - Source contains the literal constraint name `races_name_date_unique`, `physiology_zones_disc_validfrom_method_unique`, `unique_planned_date_sport_type`, `mesocycles_target_race_fk`, `plan_modulations_status_check`, and `subjective_log_kind_check`.
    - Source contains both `information_schema` (primary path) and `sb.rpc(` (fallback path).
    - Source contains `from coach.utils.supabase_client import get_supabase` and a `sys.exit(1)` call.
    - Source contains `if __name__ == "__main__":` and a `main()` definition.
  </acceptance_criteria>
  <done>verify_migrations.py exists, parses, declares the four EXPECTED_* constants with the exact constraint names from the migration file, implements primary information_schema lookup with an sb.rpc fallback, and exits 1 on any missing constraint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute all pending migrations in Supabase SQL Editor (D-02)</name>
  <read_first>
    - docs/OPEN_ISSUES.md §"Pending migrations — run in Supabase SQL editor" (the full table of 11 migration files)
    - migrations/2026-06-01-resilience-audit.sql (note: idempotent — safe to re-run; execution order WITHIN the file is fixed, do not reorder)
  </read_first>
  <what-built>
    The verifier script (Task 1) is ready. All migration files already exist on disk and are idempotent. D-02 mandates manual execution via the Supabase SQL Editor (web dashboard) — Claude has no Supabase SQL Editor access and cannot run these. This is a genuine human-only action.
  </what-built>
  <how-to-verify>
    1. Open the Supabase project dashboard → SQL Editor.
    2. Execute each pending migration from `docs/OPEN_ISSUES.md` §"Pending migrations" table that is not yet applied, in this order (skip any already-applied; all are idempotent / use IF NOT EXISTS / ON CONFLICT, so re-running is safe):
       - `migrations/2026-05-11-seed-lavarone-race.sql` (only if `races` is empty)
       - `migrations/2026-05-12-mesocycles-unique.sql`
       - `migrations/2026-05-13-subjective-log-kinds.sql`
       - `migrations/2026-05-13-fix-lavarone-date.sql` (only if Lavarone was seeded with the wrong date)
       - `migrations/2026-05-14-subjective-log-severity.sql`
       - `migrations/2026-05-14-sent-reminders.sql`
       - `migrations/2026-05-14-predictions-outcomes.sql`
       - `migrations/2026-05-14-season-year.sql`
       - `migrations/2026-05-14-hypothesis-and-audit.sql`
       - `migrations/2026-05-14-cognitive-mvp.sql`
       - `migrations/2026-06-01-resilience-audit.sql` (run LAST — it depends on `races`, `mesocycles`, `physiology_zones`, `planned_sessions`, `plan_modulations`, `subjective_log` already existing; do NOT reorder statements inside this file — the races DEDUP UPDATE+DELETE must precede the UNIQUE constraint, and the mesocycles FK update must precede the FK add)
    3. Confirm no error is raised on the final `2026-06-01-resilience-audit.sql` run. The DO $$ EXCEPTION blocks swallow duplicate_object — a clean re-run is expected to produce no error.
    4. Run `SELECT race_date FROM races WHERE name = 'Lavarone Cross Sprint';` — note the value (BUG-006 expects 2026-08-29; CLAUDE.md §2 currently says 2026-09-06; surface the discrepancy to the athlete but do NOT change it here — out of scope).
  </how-to-verify>
  <verify>
    <human-check>All 11 pending migrations executed (or confirmed already-applied) in Supabase SQL Editor with no error on the final resilience-audit.sql run.</human-check>
  </verify>
  <acceptance_criteria>
    - Every migration file in the `docs/OPEN_ISSUES.md` pending table has been executed or confirmed already-applied in Supabase.
    - The final `2026-06-01-resilience-audit.sql` execution completed without an unhandled error.
  </acceptance_criteria>
  <resume-signal>Reply "done" once all pending migrations have been executed (or confirmed already-applied) in the Supabase SQL Editor, or paste any SQL error for triage.</resume-signal>
  <done>All pending migrations from docs/OPEN_ISSUES.md are live in Supabase; the resilience-audit.sql ran without error.</done>
</task>

<task type="auto">
  <name>Task 3: Run verify_migrations.py against live Supabase and confirm PASS</name>
  <files>scripts/verify_migrations.py</files>
  <read_first>
    - scripts/verify_migrations.py (the script created in Task 1)
    - .planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md §"Pitfall 4" (if the script reports 404 / empty, create the SECURITY DEFINER RPC fallback in Supabase)
  </read_first>
  <action>
    Run `python scripts/verify_migrations.py` with the project environment (`PYTHONPATH=.`, `SUPABASE_URL` and `SUPABASE_SERVICE_KEY` loaded from `.env` via python-dotenv as the existing supabase_client does). Confirm exit code 0 and a "PASS:" line for every expected constraint and the `plan_modulations.expires_at` column.

    If the script fails because `information_schema` is not exposed via PostgREST (Pitfall 4 — 404 or empty results despite constraints existing), create the two SECURITY DEFINER fallback RPCs in the Supabase SQL Editor exactly as named in the script (`public.get_public_constraints()` returning `constraint_name,table_name,constraint_type` from `information_schema.table_constraints` WHERE `table_schema='public'`, and `public.get_public_columns()` returning `table_name,column_name` from `information_schema.columns` WHERE `table_schema='public'`), then re-run. Do NOT weaken the verifier to pass on missing constraints — a real FAIL means a migration did not land and must be re-run (back to Task 2).
  </action>
  <verify>
    <automated>python scripts/verify_migrations.py</automated>
  </verify>
  <acceptance_criteria>
    - `python scripts/verify_migrations.py` exits 0.
    - stdout/stderr contains a "PASS:" line for `races_name_date_unique`, `physiology_zones_disc_validfrom_method_unique`, `unique_planned_date_sport_type`, `mesocycles_target_race_fk`, `plan_modulations_status_check`, `subjective_log_kind_check`, and `plan_modulations.expires_at`.
    - stdout/stderr contains "All N constraints verified OK" and contains zero "FAIL:" lines.
  </acceptance_criteria>
  <done>verify_migrations.py exits 0 against live Supabase with PASS for every expected constraint and the expires_at column — DEPLOY-01 and DEPLOY-02 schema state confirmed live.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python script → Supabase PostgREST | Service-role key crosses here to read information_schema; read-only, no writes from this script |
| Telegram bot / Python writers → DB columns | Untrusted-ish status/kind string values cross into DB rows; constraints verified here are the enforcement point |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | plan_modulations.status / subjective_log.kind | mitigate | O9 + K3 CHECK constraints reject corrupt status/kind values at the DB level; this plan VERIFIES those CHECKs are live via verify_migrations.py (`plan_modulations_status_check`, `subjective_log_kind_check`) — a missing CHECK is a FAIL that blocks the phase (ref RESEARCH.md §Security Domain "DB state corruption via typo status") |
| T-03-02 | Tampering | races / mesocycles / planned_sessions rows | mitigate | UNIQUE + FK ON DELETE SET NULL constraints prevent duplicate races and orphan FKs; verifier asserts `races_name_date_unique`, `mesocycles_target_race_fk`, `unique_planned_date_sport_type` are present |
| T-03-03 | Information Disclosure | SUPABASE_SERVICE_KEY in verify script | accept | Script runs only in trusted local/CI env with the same key already used by every other coach/ script; no new exposure surface; read-only information_schema query |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan (RESEARCH.md §Package Legitimacy Audit: "No new external packages") — supabase-py already pinned in requirements.txt; no install task, no slopcheck gate needed |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('scripts/verify_migrations.py',encoding='utf-8').read())"` parses clean.
- `python scripts/verify_migrations.py` exits 0 with all-PASS after migrations are live.
- `pytest tests/test_audit_resilience.py::test_o4_o6_migration_present tests/test_audit_resilience.py::test_o7_e4_o8_o9_migration_present -x -q` passes (confirms the migration FILE content is intact — complements the live DB check).
</verification>

<success_criteria>
- DEPLOY-01: every pending migration in docs/OPEN_ISSUES.md is executed in Supabase and verify_migrations.py confirms the resulting constraints.
- DEPLOY-02: 2026-06-01-resilience-audit.sql content (UNIQUE, FK ON DELETE, CHECK, expires_at) is live and verified.
- verify_migrations.py exits 1 (not 0) if any expected constraint is missing — proven by the FAIL/exit-1 branch existing and the acceptance assertion that zero FAIL lines appear on the live run.
</success_criteria>

## Artifacts this phase produces

- `scripts/verify_migrations.py` (NEW file) — module-level constants `EXPECTED_UNIQUE_CONSTRAINTS`, `EXPECTED_FK_CONSTRAINTS`, `EXPECTED_CHECK_CONSTRAINTS`, `EXPECTED_COLUMNS`; function `main()`; `if __name__ == "__main__"` entry point.
- (Supabase, conditional) SQL functions `public.get_public_constraints()` and `public.get_public_columns()` — SECURITY DEFINER fallback RPCs, created only if PostgREST does not expose `information_schema` (Pitfall 4).
- Live Supabase schema state: constraints `races_name_date_unique`, `physiology_zones_disc_validfrom_method_unique`, `unique_planned_date_sport_type`, `mesocycles_target_race_fk`, `planned_sessions_completed_activity_id_fkey`, `physiology_zones_test_activity_id_fkey`, `plan_modulations_status_check`, `subjective_log_kind_check`; column `plan_modulations.expires_at`.

<output>
Create `.planning/phases/03-deploy-pipeline-resilience/03-01-SUMMARY.md` when done
</output>
