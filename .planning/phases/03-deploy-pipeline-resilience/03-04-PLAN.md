---
phase: 03-deploy-pipeline-resilience
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: []
autonomous: false
requirements: [DEPLOY-03]
must_haves:
  truths:
    - "npx tsc --noEmit passes in workers/telegram-bot/ before deploy (no type errors in the K2-K5 source)"
    - "wrangler deploy publishes the telegram-bot Worker with K2/K3/K4/K5 fixes live"
    - "A manual Telegram chat test confirms an accept callback succeeds (K5 resp.ok guard) and the bot does not 500 on a malformed body (K4)"
  artifacts:
    - path: "workers/telegram-bot/src/index.ts"
      provides: "deployed worker source carrying K2 (pending_confirmations.status), K3 (pattern_correction kind), K4 (req.json guard), K5 (PATCH resp.ok check)"
      contains: "Bug fix audit K5"
  key_links:
    - from: "workers/telegram-bot/src/index.ts"
      to: "subjective_log.kind CHECK ('pattern_correction')"
      via: "K3 insert depends on the migration from plan 03-01 being live first (D-01)"
      pattern: "pattern_correction"
    - from: "wrangler deploy"
      to: "live telegram-bot Worker"
      via: "npm run deploy from workers/telegram-bot/"
      pattern: "wrangler deploy"
---

## Phase Goal

**As** Nicolò, **I want** the Telegram bot redeployed with the audit K2-K5 fixes live, **so that** accepting a modulation reliably writes to the DB, a malformed update never 500s, and pattern corrections are accepted by the widened kind constraint.

> Note: deploy/verification phase. The deployable artifact (index.ts with K2-K5) already exists committed; this plan's "capability" is making those fixes live. See plan 03-01 for the ROADMAP goal-line discrepancy note.

<objective>
Redeploy the Telegram bot Worker via `wrangler deploy` (D-03 — wrangler is configured locally), gated behind a `npx tsc --noEmit` type check (RESEARCH.md Pitfall 3), then manually confirm via Telegram chat that K4 (malformed body → 200 not 500) and K5 (PATCH resp.ok guard on accept) behave live (D-08). This is the bot-deploy vertical slice: the single deploy+verify unit for DEPLOY-03.

Purpose: K2-K5 fixes are committed in `index.ts` but a Cloudflare Worker only runs deployed code (PROJECT constraint: "il codice committato non è live finché non viene deployato"). K3 inserts `kind='pattern_correction'`, which the DB rejects until plan 03-01's migration widens the `subjective_log.kind` CHECK — hence this plan depends on 03-01 (D-01: migrations before deploy).

Output: telegram-bot Worker redeployed with K2-K5 live; manual Telegram verification recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-deploy-pipeline-resilience/03-CONTEXT.md
@.planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md
@.planning/phases/03-deploy-pipeline-resilience/03-01-SUMMARY.md
@workers/telegram-bot/src/index.ts
@workers/telegram-bot/package.json
@workers/telegram-bot/wrangler.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript type-check the telegram-bot worker before deploy</name>
  <files>workers/telegram-bot/src/index.ts</files>
  <read_first>
    - workers/telegram-bot/tsconfig.json (compiler options; confirm strict mode so tsc catches K2-K5 type issues)
    - workers/telegram-bot/package.json (scripts: "deploy": "wrangler deploy"; devDependency typescript ^5.4.0)
    - .planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md §"Pitfall 3" (wrangler deploy uses esbuild, NOT full tsc — type errors surface at runtime unless tsc is run first)
  </read_first>
  <action>
    From `workers/telegram-bot/`, install dev dependencies if not present (`npm install`) and run `npx tsc --noEmit` to type-check `src/index.ts` (which carries the K2/K3/K4/K5 fixes) without emitting JS. This catches any type error before it becomes a Worker 500 at runtime (RESEARCH.md Pitfall 3). If `tsc` reports errors, fix only genuine type errors in `index.ts` — do NOT alter the K2-K5 fix logic (the audit comments `Bug fix audit K2/K3/K4/K5` mark the intended behavior; preserve it). If `tsc` is clean, proceed.

    Do NOT run `wrangler deploy` in this task — deploy is the next task, gated behind a green tsc.
  </action>
  <verify>
    <automated>cd workers/telegram-bot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` run from `workers/telegram-bot/` exits 0 (no type errors).
    - `workers/telegram-bot/src/index.ts` still contains the literal audit markers `Bug fix audit K4`, `Bug fix audit K5`, `Bug fix audit K2`, and the `pattern_correction` kind (K3) — i.e., the K2-K5 logic was not removed by any type fix.
  </acceptance_criteria>
  <done>npx tsc --noEmit passes for the telegram-bot worker and the K2-K5 audit markers remain intact in index.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the telegram-bot Worker with wrangler (D-03)</name>
  <files>workers/telegram-bot/src/index.ts</files>
  <read_first>
    - workers/telegram-bot/wrangler.toml (KV namespace binding already configured — id 9d1822af8eff446e832c004178d578ad; no toml changes needed per RESEARCH.md §DEPLOY-03)
    - .planning/phases/03-deploy-pipeline-resilience/03-01-SUMMARY.md (confirm migrations are LIVE — this deploy must not precede the subjective_log.kind CHECK widening; D-01)
    - .planning/phases/03-deploy-pipeline-resilience/03-RESEARCH.md §"DEPLOY-03: Wrangler Deploy" (secrets already set in prior phases — do NOT re-set secrets unless changed)
  </read_first>
  <action>
    Confirm plan 03-01 completed (migrations live — DEPLOY-01/02 verified) before deploying; if 03-01's SUMMARY does not confirm the `subjective_log_kind_check` is live, STOP and resolve 03-01 first (D-01 ordering — deploying K3 before the CHECK is widened reproduces the original insert-rejection bug, RESEARCH.md Pitfall 1).

    From `workers/telegram-bot/`, run `npm run deploy` (equivalently `wrangler deploy`). This bundles `src/index.ts` (with K2-K5) via esbuild and publishes the Worker. Do NOT re-run `wrangler secret put` for any secret — secrets (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_ALLOWED_CHAT_ID`, `SUPABASE_URL`, `SUPABASE_SERVICE_KEY`) are already set from prior phases and unchanged. Capture the wrangler deploy output (version id / deployed URL) for the SUMMARY.

    If `wrangler deploy` fails with an authentication error, this is an auth gate: surface it (the executor will create a checkpoint for the athlete to authenticate `wrangler login` / set `CLOUDFLARE_API_TOKEN`), then retry. D-03 states wrangler is configured locally, so this should succeed without extra setup.
  </action>
  <verify>
    <automated>cd workers/telegram-bot && wrangler deployments list 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `wrangler deploy` (via `npm run deploy`) completed successfully and printed a deployed Worker URL / version id.
    - `wrangler deployments list` shows a deployment timestamped at or after this task's execution (proves the redeploy landed).
    - No secret was re-set during this task (secrets unchanged from prior phases).
    - The deploy was performed only after 03-01 confirmed migrations live (D-01 ordering honored).
  </acceptance_criteria>
  <done>The telegram-bot Worker is redeployed via wrangler with K2-K5 live, after migrations were confirmed live, with no secret changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually verify K4 and K5 live behavior via Telegram (D-08)</name>
  <read_first>
    - workers/telegram-bot/src/index.ts lines 112-117 (K4: req.json() in try/catch — malformed body returns early, no 500) and lines 803, 1215-1219 (K5: PATCH resp.ok check before confirming to the user)
    - .planning/phases/03-deploy-pipeline-resilience/03-CONTEXT.md (D-08: bot verification is a manual Telegram chat test, not an automated script; Phase 4 does full behavioral verification)
  </read_first>
  <what-built>
    Task 2 deployed the bot with K2-K5 live. K4 wraps `req.json()` in try/catch so a malformed/empty webhook body returns 200 instead of a 500 storm. K5 checks `resp.ok` on the PATCH that flips a modulation to accepted, so a failed DB write surfaces to the user instead of a false "done". K2 fixes pending_confirmations.status routing; K3 lets the bot insert kind='pattern_correction' (now accepted by the widened CHECK from plan 03-01).
  </what-built>
  <how-to-verify>
    1. In Telegram, send any normal command to the bot (e.g. `/status` or `/budget`) and confirm it replies — proves the redeploy is live and healthy.
    2. If a `plan_modulations` proposal with inline ✅/❌ buttons is available (or trigger one per Phase-4 setup), tap ✅ and confirm the bot reports success and the row moves toward accepted/applied — proves K5's resp.ok guard does not falsely confirm. If no proposal is currently available, note this and defer the ✅-tap check to Phase 4 (VERIFY-05), recording that K5 is confirmed present in source.
    3. Confirm no error message / no missing reply occurred for a normal interaction (K4: the bot did not 500). Optionally, if comfortable, observe `wrangler tail` (from workers/telegram-bot/) during the interactions to confirm no unhandled 500 is logged.
    4. Note in your reply whether the `subjective_log` accepts a `pattern_correction` insert without a 23514 CHECK error (K3 + migration), if such an interaction is exercised.
  </how-to-verify>
  <verify>
    <human-check>Bot replies to a normal command after redeploy; no 500 on normal interaction (K4); an accept tap (if available) confirms via K5 without a false success — otherwise the ✅-tap check is explicitly deferred to Phase 4 VERIFY-05.</human-check>
  </verify>
  <acceptance_criteria>
    - The deployed bot replies to at least one normal Telegram command (redeploy is live).
    - No 500 / missing reply observed for a normal interaction (K4 behavior live).
    - Either an accept-tap succeeded with a truthful confirmation (K5 live), OR the athlete explicitly defers the accept-tap to Phase 4 VERIFY-05 with K5 confirmed present in source.
  </acceptance_criteria>
  <resume-signal>Reply "verified" once the bot responds in Telegram and no 500 occurred (note whether the accept-tap was tested or deferred to Phase 4), or describe any failure.</resume-signal>
  <done>Manual Telegram test confirms the redeployed bot is live and healthy (K4 no-500); K5 accept-tap is either verified live or explicitly deferred to Phase 4 with source confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → telegram-bot Worker webhook | Arbitrary POST bodies (possibly malformed) cross into the Worker; the entry point must not 500 |
| Telegram callback tap → plan_modulations / pending_confirmations PATCH | Athlete acceptance crosses into a DB state change that must be confirmed only if the write succeeded |
| Worker → Supabase (subjective_log insert) | kind='pattern_correction' crosses into a CHECK-constrained column |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | malformed JSON body → 500 storm | mitigate | K4 wraps `req.json()` in try/catch and returns early (200), preventing a 500 storm (RESEARCH.md §Security Domain "Malformed JSON body → 500 storm"); verified live in Task 3 |
| T-03-11 | Spoofing | Telegram webhook replay / unauthorized chat | mitigate | Existing `TELEGRAM_ALLOWED_CHAT_ID` allowlist + KV dedup on update_id remain active in the deployed worker (RESEARCH.md §Security Domain "Telegram webhook replay"); not regressed by this deploy |
| T-03-12 | Tampering | false "accepted" confirmation on a failed PATCH | mitigate | K5 checks `resp.ok` before confirming to the user, so a rejected/failed DB write is not reported as success; verified via accept-tap (Task 3) or deferred to Phase 4 |
| T-03-13 | Tampering | subjective_log insert with kind='pattern_correction' rejected (23514) | mitigate | K3 + the migration from plan 03-01 (subjective_log_kind_check widened) — DEPENDENCY: deploy must follow migration (D-01) or the insert is rejected (RESEARCH.md Pitfall 1) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | The only install is `npm install` of already-pinned devDependencies in workers/telegram-bot/package.json (typescript, wrangler, @cloudflare/workers-types — all in the committed lockfile-adjacent package.json); RESEARCH.md §Package Legitimacy Audit reports no new external packages; no slopcheck gate required |
</threat_model>

<verification>
- `cd workers/telegram-bot && npx tsc --noEmit` exits 0.
- `wrangler deploy` succeeds; `wrangler deployments list` shows a fresh deployment.
- Manual Telegram check: bot replies, no 500 (K4); accept-tap verified or deferred to Phase 4 (K5).
- DEPENDENCY GUARD: 03-01 SUMMARY confirms subjective_log_kind_check is live before this deploy (D-01).
</verification>

<success_criteria>
- DEPLOY-03: telegram-bot Worker redeployed via wrangler with K2/K3/K4/K5 live, after migrations live (D-01), type-checked (Pitfall 3), and manually verified on Telegram (D-08).
</success_criteria>

## Artifacts this phase produces

- No new source files. This plan deploys the already-committed `workers/telegram-bot/src/index.ts` (carrying K2/K3/K4/K5) to the live Cloudflare Worker.
- Live artifact: a new `telegram-bot` Worker deployment (version id captured in SUMMARY).

<output>
Create `.planning/phases/03-deploy-pipeline-resilience/03-04-SUMMARY.md` when done
</output>
