---
phase: 01-test-suite-analytics-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify_analytics.py
autonomous: false
requirements:
  - ANALYTICS-01
  - ANALYTICS-03
  - ANALYTICS-04
  - ANALYTICS-05
user_setup:
  - service: supabase
    why: "verify_analytics.py legge la Supabase di produzione per ispezione live (D-01)"
    env_vars:
      - name: SUPABASE_URL
        source: ".env locale (Supabase Dashboard -> Project Settings -> API)"
      - name: SUPABASE_SERVICE_KEY
        source: ".env locale (Supabase Dashboard -> Project Settings -> API -> service_role key)"

must_haves:
  truths:
    - "L'operatore esegue `python scripts/verify_analytics.py` e vede 4 sezioni: HRV, PMC, Readiness, Risk — ANALYTICS-01/03/04/05 (D-03)"
    - "La sezione HRV ricalcola la baseline 28d escludendo oggi PER DATA e mostra z-score + flag — ANALYTICS-01"
    - "La sezione PMC mostra CTL/ATL/TSB e stampa None (non 0) quando i dati mancano — ANALYTICS-03"
    - "La sezione Readiness mostra score 0-100 e label leggibile — ANALYTICS-04"
    - "La sezione Risk mostra il volume per disciplina con date Europe/Rome — ANALYTICS-05"
  artifacts:
    - path: "scripts/verify_analytics.py"
      provides: "Script operativo live a 4 sezioni per ispezione visiva dei valori analytics reali"
      contains: "def main"
      min_lines: 60
  key_links:
    - from: "scripts/verify_analytics.py"
      to: "coach/utils/supabase_client.py::get_supabase"
      via: "import dopo load_dotenv()"
      pattern: "get_supabase\\(\\)"
    - from: "scripts/verify_analytics.py"
      to: "coach/utils/dt.py::to_rome_date"
      via: "bucketing volume per data Rome (fix B4)"
      pattern: "to_rome_date\\("
    - from: "scripts/verify_analytics.py"
      to: "coach/analytics/readiness.py::hrv_z_score"
      via: "ricalcolo z-score per verifica baseline B1"
      pattern: "hrv_z_score\\("
---

<objective>
Creare `scripts/verify_analytics.py` (D-01): script operativo committato che query la Supabase di produzione e stampa 4 sezioni leggibili — HRV baseline+z-score, PMC, Readiness, Risk volume bucketing — per ispezione visiva dei fix B1/B3/B4 su dati reali.

Purpose: ANALYTICS-01/03/05 richiedono verifica su DATI REALI (non solo unit test). I fix B1 (baseline HRV per data), B3 (PMC None su cold-start) e B4 (risk bucketing data Rome) sono testati in unit, ma D-01/D-08 richiedono di osservare i valori effettivi nel DB di Nicolò. Questo script è l'unico strumento che fornisce quella visibilità live.

Output: `scripts/verify_analytics.py` — informativo (D-02: nessun exit 1 automatico), eseguibile con `python scripts/verify_analytics.py` dato `.env` locale.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-suite-analytics-correctness/01-CONTEXT.md
@.planning/phases/01-test-suite-analytics-correctness/01-RESEARCH.md
@.planning/phases/01-test-suite-analytics-correctness/01-PATTERNS.md
</context>

## Phase Goal

**As an** operatore del sistema (Nicolò/dev), **I want to** un singolo comando che stampa i valori analytics reali dal DB (HRV, PMC, readiness, risk), **so that** posso ispezionare a vista che i fix di audit (B1/B3/B4) si comportano correttamente sui miei dati di produzione, non solo nei test sintetici.

## Artifacts this phase produces

Symbols/files created by this plan:
- `scripts/verify_analytics.py` (NEW file) — con almeno: `main()` e funzioni helper di sezione (es. `_verify_hrv`, `_fetch_pmc`/`_fetch_today_metrics`, `_fetch_readiness`, `_fetch_risk_volumes`). I nomi helper sono a discrezione dell'esecutore purché lo script produca le 4 sezioni; `main` è obbligatorio.

Simboli consumati (già esistenti, NON creati qui): `get_supabase` (coach/utils/supabase_client.py), `today_rome`, `to_rome_date` (coach/utils/dt.py), `hrv_z_score` (coach/analytics/readiness.py).

<tasks>

<task type="auto">
  <name>Task 1: Creare scripts/verify_analytics.py con sezioni HRV e PMC</name>
  <files>scripts/verify_analytics.py</files>
  <read_first>
    - scripts/smoke_test.py — pattern di script operativo: docstring, `from __future__ import annotations`, imports, `load_dotenv()` PRIMA degli import `coach.*`, struttura `main()` + `if __name__ == "__main__"`
    - scripts/backfill_metrics.py — pattern try/except per-sezione (righe ~30-33) così che un errore in una sezione non interrompa le altre
    - coach/utils/dt.py — firme `today_rome() -> date` (riga 13) e `to_rome_date(ts) -> Optional[date]` (riga 18)
    - coach/analytics/readiness.py — firma `hrv_z_score(hrv_today: float, history: list[float]) -> Optional[float]` (riga 69) e la soglia minima di 7 giorni di storico
    - .planning/phases/01-test-suite-analytics-correctness/01-RESEARCH.md — Code Examples §1 (fetch daily_metrics) e §2 (ricalcolo HRV baseline B1); Common Pitfalls §2 (PMC None corretto) e §3 (baseline < 7 giorni)
    - .planning/phases/01-test-suite-analytics-correctness/01-PATTERNS.md — sezione `scripts/verify_analytics.py` (imports, ordering constraint, output format esatto)
  </read_first>
  <action>
    Creare il nuovo file `scripts/verify_analytics.py`. Header esatto: docstring di una riga che descrive lo scopo, poi `from __future__ import annotations`, import stdlib (`logging`, `statistics`, `from datetime import date, timedelta`), poi `from dotenv import load_dotenv` e `load_dotenv()` a livello di modulo PRIMA di qualsiasi import `coach.*` (vincolo lru_cache — RESEARCH.md Anti-Patterns / Pitfall 1). Dopo `load_dotenv()` importare: `from coach.utils.supabase_client import get_supabase`, `from coach.utils.dt import today_rome, to_rome_date`, `from coach.analytics.readiness import hrv_z_score`. Definire `logger = logging.getLogger(__name__)`.

    Implementare la sezione HRV (verifica ANALYTICS-01 / fix B1): query `daily_wellness` selezionando `date,hrv_rmssd` da (oggi - 28 giorni). Costruire `hist_rows = [r for r in rows if r["date"] != today_iso]` — esclusione PER DATA, NON per valore (questo È la verifica B1). Estrarre `today_row` separatamente. Se `len(hrv_history) >= 7` e `today_row` ha hrv: calcolare `mean = statistics.fmean(hrv_history)`, `sd = statistics.pstdev(hrv_history)`, `z = hrv_z_score(today_row["hrv_rmssd"], hrv_history)`, derivare il flag dalle soglie CLAUDE.md §5.1 (z < -2.0 → critical, z < -1.0 → warning, altrimenti ok). Stampare la sezione `=== HRV Analytics ===` nel formato esatto di PATTERNS.md (riga "Baseline 28d: media=...ms, SD=...ms (N giorni, oggi escluso)", "Z-score oggi: ...σ → ...", "Flag: ..."). Gestire `len(hrv_history) < 7` con messaggio "Dati storici insufficienti (< 7 giorni HRV)" (Pitfall 3).

    Implementare la sezione PMC (verifica ANALYTICS-03 / fix B3): leggere da `daily_metrics` la riga di `today_rome().isoformat()` selezionando `ctl,atl,tsb`. Stampare `=== PMC ===` con `CTL: {ctl} | ATL: {atl} | TSB: {tsb}`. Se i valori sono None, stamparli ESPLICITAMENTE come `None` con la nota "PMC non disponibile (test FTP/soglia non ancora eseguiti — vedi Phase 2)" (Pitfall 2). NON sostituire None con 0 — un eventuale `CTL: 0.00` (non None) sarebbe il bug B3 da segnalare.

    Avvolgere ogni sezione in try/except (pattern backfill_metrics.py) che stampa il messaggio di errore della singola sezione senza interrompere le sezioni successive. Vincolo di sicurezza (T-02-01): NON loggare né stampare mai `SUPABASE_URL` né `SUPABASE_SERVICE_KEY` — solo valori analytics in stdout.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/verify_analytics.py', encoding='utf-8').read()); print('syntax ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `scripts/verify_analytics.py` esiste e contiene `load_dotenv()` su una riga che precede testualmente ogni `from coach.` o `import coach`.
    - Source assertion: il file contiene la stringa esatta `=== HRV Analytics ===` e `=== PMC ===`.
    - Source assertion: il file contiene `r["date"] != today_iso` (esclusione baseline per data — verifica B1) e NON contiene un confronto di esclusione basato sul valore hrv.
    - Source assertion: il file NON contiene le stringhe `SUPABASE_URL` né `SUPABASE_SERVICE_KEY` (nessuna stampa di segreti).
    - Source assertion: il file NON contiene `sys.exit(1)` né `exit(1)` (D-02: informativo, nessun gate).
    - Test command: `python -c "import ast; ast.parse(open('scripts/verify_analytics.py', encoding='utf-8').read())"` esce 0 (sintassi valida).
  </acceptance_criteria>
  <done>Il file esiste, è sintatticamente valido, ha sezioni HRV e PMC con l'ordering load_dotenv corretto e nessun segreto stampato.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiungere sezioni Readiness e Risk + wiring main()</name>
  <files>scripts/verify_analytics.py</files>
  <read_first>
    - scripts/verify_analytics.py — lo stato corrente dopo Task 1 (sezioni HRV + PMC, helper esistenti, import già presenti)
    - coach/analytics/risk.py — riga ~246 (`to_rome_date(a.get("started_at"))` — il fix B4 da rispecchiare nel bucketing dello script)
    - .planning/phases/01-test-suite-analytics-correctness/01-RESEARCH.md — Code Examples §4 (risk volume bucketing con to_rome_date) e Code Examples §1 (campi readiness_score/readiness_label da daily_metrics)
    - .planning/phases/01-test-suite-analytics-correctness/01-PATTERNS.md — output format esatto delle sezioni Readiness e Risk
  </read_first>
  <action>
    Aggiungere la sezione Readiness (verifica ANALYTICS-04): leggere da `daily_metrics` (riousare la riga di oggi già fetchata in Task 1 oppure rifare la query) i campi `readiness_score,readiness_label`. Stampare `=== Readiness ===` con `Score: {readiness_score}/100 | Label: {readiness_label}`. Se la riga manca, stampare "Nessun dato readiness per oggi" (non un errore — A1 RESEARCH.md).

    Aggiungere la sezione Risk volume bucketing (verifica ANALYTICS-05 / fix B4): query `activities` selezionando `started_at,sport,duration_s` da (oggi - 14 giorni) con `gte("started_at", f"{since}T00:00:00Z")`. Calcolare `today = today_rome()`, `this_week_start = today - timedelta(days=today.weekday())`. Per ogni activity calcolare `d = to_rome_date(a.get("started_at"))` (questo È la verifica B4: usa to_rome_date, non slicing `[:10]`); se `d is not None and d >= this_week_start`, accumulare il volume per `sport`. Stampare `=== Risk: Volume Bucketing (settimana corrente) ===` con i volumi per disciplina e il suffisso `(date: Europe/Rome)`, nel formato di PATTERNS.md (es. `swim: ... | bike: ... | run: ...`).

    Wiring `main()`: definire `def main() -> None:` che chiama `logging.basicConfig(level=logging.INFO, format="%(levelname)s %(message)s")`, ottiene `sb = get_supabase()`, calcola `today_iso = today_rome().isoformat()`, e invoca le 4 sezioni in ordine (HRV, PMC, Readiness, Risk), ciascuna nel proprio try/except. Aggiungere `if __name__ == "__main__": main()` (NON `sys.exit(main())` con codice — D-02: nessun gate exit 1).
  </action>
  <verify>
    <automated>python -c "import ast; t=ast.parse(open('scripts/verify_analytics.py', encoding='utf-8').read()); names={n.name for n in ast.walk(t) if isinstance(n, ast.FunctionDef)}; assert 'main' in names, names; print('main ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: il file contiene le stringhe esatte `=== Readiness ===` e `=== Risk: Volume Bucketing (settimana corrente) ===`.
    - Source assertion: la sezione Risk usa `to_rome_date(` per il bucketing (verifica B4) e contiene il suffisso `Europe/Rome`.
    - Source assertion: il file contiene `def main` e il blocco `if __name__ == "__main__":` che chiama `main()` senza `sys.exit(1)`.
    - Test command: lo snippet ast verifica che la funzione `main` esista (esce 0).
    - Behavior (manuale, vedi Task 3): le 4 sezioni vengono stampate nell'ordine HRV → PMC → Readiness → Risk.
  </acceptance_criteria>
  <done>Lo script ha tutte e 4 le sezioni, `main()` le orchestra in ordine, ed è informativo (nessun exit gate).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verifica live dell'output su dati reali di Nicolò</name>
  <files>scripts/verify_analytics.py</files>
  <action>
    Checkpoint di verifica umana: l'esecutore presenta lo script all'operatore (Nicolò/dev) perché lo lanci sul DB reale e confermi visivamente l'output. Nessuna modifica al codice in questa task — è la verifica live richiesta da D-01/D-08 per ANALYTICS-01/03/04/05 che NON può essere automatizzata (richiede credenziali Supabase reali, assenti in CI). Vedi `<what-built>` e `<how-to-verify>` per i dettagli.
  </action>
  <what-built>
    `scripts/verify_analytics.py` — script a 4 sezioni che legge la Supabase di produzione e stampa HRV baseline+z-score, PMC, Readiness, Risk volume bucketing per ispezione visiva (D-01/D-08). Verifica i fix B1 (baseline per data), B3 (PMC None su cold-start), B4 (risk bucketing data Rome) su dati reali.
  </what-built>
  <how-to-verify>
    Prerequisito: `.env` locale con `SUPABASE_URL` e `SUPABASE_SERVICE_KEY` (Supabase Dashboard -> Project Settings -> API). Senza `.env` lo script solleva `KeyError: SUPABASE_URL` — atteso, non è un bug del codice.

    1. Da `C:\dev\triathlon-coach`, esegui: `python scripts/verify_analytics.py`
    2. Conferma che vengano stampate tutte e 4 le sezioni nell'ordine: `=== HRV Analytics ===`, `=== PMC ===`, `=== Readiness ===`, `=== Risk: Volume Bucketing (settimana corrente) ===`.
    3. Verifica ANALYTICS-01 (B1): la riga HRV indica "(N giorni, oggi escluso)" — oggi NON è nel conteggio baseline. Se < 7 giorni di storico, accetta "Dati storici insufficienti".
    4. Verifica ANALYTICS-03 (B3): nella sezione PMC, se i valori sono assenti devono comparire come `None` (con la nota Phase 2), NON come `0.00`. Un `CTL: 0.00` segnalerebbe il bug B3 non risolto.
    5. Verifica ANALYTICS-04: la sezione Readiness mostra uno Score tra 0 e 100 e una Label leggibile ("ready"/"caution"/"rest"), non `(None)`.
    6. Verifica ANALYTICS-05 (B4): la sezione Risk mostra i volumi per disciplina con suffisso `(date: Europe/Rome)` e NON crasha.
    7. Verifica sicurezza (T-02-01): l'output NON contiene mai l'URL Supabase né la service key.
  </how-to-verify>
  <verify>
    <human-check>L'operatore esegue `python scripts/verify_analytics.py` e conferma le 4 sezioni con i criteri B1/B3/B4 sopra elencati.</human-check>
  </verify>
  <acceptance_criteria>
    - Behavior: `python scripts/verify_analytics.py` stampa 4 sezioni nell'ordine atteso senza crashare (un errore isolato in una sezione non interrompe le altre).
    - Behavior: la sezione HRV mostra "oggi escluso" (B1); la sezione PMC mostra None-non-0 quando i dati mancano (B3); la sezione Risk usa date Europe/Rome senza crash (B4).
    - Behavior: nessun segreto (URL/service key) compare nell'output.
  </acceptance_criteria>
  <done>L'operatore conferma "approved": le 4 sezioni sono corrette e i fix B1/B3/B4 si comportano come atteso su dati reali.</done>
  <resume-signal>Scrivi "approved" se l'output è corretto, oppure descrivi quale sezione è anomala (es. "CTL=0.00 invece di None", "manca sezione Risk", "crash su started_at").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operatore locale → Supabase produzione | `verify_analytics.py` legge il DB reale con la service key da `.env`. Connessione di sola lettura (SELECT), nessuna scrittura. |
| script → stdout | I valori analytics finiscono su stdout/log; i segreti di connessione NON devono mai finirci. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | `scripts/verify_analytics.py` stdout/log | mitigate | Lo script stampa SOLO dati analytics; mai `SUPABASE_URL` né `SUPABASE_SERVICE_KEY`. Asserzione di sorgente in Task 1 verifica l'assenza di quelle stringhe. `logging.basicConfig` non logga env vars. |
| T-02-02 | Information Disclosure | `.env` con credenziali Supabase | mitigate | `.env` è già in `.gitignore` (pattern standard del progetto — RESEARCH.md Security). NON aggiungere `.env` né credenziali a `scripts/verify_analytics.py`; le credenziali sono lette a runtime via `load_dotenv()`. |
| T-02-03 | Tampering | scrittura accidentale su `planned_sessions`/`daily_metrics` | accept | Lo script è di sola lettura (solo `.select().execute()`); nessuna chiamata `.upsert`/`.insert`/`.update`. Rischio nullo per design (RESEARCH.md: "Phase 1 è di sola lettura"). |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Nessun package install (RESEARCH.md: "No New Packages Required" — tutti gli import già in requirements.txt). T-SC non applicabile. |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('scripts/verify_analytics.py', encoding='utf-8').read())"` → sintassi valida
- `python scripts/verify_analytics.py` (con `.env`) → 4 sezioni stampate, nessun crash, nessun segreto (checkpoint Task 3)
- Source grep: nessuna occorrenza di `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`/`sys.exit(1)` nello script
</verification>

<success_criteria>
- `scripts/verify_analytics.py` esiste, è committato, e stampa 4 sezioni (HRV, PMC, Readiness, Risk) su dati reali.
- L'ordering `load_dotenv()` prima degli import `coach.*` è rispettato (no lru_cache lock).
- HRV baseline esclude oggi per data (B1); PMC stampa None-non-0 (B3); Risk usa date Europe/Rome (B4).
- Nessun segreto stampato; nessun exit 1 automatico (D-02).
</success_criteria>

<output>
Create `.planning/phases/01-test-suite-analytics-correctness/01-02-SUMMARY.md` when done
</output>
