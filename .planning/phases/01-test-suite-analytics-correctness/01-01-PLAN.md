---
phase: 01-test-suite-analytics-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_audit_resilience.py
autonomous: true
requirements:
  - VERIFY-01
  - ANALYTICS-02
  - ANALYTICS-04
user_setup: []

must_haves:
  truths:
    - "L'intera suite pytest passa verde localmente (0 failures) — VERIFY-01"
    - "Esiste un test che prova: readiness_label non-null e readiness_score 0-100 quando PMC è assente — ANALYTICS-04"
    - "I tre test B2 esistenti dimostrano fatigue_warning dopo 2 giorni e non 1 — ANALYTICS-02"
  artifacts:
    - path: "tests/test_audit_resilience.py"
      provides: "Nuovo metodo test_b3_readiness_label_not_null per ANALYTICS-04"
      contains: "def test_b3_readiness_label_not_null"
  key_links:
    - from: "tests/test_audit_resilience.py::test_b3_readiness_label_not_null"
      to: "coach/analytics/daily.py::compute_for"
      via: "_make_daily_module(sb) injection + sb.last_upsert capture"
      pattern: "_make_daily_module\\(sb\\)"
---

<objective>
Aggiungere la copertura unit mancante per ANALYTICS-04 e confermare che l'intera suite pytest sia verde — chiudendo la parte "test layer" di Phase 1.

Purpose: ANALYTICS-04 (readiness_label non-null + readiness_score clamped 0-100 quando PMC è assente) è l'unico requisito Phase 1 senza copertura unit esistente. Senza questo test, la regressione del bug "label (None) / score fuori range su cold-start" non è bloccata. VERIFY-01 e ANALYTICS-02 sono già coperti — questo plan li ri-verifica come gate.

Output: un nuovo metodo `test_b3_readiness_label_not_null` in `tests/test_audit_resilience.py` (per D-06), suite completa verde.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-suite-analytics-correctness/01-CONTEXT.md
@.planning/phases/01-test-suite-analytics-correctness/01-RESEARCH.md
@.planning/phases/01-test-suite-analytics-correctness/01-PATTERNS.md
</context>

## Phase Goal

**As an** operatore del sistema (Nicolò/dev), **I want to** una suite pytest verde con una regressione che blocca readiness_label null / score fuori range su cold-start, **so that** posso fidarmi che la logica readiness produca sempre un'etichetta leggibile e uno score valido anche senza dati PMC.

## Artifacts this phase produces

Symbols/files created or modified by this plan:
- `tests/test_audit_resilience.py` — nuovo metodo `def test_b3_readiness_label_not_null()` (aggiunto, file già esistente)

Nessun nuovo simbolo di produzione (zero modifiche a `coach/`). Il test consuma simboli già esistenti: `_FakeSupabase`, `_make_daily_module`, `daily.compute_for`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Aggiungere test_b3_readiness_label_not_null per ANALYTICS-04</name>
  <files>tests/test_audit_resilience.py</files>
  <read_first>
    - tests/test_audit_resilience.py — leggere righe 31-130 (helper `_load`, `_FakeSupabase`, `_make_daily_module`) e righe 190-206 (`test_b3_missing_pmc_does_not_score_tsb_optimal` e il divisore di sezione B11 che segue)
    - coach/analytics/readiness.py — righe 195-235 (mapping label: i valori esatti sono "ready", "caution", "rest")
    - coach/analytics/daily.py — funzione `compute_for` (cosa scrive in `daily_metrics`: campi `ctl`, `readiness_label`, `readiness_score`)
    - .planning/phases/01-test-suite-analytics-correctness/01-PATTERNS.md — sezione "New test to add" (righe 137-161) con il corpo esatto del test
  </read_first>
  <behavior>
    - Test: con `activities: []` (PMC assente) e una sola riga `daily_wellness`, `compute_for(day)` scrive `m["ctl"] is None` (path None-PMC esercitato).
    - Test: `m["readiness_label"]` ∈ {"ready", "caution", "rest"} (mai None, mai stringa vuota).
    - Test: `m["readiness_score"]` è `int` ed è nel range chiuso 0-100.
  </behavior>
  <action>
    Inserire un nuovo metodo `def test_b3_readiness_label_not_null()` in `tests/test_audit_resilience.py` immediatamente dopo `test_b3_missing_pmc_does_not_score_tsb_optimal` (che termina a riga 205) e PRIMA del divisore di sezione B11 (riga 208). Per D-06: metodo separato, non modificare il test B3 esistente.

    Riusare ESATTAMENTE l'infrastruttura esistente nel file (NON reimportare, NON ridefinire): `_FakeSupabase`, `_make_daily_module`. Costruire `sb = _FakeSupabase({"activities": [], "daily_wellness": wellness, "subjective_log": []})` con `day = date(2026, 5, 30)` e una riga wellness `{"date": day.isoformat(), "hrv_rmssd": 55.0, "sleep_score": 80, "body_battery_max": 80, "resting_hr": 50}`. Chiamare `daily = _make_daily_module(sb)` FRESCO in questo test (non riusare istanze tra test — RESEARCH.md Pitfall 1) e poi `daily.compute_for(day)`. Leggere `m = sb.last_upsert`.

    Asserzioni (con messaggi diagnostici espliciti come da PATTERNS.md righe 139-159):
    - `assert m["ctl"] is None` (commento: PMC assente — forza il path None per D-06 / Pitfall 4)
    - `assert m["readiness_label"] in {"ready", "caution", "rest"}` con messaggio che stampa il valore ottenuto
    - `assert isinstance(m["readiness_score"], int)` con messaggio che stampa il tipo
    - `assert 0 <= m["readiness_score"] <= 100` con messaggio che stampa il valore

    Vincolo critico (Pitfall 4): mantenere `activities: []` vuoto. Aggiungere activities produrrebbe un PMC valido e NON eserciterebbe il path None-PMC che ANALYTICS-04 deve coprire. NON usare `unittest.mock.patch` su `get_supabase` (lru_cache leak — RESEARCH.md Anti-Patterns); l'iniezione avviene solo via `_make_daily_module`.
  </action>
  <verify>
    <automated>set PYTHONPATH=. && python -m pytest tests/test_audit_resilience.py::test_b3_readiness_label_not_null -v</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `tests/test_audit_resilience.py` contiene `def test_b3_readiness_label_not_null` posizionato dopo `test_b3_missing_pmc_does_not_score_tsb_optimal` e prima del commento `# B11`.
    - Source assertion: il nuovo test usa `activities: []` (lista vuota) nella costruzione di `_FakeSupabase`.
    - Source assertion: il test contiene `assert m["readiness_label"] in {"ready", "caution", "rest"}` e `assert 0 <= m["readiness_score"] <= 100`.
    - Test command: `python -m pytest tests/test_audit_resilience.py::test_b3_readiness_label_not_null -v` esce con 1 passed, 0 failed.
    - Behavior: il test verifica `m["ctl"] is None` (path None-PMC effettivamente esercitato).
  </acceptance_criteria>
  <done>Il metodo test_b3_readiness_label_not_null esiste, passa, e prova readiness_label non-null + score 0-100 con PMC assente.</done>
</task>

<task type="auto">
  <name>Task 2: Confermare suite pytest completa verde (VERIFY-01) e copertura B2 (ANALYTICS-02)</name>
  <files>tests/test_audit_resilience.py</files>
  <read_first>
    - tests/test_audit_resilience.py — i tre test B2 esistenti: `test_b2_single_low_day_does_not_warn`, `test_b2_two_consecutive_low_days_warn`, `test_b2_daily_excludes_today_from_recent_z` (verificare che esistano e siano i nomi citati in D-04)
    - .planning/phases/01-test-suite-analytics-correctness/01-RESEARCH.md — sezione "Validation Architecture" (Phase Requirements → Test Map) per i comandi esatti
  </read_first>
  <action>
    Questa è una task di gate-verification (nessuna modifica al codice di produzione, per D-04/D-05: nessun nuovo test B2 richiesto, la copertura attuale è sufficiente).

    Eseguire la suite completa con `PYTHONPATH=.` (non esiste `setup.py`, per code_context CONTEXT.md). Confermare 0 failures. Confermare esplicitamente che i tre test B2 nominati in D-04 esistono e passano: `test_b2_single_low_day_does_not_warn` (1 giorno basso → nessun warning), `test_b2_two_consecutive_low_days_warn` (2 giorni consecutivi → warning), `test_b2_daily_excludes_today_from_recent_z` (oggi escluso da recent_z_scores).

    Se la suite NON è verde: NON modificare codice di produzione in `coach/` per farla passare senza prima documentare il failure nel SUMMARY. Phase 1 assume la suite già verde (172 test, RESEARCH.md). Un failure inatteso è un segnale da riportare, non da sopprimere.
  </action>
  <verify>
    <automated>set PYTHONPATH=. && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - Test command: `python -m pytest tests/ -q` riporta 0 failed (tutti passed/skipped).
    - Test command: `python -m pytest tests/test_audit_resilience.py -k "b2" -v` riporta 3 passed (i tre test B2 nominati in D-04).
    - CLI output: il conteggio totale dei test passati è ≥ 173 (172 preesistenti + il nuovo test_b3_readiness_label_not_null della Task 1).
  </acceptance_criteria>
  <done>`pytest tests/ -q` è verde con 0 failures; i 3 test B2 confermati passanti.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nessuno | Plan 01 non attraversa alcun trust boundary: opera interamente su `_FakeSupabase` (dict in-memory), nessuna connessione DB reale, nessun input utente, nessun segreto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | test che importa daily.py direttamente → connessione DB reale con credenziali | mitigate | Usare esclusivamente `_make_daily_module(sb)` (sys.modules injection); MAI import diretto di `coach.analytics.daily` né `unittest.mock.patch` su `get_supabase` (lru_cache leak — RESEARCH.md Anti-Patterns). Il test gira con `_FakeSupabase` senza `.env`. |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Nessun package install in questo plan (RESEARCH.md: "No New Packages Required"). T-SC non applicabile. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` → 0 failures (VERIFY-01)
- `python -m pytest tests/test_audit_resilience.py::test_b3_readiness_label_not_null -v` → passed (ANALYTICS-04)
- `python -m pytest tests/test_audit_resilience.py -k "b2" -v` → 3 passed (ANALYTICS-02)
</verification>

<success_criteria>
- La suite pytest completa è verde (0 failures) con almeno 173 test passati.
- `test_b3_readiness_label_not_null` esiste, prova `readiness_label` ∈ {"ready","caution","rest"} e `readiness_score` int 0-100 con `ctl is None`.
- Nessuna modifica a file di produzione in `coach/`.
</success_criteria>

<output>
Create `.planning/phases/01-test-suite-analytics-correctness/01-01-SUMMARY.md` when done
</output>
